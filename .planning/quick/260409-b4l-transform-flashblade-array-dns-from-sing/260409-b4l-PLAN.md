---
phase: quick
plan: 260409-b4l
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/client/array_admin.go
  - internal/client/array_admin_test.go
  - internal/client/models_admin.go
  - internal/testmock/handlers/array_admin.go
  - internal/provider/array_dns_resource.go
  - internal/provider/array_dns_resource_test.go
  - internal/provider/array_dns_data_source.go
  - internal/provider/array_dns_data_source_test.go
  - examples/resources/flashblade_array_dns/resource.tf
  - examples/resources/flashblade_array_dns/import.sh
  - examples/data-sources/flashblade_array_dns/data-source.tf
autonomous: true
must_haves:
  truths:
    - "Client CRUD methods accept a name parameter and use ?names= query param"
    - "DeleteArrayDns exists and sends DELETE to /dns?names="
    - "Mock handler uses byName map instead of singleton"
    - "Resource has name attribute with RequiresReplace plan modifier"
    - "Schema version 1 with v0→v1 state upgrader"
    - "Import uses DNS config name, not hardcoded 'default'"
    - "Data source requires name attribute for lookup"
    - "All tests pass with TestUnit_ prefix naming"
  artifacts:
    - path: "internal/client/array_admin.go"
      provides: "Named CRUD methods for DNS"
    - path: "internal/testmock/handlers/array_admin.go"
      provides: "Multi-instance DNS mock handler with byName map"
    - path: "internal/provider/array_dns_resource.go"
      provides: "Named resource with schema v1, state upgrader, drift detection on name"
  key_links:
    - from: "internal/provider/array_dns_resource.go"
      to: "internal/client/array_admin.go"
      via: "GetArrayDns(ctx, name), PostArrayDns(ctx, name, body), etc."
      pattern: "client\\.\\w+ArrayDns\\(ctx,\\s*name"
    - from: "internal/provider/array_dns_data_source.go"
      to: "internal/client/array_admin.go"
      via: "GetArrayDns(ctx, name)"
      pattern: "client\\.GetArrayDns\\(ctx,\\s*name"
---

<objective>
Transform flashblade_array_dns from singleton pattern to standard named resource pattern.

Purpose: DNS configs are named entities in the FlashBlade API. The singleton pattern prevents managing multiple DNS configs and breaks import/drift semantics.
Output: Named resource with full CRUD, schema v1 with state upgrader, updated mock/tests/docs.
</objective>

<execution_context>
@.claude/skills/flashblade-resource-builder/SKILL.md
@CONVENTIONS.md
</execution_context>

<context>
@internal/client/array_admin.go
@internal/client/array_admin_test.go
@internal/client/models_admin.go
@internal/testmock/handlers/array_admin.go
@internal/provider/array_dns_resource.go
@internal/provider/array_dns_resource_test.go
@internal/provider/array_dns_data_source.go
@internal/provider/array_dns_data_source_test.go
@examples/resources/flashblade_array_dns/resource.tf
@examples/resources/flashblade_array_dns/import.sh
@examples/data-sources/flashblade_array_dns/data-source.tf

Reference patterns (named resource CRUD, mock handler, tests):
@internal/client/remote_credentials.go
@internal/client/remote_credentials_test.go
@internal/testmock/handlers/remote_credentials.go
@internal/provider/remote_credentials_resource.go (for state upgrader v0→v1 pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Client + Models + Mock — Named DNS CRUD</name>
  <files>
    internal/client/array_admin.go,
    internal/client/array_admin_test.go,
    internal/client/models_admin.go,
    internal/testmock/handlers/array_admin.go
  </files>
  <action>
  **Models (models_admin.go):**
  - ArrayDnsPost: add `Name` field with `json:"-"` (name goes via query param, excluded from body). Keep existing Domain, Nameservers, Services, Sources fields.
  - ArrayDns and ArrayDnsPatch: no changes needed (Name already in GET struct, Patch fields already pointers).

  **Client (array_admin.go):**
  - Change ALL DNS method signatures to accept `name string` parameter:
    - `GetArrayDns(ctx, name)` — use `getOneByName[ArrayDns](c, ctx, "/dns?names="+url.QueryEscape(name), "DNS", name)`
    - `PostArrayDns(ctx, name, body)` — use `postOne[ArrayDnsPost, ArrayDns](c, ctx, "/dns?names="+url.QueryEscape(name), body, "PostArrayDns")`
    - `PatchArrayDns(ctx, name, body)` — use `patchOne[ArrayDnsPatch, ArrayDns](c, ctx, "/dns?names="+url.QueryEscape(name), body, "PatchArrayDns")`
    - Add `DeleteArrayDns(ctx, name)` — `return c.delete(ctx, "/dns?names="+url.QueryEscape(name))` with error wrap `fmt.Errorf("DeleteArrayDns: %w", err)`
  - Remove old singleton GET pattern (hand-rolled list+filter). Use `getOneByName[T]` generic.
  - Remove old POST pattern (hand-rolled ListResponse unwrap). Use `postOne[T,R]` generic.
  - Remove old PATCH pattern. Use `patchOne[T,R]` generic.

  **Mock handler (handlers/array_admin.go):**
  - Replace singleton `dns *client.ArrayDns` field with standard store pattern:
    ```go
    type arrayDnsStore struct {
        mu     sync.Mutex
        byName map[string]*client.ArrayDns
        nextID int
    }
    ```
  - Add `Seed(item *client.ArrayDns)` method.
  - Update `RegisterArrayAdminHandlers` to return `*arrayDnsStore` (in addition to existing NTP/SMTP stores if any — check current return signature).
  - GET: filter by `?names=` param, return empty list on miss (HTTP 200).
  - POST: require `?names=` param, 409 on conflict, generate ID.
  - PATCH: require `?names=`, apply non-nil fields, 404 on miss.
  - DELETE: require `?names=`, remove from store, 404 on miss.
  - Use shared helpers: `ValidateQueryParams`, `RequireQueryParam`, `WriteJSONListResponse`, `WriteJSONError`.

  **Client tests (array_admin_test.go):**
  - Rename existing tests to `TestUnit_` prefix:
    - `TestUnit_ArrayDns_Get_Found`
    - `TestUnit_ArrayDns_Get_NotFound`
    - `TestUnit_ArrayDns_Patch`
  - Add missing tests:
    - `TestUnit_ArrayDns_Post` — POST with name, verify returned object
    - `TestUnit_ArrayDns_Delete` — DELETE by name, verify success
  - All tests use `newTestClient(t, srv)` helper pattern.
  - Total: 5 client tests (was 3).
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && go test ./internal/client/ -run TestUnit_ArrayDns -count=1 -v && go test ./internal/testmock/... -count=1</automated>
  </verify>
  <done>Client has 4 named CRUD methods using generics, mock handler uses byName map with all 4 HTTP methods, 5 client tests pass with TestUnit_ prefix.</done>
</task>

<task type="auto">
  <name>Task 2: Resource + Data Source + Tests + Docs — Named Pattern</name>
  <files>
    internal/provider/array_dns_resource.go,
    internal/provider/array_dns_resource_test.go,
    internal/provider/array_dns_data_source.go,
    internal/provider/array_dns_data_source_test.go,
    examples/resources/flashblade_array_dns/resource.tf,
    examples/resources/flashblade_array_dns/import.sh,
    examples/data-sources/flashblade_array_dns/data-source.tf
  </files>
  <action>
  **Resource (array_dns_resource.go):**
  - Add `name` attribute to schema: Required, StringPlanModifier RequiresReplace().
  - Add `name` field to `arrayDnsModel` struct: `Name types.String \`tfsdk:"name"\``
  - Bump SchemaVersion from 0 to 1.
  - Add `arrayDnsV0Model` intermediate struct (all current fields minus `name`, plus Timeouts).
  - Implement `UpgradeState` with v0→v1 upgrader:
    - PriorSchema: exact copy of v0 schema (no `name` attribute).
    - StateUpgrader: read old state into `arrayDnsV0Model`, create `arrayDnsModel` with `Name: types.StringValue("")` (or call API to get actual name — prefer setting empty and letting Read fill it on next refresh).
    - Actually: since name is Required+RequiresReplace, set `Name: types.StringValue("default")` as a sensible migration default. The next `terraform plan` will show the real name if it differs.
  - **Create**: Remove singleton GET-first logic. Standard POST: `PostArrayDns(ctx, name, body)`. Read back into state.
  - **Read**: `GetArrayDns(ctx, name)`. Add drift detection for `name` field. Keep existing drift detection for other fields.
  - **Update**: `PatchArrayDns(ctx, name, body)`. Read back.
  - **Delete**: Replace "reset to defaults" PATCH with real `DeleteArrayDns(ctx, name)`.
  - **ImportState**: Import by name (not "default"). Use `req.ID` as name. Call `GetArrayDns(ctx, name)`. Map fields. Set `Timeouts = nullTimeoutsValue()`.

  **Data source (array_dns_data_source.go):**
  - Add `name` attribute: Required.
  - Add `Name` field to `arrayDnsDataSourceModel`.
  - Read: get name from config, call `GetArrayDns(ctx, name)`.
  - Handle not-found with AddError.

  **Resource tests (array_dns_resource_test.go):**
  - Update all test helpers (`buildArrayDnsType`, `nullArrayDnsConfig`, etc.) to include `name` field.
  - Rename tests to `TestUnit_` prefix if not already.
  - Update Lifecycle test: Create with name, Read, Update mutable fields, Delete (real delete).
  - Update Import test: import by name.
  - Update DriftDetection test: include name in seeded data.
  - Add `TestUnit_ArrayDnsResource_StateUpgrade_V0toV1` — verify v0 state (no name) upgrades to v1 state (name = "default").
  - Remove tests for singleton-specific behavior (ImportIdempotency with "default" ID, PlanModifiers for computed-only name).

  **Data source tests (array_dns_data_source_test.go):**
  - Rename to `TestUnit_ArrayDnsDataSource_Basic`.
  - Update to provide `name` in config and seed data.

  **HCL examples:**
  - `resource.tf`: add `name = "dns-config"` attribute.
  - `import.sh`: change from `terraform import flashblade_array_dns.example default` to `terraform import flashblade_array_dns.example dns-config`.
  - `data-source.tf`: add `name = "dns-config"` attribute.

  **Docs:** Run `make docs` to regenerate.

  **Test baseline:** Current 752. Adding 2 new client tests + 1 state upgrader test = ~755 minimum. Some singleton tests may be removed/merged, but net count must be >= 752.
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && make test && make lint && make docs</automated>
  </verify>
  <done>Resource has name attribute with RequiresReplace, schema v1 with working state upgrader, standard CRUD (no singleton logic), import by name. Data source requires name. All tests pass with TestUnit_ prefix, total count >= 752. Lint clean. Docs regenerated.</done>
</task>

</tasks>

<verification>
1. `make test` — all tests pass, count >= 752
2. `make lint` — 0 issues
3. `make docs` — regenerated without errors
4. Grep for singleton patterns removed: no more hardcoded "default" ID in resource, no GET-first-then-POST logic
</verification>

<success_criteria>
- flashblade_array_dns is a standard named resource (not singleton)
- Client uses getOneByName[T], postOne, patchOne generics with ?names= param
- DeleteArrayDns exists and works
- Mock handler uses byName map pattern
- Schema version 1 with v0→v1 state upgrader tested
- Import by DNS config name
- Data source requires name for lookup
- All tests pass (>= 752), lint clean, docs regenerated
</success_criteria>

<output>
After completion, update test baseline in CONVENTIONS.md if count changed.
</output>
