# Milestone Audit — v2.22.3 convention-compliance

**Audit date:** 2026-04-20
**Auditor:** autonomous audit (phase SUMMARYs cross-referenced with commits + quality gates)
**Scope:** Phases 51, 52, 53 (commit range `3254d38..HEAD`)

---

## Status: **passed (with 1 non-blocking observation)**

Milestone achieved its definition of done. All 12 requirements satisfied by shipped code with corroborating commits and passing tests. No critical gaps. One non-blocking observation: phase VERIFICATION.md files were not produced (executor agents wrote SUMMARY.md only); this audit substitutes direct SUMMARY + commit + quality-gate cross-check. Subsequent milestones should ensure executor produces both artifacts.

---

## Quality Gates

| Gate | Result | Evidence |
|---|---|---|
| `make test` | **PASS** — 779 tests (baseline 752, +27) | verified 2026-04-20 post-commit `2a53fd1` |
| `make lint` | **PASS** — 0 issues | golangci-lint v1.x clean |
| `go build ./...` | **PASS** | no compile errors |
| CI test job | **FIXED** — commit `49c6f1b` (setup-terraform@v3 pre-install) | HashiCorp GPG key expired, auto-installer bypassed |
| CI release job | **FIXED** — commit `2a53fd1` (same fix on release.yml) | same workaround applied to GoReleaser before-hook |

---

## Requirements Coverage (12/12)

| ID | Description | Phase/Plan | Commit | Status | Evidence |
|---|---|---|---|---|---|
| R-001 | `SubnetPost.VLAN *int64` (untagged `vlan=0`) | 51 / 51-02 | `d2dd828` | ✅ satisfied | `TestUnit_Subnet_Post_Vlan_Zero` + mock asserts `*int64` serialization |
| R-002 | `SubnetPatch.LinkAggregationGroup **NamedReference` | 51 / 51-02 | `d2dd828` | ✅ satisfied | `TestUnit_Subnet_Patch_ClearLAG` + state upgrader v0→v1 |
| R-003 | `FileSystemExportPatch.Server`/`.SharePolicy **NamedReference` | 51 / 51-03 | `d4dedab` | ✅ satisfied | `TestUnit_FileSystemExport_Patch_ClearSharePolicy` + state upgrader |
| R-004 | `ObjectStoreAccountExportPatch.Policy **NamedReference` | 51 / 51-04 | `a7b50cd` | ✅ satisfied | `TestUnit_ObjectStoreAccountExport_Patch_ClearPolicy` + state upgrader |
| R-005 | Schema version bumps + state upgraders for R-001..R-004 | 51 / 51-02..51-04 | `d2dd828`, `d4dedab`, `a7b50cd` | ✅ satisfied | 3 state upgrader tests: `TestUnit_Subnet_StateUpgrade_V0toV1`, `TestUnit_FileSystemExport_StateUpgrade_V0toV1`, `TestUnit_ObjectStoreAccountExport_StateUpgrade_V0toV1` |
| R-006 | `QosPolicyPost` scalar pointer fixes + `json:"-"` on Name | 52 / 52-01 | `4b83e27` | ✅ satisfied | `TestUnit_QosPolicyPost_JSONEncoding` asserts wire format, `TestUnit_QosPolicyResource_StateUpgrade_V0toV1` |
| R-007 | `ResourceWithUpgradeState` assertion on 9 resources | 52 / 52-01 + 52-02 | `4b83e27`, `5e1d9b1` | ✅ satisfied | qos_policy via 52-01, 7 others via 52-02; subnet was already compliant post-51 |
| R-008 | `object_store_access_key` ImportState shim + CONVENTIONS.md exception | 52 / 52-03 | `be99a39`, `1841434` | ✅ satisfied | `TestUnit_AccessKey_ImportRejected` + CONVENTIONS.md §Resource Implementation paragraph |
| R-009 | `NfsExportPolicyRulePatch.Security *[]string` | 52 / 52-04 | `6804f59` | ✅ satisfied | `TestUnit_NfsExportPolicyRule_Patch_Security_Clear` + `TestUnit_NfsExportPolicyRuleResource_StateUpgrade_V0toV1` |
| R-010 | Test naming normalization (6 renames) | 52 / 52-05 | `c2e2f3e` | ✅ satisfied | `grep -rE "^func (TestMocked_\|TestCompute)" internal/` → 0 matches |
| R-011 | PATCH slice `*[]string` in 3 policy rules + NetworkInterfacePatch exception documented | 53 / 53-01 | `97a603e` | ✅ satisfied | 3 `TestUnit_*_Patch_ClearList` tests + CONVENTIONS.md §Pointer rules "always send" subsection |
| R-012 | Handler hygiene (`byName`, `nextID` pattern) | 53 / 53-02 | `f153ee2` | ✅ satisfied | qos handler renamed, subnets/network_interfaces use `nextID` counter |

**Orphan check:** 0 requirements in REQUIREMENTS.md were left unassigned or unverified.

---

## Bonus Deliverables (outside original scope, committed in-milestone)

These were discovered mid-milestone and folded in rather than deferred:

| Fix | Commit | Rationale |
|---|---|---|
| Lifecycle rule `enabled=false` Create bug | `b60128f` | User reported during milestone. Same R-006-class pattern (API default `true` for `enabled`); swagger `LifecycleRulePost` doesn't accept `enabled` field — fixed via follow-up PATCH in resource Create(). Added `TestUnit_LifecycleRuleResource_Create_Disabled` |
| CI test job: pre-install Terraform | `49c6f1b` | HashiCorp GPG key expired breaking terraform-plugin-testing auto-installer. `setup-terraform@v3` bypass |
| CI release job: same fix on release.yml | `2a53fd1` | Same workaround for GoReleaser before-hook running tests |

---

## Cross-Phase Integration

Phase dependencies were respected:
- Phase 51 → Phase 52: `doublePointerRefForPatch` helper (51-01) consumed implicitly by no Phase 52 plans (they migrate slices, not refs). Phase 52 QoS upgrader shares the state-upgrader pattern established in 51-02..51-04. ✅ integrated.
- Phase 52 → Phase 53: CONVENTIONS.md §Pointer rules extended in 52-01 (for `*bool`/`*int64` clarification from pre-milestone update), then extended again in 53-01 (for `*[]T` and "always send" exception). ✅ integrated.
- All three phases share the no-op identity state upgrader idiom (`xxxModel(oldState)` type conversion instead of field-by-field copy). Pattern verified consistent across 4 migrations (Subnet, FileSystemExport, ObjectStoreAccountExport, QosPolicy, NfsExportPolicyRule).

**Drift detection:** Read handlers on affected resources still emit `tflog.Debug` with `resource`/`field`/`was`/`now` keys — PATCH pointer changes are wire-format-only, Read path untouched. ✅ no regression.

---

## CONVENTIONS.md Evolution Summary

Three additions during this milestone (committed chronologically):

1. **POST struct exceptions** (pre-milestone, after audit false-positives): `*bool` allowed when API default non-zero; `*int64`/`*string` allowed for semantic zero; pointers on nested structs and optional refs.
2. **Resource Implementation exception for access keys**: documented write-once-secret rationale and the reject-shim pattern (52-03).
3. **PATCH slice `*[]T`** with `omitempty` + **"always send" exception** for `NetworkInterfacePatch.Services`/`.AttachedServers` (53-01).

The convention document is now more defensible against regression audits.

---

## Observations / Tech Debt Captured

1. **VERIFICATION.md artifacts missing** (non-blocking): executor agents wrote SUMMARY.md but did not produce the separate VERIFICATION.md that audit-milestone expects. The SUMMARY files cover the verification content; subsequent milestones should either update templates or enforce VERIFICATION.md generation.
2. **STATE.md metadata regression**: during the milestone, `gsd-tools` operations intermittently reset `milestone: v1.0` and `milestone_name: milestone` in STATE.md (see 2026-04-20T08:11 linter-applied modification). This is a GSD tooling bug; tracked outside this milestone.
3. **LSP stale diagnostics** observed repeatedly (reported compile errors that `go build` did not reproduce). No source-level bug; editor/LSP cache issue. Not actionable here.

---

## Gate for `/gsd:complete-milestone`

**Verdict:** ✅ **AUDIT PASSED** — ready to archive.
