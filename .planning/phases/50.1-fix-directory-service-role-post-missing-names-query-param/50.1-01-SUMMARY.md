---
phase: 50.1-fix-directory-service-role-post-missing-names-query-param
plan: "01"
subsystem: api
tags: [directory-service, roles, post, names-query-param, mock-handler, client]

requires:
  - phase: 50-directory-service-roles-role-mappings
    provides: Initial (broken) PostDirectoryServiceRole + DSR mock handler + DSR client tests
provides:
  - PostDirectoryServiceRole signature with name parameter and ?names= query param
  - Mock POST handler requiring ?names= via RequireQueryParam (matches real API)
  - Updated client test verifying ?names= contract
affects:
  - 50.1-02 (resource schema + Create wiring — now needs to pass data.Name.ValueString())
  - 50.1-03 (docs + examples + phase artifact corrections)

tech-stack:
  added: []
  patterns:
    - "POST-with-?names=: client sends name via query param, mock handler requires it, store keyed by query value"

key-files:
  created: []
  modified:
    - internal/client/directory_service_roles.go
    - internal/testmock/handlers/directory_service_roles.go
    - internal/client/directory_service_roles_test.go

key-decisions:
  - "D-01 applied: PostDirectoryServiceRole(ctx, name, body) — name via ?names= query param (matches PostTarget canonical pattern)"
  - "D-02 applied: no Name field added to DirectoryServiceRolePost struct — name via query param only"
  - "D-03 applied: replaced misleading 'server-generates' comment with standard doc comment"
  - "D-10 applied: mock POST handler now rejects missing ?names= with 400, uses query value as store key, preserves 409 Conflict path"

patterns-established:
  - "Synthesis removal: when fixing a POST handler that hallucinated name derivation, remove the synthesis logic AND the unused strings import in the same commit"

requirements-completed: []

duration: 2 min
completed: 2026-04-17
---

# Phase 50.1 Plan 01: Fix DSR client POST + mock handler Summary

**Client-layer root-cause fix for HTTP 400 'Names query parameter is missing' — PostDirectoryServiceRole now forwards name via ?names= query param and the mock handler requires it, matching the real FlashBlade API contract.**

## Performance

- **Duration:** 2 min
- **Started:** 2026-04-17T11:44:37Z
- **Completed:** 2026-04-17T11:47:22Z
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments

- Fixed `PostDirectoryServiceRole` signature to accept `name string` and build `/directory-services/roles?names=<name>` (matches PostTarget canonical pattern)
- Replaced mock POST name-synthesis logic with `RequireQueryParam(w, r, "names")`; store is now keyed by the query-param value (matches real API)
- Updated `TestUnit_DirectoryServiceRole_Post` to assert `?names=my-role` is sent and the returned role carries the passed name
- 5 DSR client tests pass (Get_Found, Get_NotFound, Post, Patch_Group, Delete); 320 client tests pass total; `golangci-lint` clean on client + testmock

## Task Commits

1. **Task 1: Fix PostDirectoryServiceRole client function** — `dca8b93` (fix)
2. **Task 2: Fix mock POST handler to require ?names=** — `78b3b3f` (fix)
3. **Task 3: Update client test for correct POST behavior** — `b466923` (test)

**Plan metadata:** _committed after SUMMARY (docs)_

## Files Created/Modified

- `internal/client/directory_service_roles.go` — added `name` param + `?names=` query to POST, replaced misleading doc comment
- `internal/testmock/handlers/directory_service_roles.go` — replaced synthesis with `RequireQueryParam("names")`, removed unused `"strings"` import
- `internal/client/directory_service_roles_test.go` — flipped the POST-names assertion from "must not have" to "must be my-role", updated call site + response name

## Decisions Made

- None beyond applied context decisions D-01, D-02, D-03, D-10 (all pre-locked in 50.1-CONTEXT.md).

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## Build & Test Status at End of Plan

- `go build ./internal/client/... ./internal/testmock/...` — Success
- `go test ./internal/client/... -count=1` — 320 tests pass
- `golangci-lint run ./internal/client/... ./internal/testmock/...` — 0 issues
- `go build ./...` — expected to FAIL until Plan 50.1-02 updates the provider call site (`internal/provider/directory_service_role_resource.go:150`). Explicitly called out in the plan's `<success_criteria>`.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Ready for Plan 50.1-02: resource schema bump (name Computed→Required), SchemaVersion 0→1 with upgrader, Create wiring to pass `data.Name.ValueString()` to the fixed client function.
- Temporary build breakage in `internal/provider/` will be resolved by Plan 50.1-02.

## Self-Check: PASSED

- `internal/client/directory_service_roles.go` — FOUND
- `internal/testmock/handlers/directory_service_roles.go` — FOUND
- `internal/client/directory_service_roles_test.go` — FOUND
- `.planning/phases/50.1-.../50.1-01-SUMMARY.md` — FOUND
- Commit `dca8b93` (Task 1) — FOUND
- Commit `78b3b3f` (Task 2) — FOUND
- Commit `b466923` (Task 3) — FOUND

---
*Phase: 50.1-fix-directory-service-role-post-missing-names-query-param*
*Completed: 2026-04-17*
