---
phase: 05-quality-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - main.go
  - GNUmakefile
  - README.md
  - .github/workflows/ci.yml
  - examples/resources/flashblade_bucket/resource.tf
  - examples/resources/flashblade_bucket/import.sh
  - examples/resources/flashblade_object_store_account/resource.tf
  - examples/resources/flashblade_object_store_account/import.sh
  - examples/resources/flashblade_object_store_access_key/resource.tf
  - examples/resources/flashblade_nfs_export_policy/resource.tf
  - examples/resources/flashblade_nfs_export_policy/import.sh
  - examples/resources/flashblade_nfs_export_policy_rule/resource.tf
  - examples/resources/flashblade_nfs_export_policy_rule/import.sh
  - examples/resources/flashblade_smb_share_policy/resource.tf
  - examples/resources/flashblade_smb_share_policy/import.sh
  - examples/resources/flashblade_smb_share_policy_rule/resource.tf
  - examples/resources/flashblade_smb_share_policy_rule/import.sh
  - examples/resources/flashblade_snapshot_policy/resource.tf
  - examples/resources/flashblade_snapshot_policy/import.sh
  - examples/resources/flashblade_snapshot_policy_rule/resource.tf
  - examples/resources/flashblade_snapshot_policy_rule/import.sh
  - examples/resources/flashblade_object_store_access_policy/resource.tf
  - examples/resources/flashblade_object_store_access_policy/import.sh
  - examples/resources/flashblade_object_store_access_policy_rule/resource.tf
  - examples/resources/flashblade_object_store_access_policy_rule/import.sh
  - examples/resources/flashblade_network_access_policy/resource.tf
  - examples/resources/flashblade_network_access_policy/import.sh
  - examples/resources/flashblade_network_access_policy_rule/resource.tf
  - examples/resources/flashblade_network_access_policy_rule/import.sh
  - examples/resources/flashblade_quota_group/resource.tf
  - examples/resources/flashblade_quota_group/import.sh
  - examples/resources/flashblade_quota_user/resource.tf
  - examples/resources/flashblade_quota_user/import.sh
  - examples/resources/flashblade_array_dns/resource.tf
  - examples/resources/flashblade_array_dns/import.sh
  - examples/resources/flashblade_array_ntp/resource.tf
  - examples/resources/flashblade_array_ntp/import.sh
  - examples/resources/flashblade_array_smtp/resource.tf
  - examples/resources/flashblade_array_smtp/import.sh
  - examples/data-sources/flashblade_bucket/data-source.tf
  - examples/data-sources/flashblade_object_store_account/data-source.tf
  - examples/data-sources/flashblade_object_store_access_key/data-source.tf
  - examples/data-sources/flashblade_nfs_export_policy/data-source.tf
  - examples/data-sources/flashblade_smb_share_policy/data-source.tf
  - examples/data-sources/flashblade_snapshot_policy/data-source.tf
  - examples/data-sources/flashblade_object_store_access_policy/data-source.tf
  - examples/data-sources/flashblade_network_access_policy/data-source.tf
  - examples/data-sources/flashblade_quota_group/data-source.tf
  - examples/data-sources/flashblade_quota_user/data-source.tf
  - examples/data-sources/flashblade_array_dns/data-source.tf
  - examples/data-sources/flashblade_array_ntp/data-source.tf
  - examples/data-sources/flashblade_array_smtp/data-source.tf
autonomous: false
requirements:
  - QUA-06

must_haves:
  truths:
    - "terraform-plugin-docs generates docs/ directory with pages for all resources and data sources"
    - "Every resource has at least one HCL example in examples/"
    - "Every importable resource has an import.sh example in examples/"
    - "Every data source has at least one HCL example in examples/"
    - "README.md documents installation, configuration, and resource list"
    - "GitHub Actions CI runs go test on push and PR"
    - "All HCL example files pass terraform fmt -check (valid formatting)"
  artifacts:
    - path: "README.md"
      provides: "Project README with installation, config, resource list"
      min_lines: 50
    - path: ".github/workflows/ci.yml"
      provides: "CI workflow"
      contains: "go test"
    - path: "docs/index.md"
      provides: "Generated provider docs index"
      min_lines: 5
  key_links:
    - from: "main.go"
      to: "terraform-plugin-docs"
      via: "go:generate directive"
      pattern: "go:generate.*tfplugindocs"
    - from: ".github/workflows/ci.yml"
      to: "go test"
      via: "test step"
      pattern: "go test ./internal"
---

<objective>
Create HCL examples (usage + import) for all resources and data sources, add go:generate directive for terraform-plugin-docs, generate docs/ directory, create GitHub Actions CI workflow, and write project README.

Purpose: QUA-06 requires auto-generated documentation for every resource and data source. The tooling (terraform-plugin-docs v0.24.0) is pinned but no examples/ files exist (except file_system), no docs/ directory exists, no CI workflow exists, and no README exists.
Output: Complete examples/ tree with usage and import examples, generated docs/ directory, CI workflow, README.
</objective>

<execution_context>
@/home/gule/.claude/get-shit-done/workflows/execute-plan.md
@/home/gule/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-quality-hardening/05-RESEARCH.md

@examples/provider/provider.tf
@examples/resources/flashblade_file_system/resource.tf
@examples/data-sources/flashblade_file_system/data-source.tf
@main.go
@GNUmakefile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HCL examples (usage + import) for all resources and data sources</name>
  <files>
    examples/resources/flashblade_bucket/resource.tf,
    examples/resources/flashblade_bucket/import.sh,
    examples/resources/flashblade_object_store_account/resource.tf,
    examples/resources/flashblade_object_store_account/import.sh,
    examples/resources/flashblade_object_store_access_key/resource.tf,
    examples/resources/flashblade_nfs_export_policy/resource.tf,
    examples/resources/flashblade_nfs_export_policy/import.sh,
    examples/resources/flashblade_nfs_export_policy_rule/resource.tf,
    examples/resources/flashblade_nfs_export_policy_rule/import.sh,
    examples/resources/flashblade_smb_share_policy/resource.tf,
    examples/resources/flashblade_smb_share_policy/import.sh,
    examples/resources/flashblade_smb_share_policy_rule/resource.tf,
    examples/resources/flashblade_smb_share_policy_rule/import.sh,
    examples/resources/flashblade_snapshot_policy/resource.tf,
    examples/resources/flashblade_snapshot_policy/import.sh,
    examples/resources/flashblade_snapshot_policy_rule/resource.tf,
    examples/resources/flashblade_snapshot_policy_rule/import.sh,
    examples/resources/flashblade_object_store_access_policy/resource.tf,
    examples/resources/flashblade_object_store_access_policy/import.sh,
    examples/resources/flashblade_object_store_access_policy_rule/resource.tf,
    examples/resources/flashblade_object_store_access_policy_rule/import.sh,
    examples/resources/flashblade_network_access_policy/resource.tf,
    examples/resources/flashblade_network_access_policy/import.sh,
    examples/resources/flashblade_network_access_policy_rule/resource.tf,
    examples/resources/flashblade_network_access_policy_rule/import.sh,
    examples/resources/flashblade_quota_group/resource.tf,
    examples/resources/flashblade_quota_group/import.sh,
    examples/resources/flashblade_quota_user/resource.tf,
    examples/resources/flashblade_quota_user/import.sh,
    examples/resources/flashblade_array_dns/resource.tf,
    examples/resources/flashblade_array_dns/import.sh,
    examples/resources/flashblade_array_ntp/resource.tf,
    examples/resources/flashblade_array_ntp/import.sh,
    examples/resources/flashblade_array_smtp/resource.tf,
    examples/resources/flashblade_array_smtp/import.sh,
    examples/data-sources/flashblade_bucket/data-source.tf,
    examples/data-sources/flashblade_object_store_account/data-source.tf,
    examples/data-sources/flashblade_object_store_access_key/data-source.tf,
    examples/data-sources/flashblade_nfs_export_policy/data-source.tf,
    examples/data-sources/flashblade_smb_share_policy/data-source.tf,
    examples/data-sources/flashblade_snapshot_policy/data-source.tf,
    examples/data-sources/flashblade_object_store_access_policy/data-source.tf,
    examples/data-sources/flashblade_network_access_policy/data-source.tf,
    examples/data-sources/flashblade_quota_group/data-source.tf,
    examples/data-sources/flashblade_quota_user/data-source.tf,
    examples/data-sources/flashblade_array_dns/data-source.tf,
    examples/data-sources/flashblade_array_ntp/data-source.tf,
    examples/data-sources/flashblade_array_smtp/data-source.tf
  </files>
  <action>
    Create the full examples/ directory tree for terraform-plugin-docs. Each example must be valid HCL that demonstrates the resource or data source.

    **Directory convention:** `examples/resources/flashblade_{type}/resource.tf` and `examples/data-sources/flashblade_{type}/data-source.tf`

    **For each resource example (resource.tf):**
    - Read the corresponding resource Schema() (from `internal/provider/{type}_resource.go`) to identify required fields and their types
    - Use the existing `flashblade_file_system` example as style reference
    - Include all Required fields with realistic placeholder values
    - Include commonly-used Optional fields with comments
    - Use `provider::flashblade` provider block reference where needed
    - Show a basic usage pattern — not every optional field

    **For each importable resource (import.sh):**
    - Per CONTEXT.md locked decision: "Comprehensive examples: Multiple HCL examples per resource — basic, advanced, import, and common patterns"
    - Create `import.sh` files showing the terraform import command with the correct ID format
    - For name-based import: `terraform import flashblade_bucket.example my-bucket`
    - For composite ID import: `terraform import flashblade_nfs_export_policy_rule.example policy_name/0`
    - For singleton import: `terraform import flashblade_array_dns.example default`
    - For quota import: `terraform import flashblade_quota_group.example fs_name/gid`
    - Skip import.sh for `object_store_access_key` (import not supported per NoImport test)

    **For each data source example (data-source.tf):**
    - Read the data source Schema() for attribute names
    - Show lookup by name (primary use case)
    - Reference the existing `flashblade_file_system` data source example for style

    **Resource families to cover (18 resources):**
    1. bucket, object_store_account, object_store_access_key (object store chain)
    2. nfs_export_policy, nfs_export_policy_rule (NFS)
    3. smb_share_policy, smb_share_policy_rule (SMB)
    4. snapshot_policy, snapshot_policy_rule (snapshot)
    5. object_store_access_policy, object_store_access_policy_rule (OAP)
    6. network_access_policy, network_access_policy_rule (NAP)
    7. quota_group, quota_user (quota)
    8. array_dns, array_ntp, array_smtp (array admin)

    **Data source families to cover (13 data sources, excl. file_system which exists):**
    bucket, object_store_account, object_store_access_key, nfs_export_policy, smb_share_policy, snapshot_policy, object_store_access_policy, network_access_policy, quota_group, quota_user, array_dns, array_ntp, array_smtp

    All HCL files must be syntactically valid. Use `terraform fmt` style (2-space indent, no tabs).

    **After creating all files, validate HCL formatting:**
    If `terraform` CLI is available, run `terraform fmt -check -recursive examples/` to verify all files are correctly formatted. If terraform is not available, ensure manual compliance with HCL formatting rules (2-space indent, `=` alignment within blocks, no trailing whitespace).
  </action>
  <verify>
    <automated>find examples/ -name "*.tf" | wc -l && find examples/ -name "import.sh" | wc -l && (terraform fmt -check -recursive examples/ 2>/dev/null || echo "terraform CLI not available - manual HCL review needed")</automated>
  </verify>
  <done>34+ total .tf files exist in examples/ (3 existing + 31 new: 18 resources + 13 data sources). 17 import.sh files exist (18 importable resources minus access_key). All HCL files pass terraform fmt -check (or are manually verified as valid).</done>
</task>

<task type="auto">
  <name>Task 2: Add go:generate directive, generate docs, create CI workflow and README</name>
  <files>main.go, GNUmakefile, README.md, .github/workflows/ci.yml</files>
  <action>
    **Step 1: go:generate directive**
    Read `main.go`. Add `//go:generate go run github.com/hashicorp/terraform-plugin-docs/cmd/tfplugindocs generate --provider-name flashblade` before the `main()` function.

    **Step 2: GNUmakefile docs target**
    Read `GNUmakefile`. Add a `docs` target if not present:
    ```
    docs:
    	go generate ./...
    ```
    Ensure the `generate` target (if it exists) also works.

    **Step 3: Generate docs/**
    Run `go generate ./...` to produce the `docs/` directory. This creates markdown files for every resource and data source from schema descriptions + examples/.

    Verify `docs/index.md` and `docs/resources/*.md` and `docs/data-sources/*.md` are created.

    **Step 4: GitHub Actions CI workflow**
    Create `.github/workflows/ci.yml`:
    - Trigger: push to main, pull_request to main
    - Job: test
      - runs-on: ubuntu-latest
      - Steps: checkout, setup-go (go-version-file: go.mod, cache: true), `go test ./internal/... -count=1 -timeout 5m`
    - Job: lint (optional, if golangci-lint is configured — check .golangci.yml exists)
      - Same setup, run `golangci-lint run ./...`
    - Job: docs-check
      - Same setup, run `go generate ./...`, then `git diff --exit-code docs/` to verify docs are up to date

    **Step 5: README.md**
    Create `README.md` at repo root with:
    - Title: Terraform Provider FlashBlade
    - Badges placeholder (CI status)
    - Overview: what this provider manages (FlashBlade storage resources via REST API v2.22)
    - Requirements: Go 1.22+, Terraform 1.x
    - Installation: `terraform { required_providers { flashblade = { source = "soulkyu/flashblade" } } }`
    - Provider Configuration: code block showing endpoint + auth options (token and oauth2)
    - Resource List: table of all 19 resources + 14 data sources with brief descriptions
    - Development: `make build`, `make test`, `make lint`, `make docs`
    - Contributing: brief section
    - License: placeholder

    Keep README under 200 lines. Practical, not verbose.
  </action>
  <verify>
    <automated>test -f docs/index.md && test -f .github/workflows/ci.yml && test -f README.md && ls docs/resources/ | wc -l</automated>
  </verify>
  <done>docs/ directory generated with pages for all resources and data sources. CI workflow exists with test + lint + docs-check jobs. README.md exists with installation, config, and resource list. `make docs` regenerates docs/.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify documentation suite quality</name>
  <files>README.md, .github/workflows/ci.yml, docs/index.md</files>
  <action>
    Human reviews the complete documentation output: README, generated docs, HCL examples, and CI workflow.

    **What was built:**
    Complete documentation suite: HCL examples (usage + import) for all 19 resources and 14 data sources, auto-generated docs/ via terraform-plugin-docs, GitHub Actions CI workflow, and project README.

    **How to verify:**
    1. Review `README.md` — check installation instructions, resource list completeness, configuration examples
    2. Browse `docs/` directory — spot-check 2-3 resource pages for attribute tables and example snippets
    3. Browse `examples/` directory — verify HCL files look realistic and use correct attribute names
    4. Check `examples/resources/*/import.sh` — verify import ID formats match resource conventions
    5. Review `.github/workflows/ci.yml` — check trigger conditions, Go version, test commands
    6. Run `go test ./internal/... -count=1 -timeout 5m` — confirm full suite passes

    **Resume signal:** Type "approved" or describe issues.
  </action>
  <verify>
    <automated>go test ./internal/... -count=1 -timeout 5m</automated>
  </verify>
  <done>Documentation suite approved by user. README is accurate, docs are complete, examples are realistic, CI workflow is correct.</done>
</task>

</tasks>

<verification>
- `go generate ./...` produces non-empty `docs/` directory
- `ls docs/resources/*.md | wc -l` >= 19 (one per resource)
- `ls docs/data-sources/*.md | wc -l` >= 14 (one per data source)
- `find examples/ -name "*.tf" | wc -l` >= 34
- `find examples/ -name "import.sh" | wc -l` >= 17
- `terraform fmt -check -recursive examples/` exits 0 (if terraform CLI available)
- `test -f .github/workflows/ci.yml` — CI workflow exists
- `test -f README.md` — README exists
- `go test ./internal/... -count=1 -timeout 5m` — full suite passes
</verification>

<success_criteria>
- terraform-plugin-docs generates complete docs/ with zero errors
- Every resource and data source has an example HCL file
- Every importable resource has an import.sh example
- Generated docs contain attribute tables and example usage sections
- All HCL files are validly formatted
- CI workflow runs tests on push/PR
- README documents installation, configuration, and all resources
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-quality-hardening/05-03-SUMMARY.md`
</output>
