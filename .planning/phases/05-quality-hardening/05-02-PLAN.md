---
phase: 05-quality-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/client/filesystems.go
  - internal/client/buckets.go
  - internal/client/object_store_accounts.go
  - internal/client/nfs_export_policies.go
  - internal/client/smb_share_policies.go
  - internal/client/snapshot_policies.go
  - internal/client/object_store_access_policies.go
  - internal/client/network_access_policies.go
  - internal/client/quotas.go
  - internal/client/object_store_access_keys.go
  - internal/client/filesystems_test.go
  - internal/provider/bucket_resource_test.go
  - internal/provider/nfs_export_policy_resource_test.go
  - internal/provider/object_store_access_policy_resource_test.go
  - internal/provider/quota_group_resource_test.go
  - internal/provider/filesystem_resource_test.go
  - internal/provider/object_store_account_resource_test.go
  - internal/provider/network_access_policy_resource_test.go
autonomous: true
requirements:
  - QUA-04
  - QUA-05

must_haves:
  truths:
    - "Client list methods auto-paginate when continuation_token is present in response"
    - "Resource Create methods produce clear diagnostics on 409 Conflict"
    - "Resource Read methods remove state on 404 for all resources (not just filesystem)"
    - "Pagination test verifies all items collected across 2 pages"
  artifacts:
    - path: "internal/client/filesystems_test.go"
      provides: "Pagination test"
      contains: "TestUnit_FileSystem_List_Paginated"
    - path: "internal/provider/bucket_resource_test.go"
      provides: "409 conflict and 404 read-not-found tests"
      contains: "Conflict"
  key_links:
    - from: "internal/client/filesystems.go"
      to: "internal/client/models.go"
      via: "ListResponse ContinuationToken field"
      pattern: "ContinuationToken"
    - from: "internal/provider/*_resource_test.go"
      to: "internal/testmock/handlers"
      via: "WriteJSONError for 409/422 responses"
      pattern: "WriteJSONError"
---

<objective>
Implement auto-pagination in client list methods, add error-path tests (409 Conflict, 422 Unprocessable, 404 Read-not-found) across resource families, and verify retry coverage is complete (QUA-05 already done).

Purpose: QUA-04 requires mocked integration tests covering all CRUD lifecycle edge cases. Currently no 409/422 error tests exist in the provider layer, pagination is not auto-looping, and Read-404 state removal is only tested for filesystem and network_access_policy. QUA-05 (retry) is already complete — this plan confirms it.
Output: Auto-paginated client list methods, error-path tests for major resource families, pagination test at client layer.
</objective>

<execution_context>
@/home/gule/.claude/get-shit-done/workflows/execute-plan.md
@/home/gule/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-quality-hardening/05-RESEARCH.md

@internal/client/filesystems.go
@internal/client/models.go
@internal/client/errors.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement auto-pagination in client list methods and test</name>
  <files>
    internal/client/filesystems.go,
    internal/client/buckets.go,
    internal/client/object_store_accounts.go,
    internal/client/nfs_export_policies.go,
    internal/client/smb_share_policies.go,
    internal/client/snapshot_policies.go,
    internal/client/object_store_access_policies.go,
    internal/client/network_access_policies.go,
    internal/client/quotas.go,
    internal/client/object_store_access_keys.go,
    internal/client/filesystems_test.go
  </files>
  <behavior>
    - TestUnit_FileSystem_List_Paginated: Mock server returns page 1 with 2 items + continuation_token, page 2 with 1 item + no token. ListFileSystems returns all 3 items.
    - TestUnit_FileSystem_List_SinglePage: Mock server returns 2 items with no continuation_token. ListFileSystems returns 2 items (no extra request).
  </behavior>
  <action>
    Write the pagination test first in `internal/client/filesystems_test.go`.

    Then implement auto-pagination. The CONTEXT.md decision says "List operations with continuation_token return all results." Per RESEARCH.md recommendation, implement in client layer (DRY).

    **Implementation approach:**
    1. Read `internal/client/filesystems.go` `ListFileSystems` — currently it makes a single GET and returns the response.
    2. Modify `ListFileSystems` to loop: after receiving a response, check if `response.ContinuationToken` is non-empty. If so, set the opts ContinuationToken and make another request. Accumulate all items.
    3. Read `internal/client/models.go` to confirm `ListResponse[T]` has a `ContinuationToken` field.
    4. Apply the same pattern to ALL other client list methods:
       - `ListBuckets`, `ListObjectStoreAccounts`, `ListNfsExportPolicies`, `ListNfsExportPolicyRules`
       - `ListSmbSharePolicies`, `ListSmbSharePolicyRules`, `ListSnapshotPolicies`, `ListSnapshotPolicyRules`
       - `ListObjectStoreAccessPolicies`, `ListObjectStoreAccessPolicyRules`, `ListObjectStoreAccessPolicyMembers`
       - `ListNetworkAccessPolicies`, `ListNetworkAccessPolicyRules`
       - `ListQuotaGroups`, `ListQuotaUsers`
       - `ListObjectStoreAccessKeys`

    Read each client file first to understand the current signature. The pagination loop should be internal — callers see no change in return type.

    **Key detail:** The continuation_token is passed as a query parameter `continuation_token=...`. Check how the current code builds query params and add the token to subsequent requests.

    Only write the pagination test for `ListFileSystems` (representative test). Other list methods follow the identical pattern — one test proves the loop works.
  </action>
  <verify>
    <automated>go test ./internal/client/... -run "TestUnit_FileSystem_List_Paginated|TestUnit_FileSystem_List_SinglePage" -count=1 -v</automated>
  </verify>
  <done>ListFileSystems auto-paginates across multiple pages. All other client list methods have the same pagination loop. Pagination test passes with 3 items across 2 pages. Single-page test confirms no extra requests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add error-path tests (409/422/404-Read) for resource families</name>
  <files>
    internal/provider/bucket_resource_test.go,
    internal/provider/nfs_export_policy_resource_test.go,
    internal/provider/object_store_access_policy_resource_test.go,
    internal/provider/quota_group_resource_test.go,
    internal/provider/filesystem_resource_test.go,
    internal/provider/object_store_account_resource_test.go,
    internal/provider/network_access_policy_resource_test.go
  </files>
  <behavior>
    409 Conflict on Create:
    - TestUnit_Bucket_Create_Conflict: Mock returns 409. Create produces error diagnostic with "conflict" or "already exists".
    - TestUnit_NfsExportPolicy_Create_Conflict: Same pattern.
    - TestUnit_OAP_Create_Conflict: Same pattern.
    - TestUnit_QuotaGroup_Create_Conflict: Same pattern.

    422 Unprocessable on Create/Update:
    - TestUnit_FileSystem_Delete_Unprocessable: Mock returns 422 on DELETE (delete-before-destroy). Produces error diagnostic.

    404 Read removes state (gap filling — currently only filesystem and network_access_policy have this):
    - TestUnit_Bucket_Read_NotFound: Mock returns 404 on GET. Read sets state to null.
    - TestUnit_ObjectStoreAccount_Read_NotFound: Same pattern.
    - TestUnit_NfsExportPolicy_Read_NotFound: Same pattern.
    - TestUnit_OAP_Read_NotFound: Same pattern.
    - TestUnit_QuotaGroup_Read_NotFound: Same pattern.
  </behavior>
  <action>
    Write failing tests first, then verify they pass against existing production code (no production changes expected — resource Create/Read methods already use `resp.Diagnostics.AddError` on API errors; this task verifies that behavior).

    **For 409 Conflict tests:**
    Follow the pattern from RESEARCH.md Pattern 4. Register a handler on the mock server that returns 409 for POST. Create the resource via the test helper. Assert `resp.Diagnostics.HasError()`.

    Pick representative resources from families that emit 409 in mock handlers:
    - buckets (S3 name conflict)
    - nfs_export_policies (policy name conflict)
    - object_store_access_policies (policy name conflict)
    - quota groups (duplicate quota)

    **For 422 tests:**
    - filesystem DELETE when not destroyed first (mock returns 422)

    **For 404 Read-removes-state:**
    Follow RESEARCH.md Pattern 3. Register handler returning 404. Call Read with existing state. Assert `resp.State.Raw.IsNull()`.

    Cover resources NOT currently tested for 404 removal:
    - bucket, object_store_account, nfs_export_policy, object_store_access_policy, quota_group

    Read each resource's Read method first to confirm it calls `resp.State.RemoveResource(ctx)` on 404. If any resource does NOT handle 404 gracefully, add the handling before writing the test.

    **Important:** Use the existing `testmock` infrastructure. For custom error responses, use inline handler registration (not modifying shared handlers). Each test registers its own handler on a fresh mock server.
  </action>
  <verify>
    <automated>go test ./internal/provider/... -run "TestUnit.*(Conflict|Unprocessable|NotFound)" -count=1 -v</automated>
  </verify>
  <done>409 Conflict tests pass for bucket, nfs_export_policy, oap, quota_group. 422 test passes for filesystem delete. 404 Read-removes-state tests pass for bucket, object_store_account, nfs_export_policy, oap, quota_group. All error paths produce meaningful diagnostics (not panics). Full suite passes.</done>
</task>

</tasks>

<verification>
- `go test ./internal/... -count=1 -timeout 5m` — full suite passes (existing + new tests)
- `go test ./internal/client/... -run "TestUnit_FileSystem_List" -count=1` — pagination tests pass
- `go test ./internal/provider/... -run "TestUnit.*(Conflict|Unprocessable|NotFound)" -count=1` — all error-path tests pass
- `go test ./internal/client/... -run "TestUnit_RetryTransport" -count=1` — retry tests still pass (QUA-05 confirmation)
</verification>

<success_criteria>
- Client list methods auto-paginate (loop on continuation_token)
- Pagination test passes with multi-page mock response
- 409 Conflict on Create produces diagnostic for 4 resource families
- 422 Unprocessable on Delete produces diagnostic for filesystem
- 404 Read removes state for 5 additional resources
- Existing 147 tests still pass (zero regressions)
- QUA-05 retry tests confirmed green
</success_criteria>

<output>
After completion, create `.planning/phases/05-quality-hardening/05-02-SUMMARY.md`
</output>
