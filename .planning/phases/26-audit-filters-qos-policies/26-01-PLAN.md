---
phase: 26-audit-filters-qos-policies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/client/models_storage.go
  - internal/client/bucket_audit_filters.go
  - internal/client/qos_policies.go
  - internal/testmock/handlers/bucket_audit_filters.go
  - internal/testmock/handlers/qos_policies.go
autonomous: true
requirements:
  - BAF-01
  - BAF-02
  - BAF-03
  - QOS-01
  - QOS-02
  - QOS-03
  - QOS-04

must_haves:
  truths:
    - "Client can CRUD bucket audit filters by bucket name"
    - "Client can CRUD QoS policies by name"
    - "Client can add and remove QoS policy members (buckets/file systems)"
    - "Mock handlers serve all audit filter and QoS endpoints for unit tests"
  artifacts:
    - path: "internal/client/models_storage.go"
      provides: "BucketAuditFilter, QosPolicy, PolicyMember model structs"
      contains: "BucketAuditFilter"
    - path: "internal/client/bucket_audit_filters.go"
      provides: "GetBucketAuditFilter, PostBucketAuditFilter, PatchBucketAuditFilter, DeleteBucketAuditFilter"
      exports: ["GetBucketAuditFilter", "PostBucketAuditFilter", "PatchBucketAuditFilter", "DeleteBucketAuditFilter"]
    - path: "internal/client/qos_policies.go"
      provides: "CRUD for QoS policies + member assignment"
      exports: ["GetQosPolicy", "PostQosPolicy", "PatchQosPolicy", "DeleteQosPolicy", "ListQosPolicyMembers", "PostQosPolicyMember", "DeleteQosPolicyMember"]
    - path: "internal/testmock/handlers/bucket_audit_filters.go"
      provides: "Mock CRUD for /buckets/audit-filters"
      exports: ["RegisterBucketAuditFilterHandlers"]
    - path: "internal/testmock/handlers/qos_policies.go"
      provides: "Mock CRUD for /qos-policies and /qos-policies/members"
      exports: ["RegisterQosPolicyHandlers"]
  key_links:
    - from: "internal/client/bucket_audit_filters.go"
      to: "internal/client/models_storage.go"
      via: "BucketAuditFilter types"
      pattern: "client\\.BucketAuditFilter"
    - from: "internal/client/qos_policies.go"
      to: "internal/client/models_storage.go"
      via: "QosPolicy types"
      pattern: "client\\.QosPolicy"
    - from: "internal/testmock/handlers/bucket_audit_filters.go"
      to: "internal/client/models_storage.go"
      via: "client model types for JSON serialization"
      pattern: "client\\.BucketAuditFilter"
---

<objective>
Create client models, CRUD methods, and mock handlers for both bucket audit filters and QoS policies.

Purpose: Foundation layer that resource and data source implementations (plans 26-02 and 26-03) depend on.
Output: Client CRUD functions and mock HTTP handlers ready for unit testing.
</objective>

<execution_context>
@/home/gule/.claude/get-shit-done/workflows/execute-plan.md
@/home/gule/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@internal/client/models_storage.go
@internal/client/bucket_access_policies.go
@internal/client/lifecycle_rules.go
@internal/testmock/handlers/bucket_access_policies.go
@internal/testmock/handlers/lifecycle_rules.go

<interfaces>
<!-- Existing patterns the executor must follow -->

From internal/client/models_storage.go (BucketAccessPolicy pattern):
```go
type BucketAccessPolicy struct {
	ID         string                   `json:"id,omitempty"`
	Name       string                   `json:"name,omitempty"`
	Bucket     NamedReference           `json:"bucket"`
	Enabled    bool                     `json:"enabled"`
	IsLocal    bool                     `json:"is_local,omitempty"`
	PolicyType string                   `json:"policy_type,omitempty"`
	Rules      []BucketAccessPolicyRule `json:"rules,omitempty"`
}
```

From internal/client/models_common.go:
```go
type NamedReference struct {
	Name string `json:"name,omitempty"`
	ID   string `json:"id,omitempty"`
}
type ListResponse[T any] struct {
	Items             []T    `json:"items"`
	ContinuationToken string `json:"continuation_token,omitempty"`
	TotalItemCount    int    `json:"total_item_count,omitempty"`
}
```

From FLASHBLADE_API.md — Bucket Audit Filter endpoints:
- GET /api/2.22/buckets/audit-filters — Params: bucket_ids, bucket_names
- POST /api/2.22/buckets/audit-filters — Params: bucket_ids, bucket_names — Body: actions(array), s3_prefixes(array)
- PATCH /api/2.22/buckets/audit-filters — Params: bucket_ids, bucket_names — Body: actions(array), s3_prefixes(array)
- DELETE /api/2.22/buckets/audit-filters — Params: bucket_ids, bucket_names

BucketAuditFilter model: actions(array), bucket(object/NamedReference), name(ro string), s3_prefixes(array)

From FLASHBLADE_API.md — QoS Policy endpoints:
- GET /api/2.22/qos-policies
- POST /api/2.22/qos-policies — Body: enabled(boolean), max_total_bytes_per_sec(integer), max_total_ops_per_sec(integer), name(string)
- PATCH /api/2.22/qos-policies — Body: enabled(boolean), max_total_bytes_per_sec(integer), max_total_ops_per_sec(integer), name(string)
- DELETE /api/2.22/qos-policies
- GET /api/2.22/qos-policies/members — Params: member_types(array)
- POST /api/2.22/qos-policies/members — Params: member_types(array)
- DELETE /api/2.22/qos-policies/members — Params: member_types(array)

QosPolicy model: enabled(boolean), id(ro string), is_local(ro boolean), location(object), max_total_bytes_per_sec(integer), max_total_ops_per_sec(integer), name(string), policy_type(ro string), realms(ro array)
PolicyMemberContext model: member(object/NamedReference), policy(object/NamedReference)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Client models and CRUD methods for audit filters and QoS policies</name>
  <files>internal/client/models_storage.go, internal/client/bucket_audit_filters.go, internal/client/qos_policies.go</files>
  <action>
1. Add model structs to `internal/client/models_storage.go` after the BucketAccessPolicyRulePost type:

   **BucketAuditFilter** (GET response):
   - Actions []string, Bucket NamedReference, Name string, S3Prefixes []string
   - JSON tags: actions, bucket, name, s3_prefixes

   **BucketAuditFilterPost** (POST body):
   - Actions []string, S3Prefixes []string
   - JSON tags: actions, s3_prefixes

   **BucketAuditFilterPatch** (PATCH body — pointer fields for partial update):
   - Actions *[]string, S3Prefixes *[]string
   - JSON tags: actions,omitempty; s3_prefixes,omitempty

   **QosPolicy** (GET response):
   - ID string, Name string, Enabled bool, IsLocal bool, MaxTotalBytesPerSec int64, MaxTotalOpsPerSec int64, PolicyType string
   - JSON tags match API snake_case: id, name, enabled, is_local, max_total_bytes_per_sec, max_total_ops_per_sec, policy_type

   **QosPolicyPost** (POST body):
   - Name string, Enabled *bool (pointer for omitempty distinction), MaxTotalBytesPerSec int64, MaxTotalOpsPerSec int64
   - JSON tags: name, enabled,omitempty, max_total_bytes_per_sec,omitempty, max_total_ops_per_sec,omitempty

   **QosPolicyPatch** (PATCH body — pointer fields):
   - Enabled *bool, MaxTotalBytesPerSec *int64, MaxTotalOpsPerSec *int64, Name *string
   - JSON tags with omitempty

   **PolicyMember** (GET/POST response for /qos-policies/members):
   - Member NamedReference, Policy NamedReference
   - JSON tags: member, policy

   **PolicyMemberPost** (POST body for /qos-policies/members):
   - Member NamedReference
   - JSON tags: member

2. Create `internal/client/bucket_audit_filters.go` following the bucket_access_policies.go pattern:
   - `GetBucketAuditFilter(ctx, bucketName) (*BucketAuditFilter, error)` — GET with bucket_names param, return first item or 404
   - `PostBucketAuditFilter(ctx, bucketName, body BucketAuditFilterPost) (*BucketAuditFilter, error)` — POST with bucket_names param
   - `PatchBucketAuditFilter(ctx, bucketName, body BucketAuditFilterPatch) (*BucketAuditFilter, error)` — PATCH with bucket_names param
   - `DeleteBucketAuditFilter(ctx, bucketName) error` — DELETE with bucket_names param
   - API path: `/buckets/audit-filters`

3. Create `internal/client/qos_policies.go` following the lifecycle_rules.go pattern:
   - `GetQosPolicy(ctx, name) (*QosPolicy, error)` — uses getOneByName generic helper if available, otherwise GET with names param + first item
   - `PostQosPolicy(ctx, body QosPolicyPost) (*QosPolicy, error)` — POST to /qos-policies
   - `PatchQosPolicy(ctx, name string, body QosPolicyPatch) (*QosPolicy, error)` — PATCH with names param
   - `DeleteQosPolicy(ctx, name string) error` — DELETE with names param
   - `ListQosPolicyMembers(ctx, policyName string) ([]PolicyMember, error)` — GET /qos-policies/members with policy_names param, paginate with continuation_token
   - `PostQosPolicyMember(ctx, policyName string, body PolicyMemberPost) (*PolicyMember, error)` — POST /qos-policies/members with policy_names param
   - `DeleteQosPolicyMember(ctx, policyName string, memberName string) error` — DELETE /qos-policies/members with policy_names + member_names params

  Check if getOneByName[T] generic helper exists in the client package (from Phase 20 dedup). If it does, use it for GetQosPolicy. Audit filters use bucket_names param, so follow the GetBucketAccessPolicy pattern instead.
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && go build ./...</automated>
  </verify>
  <done>All new model types compile. Client CRUD functions for audit filters (4 methods) and QoS policies (7 methods including members) exist and follow established patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Mock handlers for audit filters and QoS policies</name>
  <files>internal/testmock/handlers/bucket_audit_filters.go, internal/testmock/handlers/qos_policies.go</files>
  <action>
1. Create `internal/testmock/handlers/bucket_audit_filters.go` following the bucket_access_policies.go handler pattern:
   - Thread-safe in-memory store: `bucketAuditFilterStore` with `filters map[string]*client.BucketAuditFilter` keyed by bucket name, `mu sync.Mutex`, `nextID int`
   - `RegisterBucketAuditFilterHandlers(mux *http.ServeMux) *bucketAuditFilterStore` — registers on `/api/2.22/buckets/audit-filters`
   - `Seed(filter *client.BucketAuditFilter)` method for test setup
   - Dispatch by method: GET, POST, PATCH, DELETE
   - GET: validate query params (bucket_ids, bucket_names), filter by bucket_names, return matching items
   - POST: validate bucket_names param required, decode BucketAuditFilterPost body, create filter with auto-generated name, set bucket reference, return 200 with item
   - PATCH: validate bucket_names param required, find existing filter, apply partial update (only non-nil fields from BucketAuditFilterPatch), return 200
   - DELETE: validate bucket_names param required, find and remove filter, return 200
   - Use `ValidateQueryParams` from query_params.go for param validation
   - Return 404 JSON error `{"errors":[{"message":"..."}]}` when filter not found

2. Create `internal/testmock/handlers/qos_policies.go` following similar patterns:
   - Thread-safe in-memory store: `qosPolicyStore` with `policies map[string]*client.QosPolicy` keyed by name, `members map[string][]client.PolicyMember` keyed by policy name, `mu sync.Mutex`, `nextID int`
   - `RegisterQosPolicyHandlers(mux *http.ServeMux) *qosPolicyStore` — registers on:
     - `/api/2.22/qos-policies` for policy CRUD
     - `/api/2.22/qos-policies/members` for member management
   - `Seed(policy *client.QosPolicy)` and `SeedMember(policyName string, member client.PolicyMember)` methods
   - Policy CRUD (GET/POST/PATCH/DELETE):
     - GET: support names param filtering, return all if no filter
     - POST: decode QosPolicyPost, auto-generate ID, set defaults (enabled=true if nil, policy_type="bandwidth-limit"), return 200
     - PATCH: names param required, find policy, apply pointer fields, return 200
     - DELETE: names param required, find and remove policy + its members, return 200
   - Member management (GET/POST/DELETE on /members):
     - GET: filter by policy_names param, support member_types param (optional filter)
     - POST: policy_names param required, decode PolicyMemberPost, add member to store, return 200
     - DELETE: policy_names + member_names params required, remove member, return 200
   - Return 404 JSON error when policy or member not found
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && go build ./... && go vet ./...</automated>
  </verify>
  <done>Mock handlers compile and register on correct API paths. Both stores have Seed methods for test setup. All CRUD operations + QoS member management handled.</done>
</task>

</tasks>

<verification>
```bash
cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade
go build ./...
go vet ./...
# Verify new files exist
test -f internal/client/bucket_audit_filters.go
test -f internal/client/qos_policies.go
test -f internal/testmock/handlers/bucket_audit_filters.go
test -f internal/testmock/handlers/qos_policies.go
```
</verification>

<success_criteria>
- All 4 new files compile without errors
- Client CRUD for audit filters: Get, Post, Patch, Delete (4 methods)
- Client CRUD for QoS policies: Get, Post, Patch, Delete + ListMembers, PostMember, DeleteMember (7 methods)
- Mock handlers register on correct /api/2.22/ paths
- Model structs match API spec field names and types
</success_criteria>

<output>
After completion, create `.planning/phases/26-audit-filters-qos-policies/26-01-SUMMARY.md`
</output>
