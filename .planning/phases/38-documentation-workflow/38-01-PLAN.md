---
phase: 38-documentation-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/resources/flashblade_target/import.sh
  - examples/workflows/s3-target-replication/main.tf
  - docs/resources/target.md
  - docs/data-sources/target.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03

must_haves:
  truths:
    - "examples/resources/flashblade_target/import.sh exists and contains a valid terraform import command using the target name as identifier"
    - "examples/workflows/s3-target-replication/main.tf demonstrates the full stack: flashblade_target, flashblade_object_store_remote_credentials (with target_name), flashblade_bucket_replica_link"
    - "make docs runs without errors and regenerates docs/ for all resources including flashblade_target"
    - "docs/resources/target.md is generated (not hand-edited) and includes the Import section"
  artifacts:
    - path: "examples/resources/flashblade_target/import.sh"
      provides: "Import documentation for flashblade_target"
      contains: "terraform import flashblade_target"
    - path: "examples/workflows/s3-target-replication/main.tf"
      provides: "Full S3 target replication workflow example"
      min_lines: 80
    - path: "docs/resources/target.md"
      provides: "Auto-generated Terraform documentation for flashblade_target"
  key_links:
    - from: "examples/resources/flashblade_target/import.sh"
      to: "docs/resources/target.md"
      via: "tfplugindocs embeds import.sh content into Import section"
      pattern: "terraform import flashblade_target\\.\\S+ \\S+"
    - from: "examples/workflows/s3-target-replication/main.tf"
      to: "flashblade_target + flashblade_object_store_remote_credentials + flashblade_bucket_replica_link"
      via: "HCL resource references (target_name attribute on remote credentials)"
      pattern: "target_name\\s*="
---

<objective>
Add import documentation for flashblade_target, create the s3-target-replication workflow example demonstrating the full external S3 replication stack, and regenerate all provider documentation with tfplugindocs.

Purpose: Complete the v2.2 documentation requirements so operators can discover, use, and import the new target resource and understand the full replication workflow to an external S3 endpoint.
Output: import.sh for flashblade_target, examples/workflows/s3-target-replication/main.tf, regenerated docs/
</objective>

<execution_context>
@/home/gule/.claude/get-shit-done/workflows/execute-plan.md
@/home/gule/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- flashblade_target resource (target_resource.go) -->
<!-- Import: terraform import flashblade_target.NAME TARGET_NAME -->
<!-- ImportState uses req.ID as the target name directly (not UUID) -->

<!-- flashblade_object_store_remote_credentials schema (Phase 37 enhancement) -->
<!-- New optional attribute: target_name (mutually exclusive with remote_name) -->
<!-- Name format when using target: "target-name/creds-name" -->

<!-- flashblade_bucket_replica_link schema (unchanged) -->
<!-- remote_credentials_name: references the credentials by their short name (not prefixed) -->

<!-- Existing workflow pattern to follow: examples/workflows/s3-bucket-replication/main.tf -->
<!-- - terraform {} block with required_providers -->
<!-- - variable blocks for sensitive inputs -->
<!-- - single provider block (target workflow is single-array, not cross-array) -->
<!-- - numbered step comments (Step 1, Step 2, ...) -->
<!-- - outputs at the end -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add import.sh for flashblade_target</name>
  <files>examples/resources/flashblade_target/import.sh</files>
  <action>
Create `examples/resources/flashblade_target/import.sh` with the following content (match the pattern used by other resources, e.g. flashblade_object_store_remote_credentials/import.sh):

```
terraform import flashblade_target.example s3-replication-target
```

The identifier is the target name (string), not a UUID. This mirrors the ImportState implementation in target_resource.go which uses req.ID directly as the target name to call GetTarget.

Do NOT add comments or extra lines — other import.sh files in this repo are a single terraform import command.
  </action>
  <verify>
    <automated>test -f /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade/examples/resources/flashblade_target/import.sh && grep -q "terraform import flashblade_target" /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade/examples/resources/flashblade_target/import.sh</automated>
  </verify>
  <done>import.sh exists and contains a valid terraform import command for flashblade_target using the target name as identifier.</done>
</task>

<task type="auto">
  <name>Task 2: Create s3-target-replication workflow example</name>
  <files>examples/workflows/s3-target-replication/main.tf</files>
  <action>
Create `examples/workflows/s3-target-replication/main.tf` demonstrating the full external S3 replication stack on a single FlashBlade. Follow the structural conventions of `examples/workflows/s3-bucket-replication/main.tf`:

Structure:
1. File header comment block with architecture diagram and prerequisites
2. `terraform {}` block with `required_providers { flashblade = { source = "numberly/flashblade", version = "~> 2.2" } }`
3. Variable declarations: `endpoint`, `api_token` (sensitive), `tenant_name` (default = "s3-target-tenant"), `s3_target_address` (description: "External S3 endpoint address, e.g. s3.us-east-1.amazonaws.com"), `s3_access_key_id` (sensitive), `s3_secret_access_key` (sensitive)
4. `provider "flashblade"` block using the variables
5. Step-by-step resources with numbered comment blocks:

**Step 1: Object store account**
```
resource "flashblade_object_store_account" "tenant" {
  name                = var.tenant_name
  skip_default_export = true
}
```

**Step 2: Bucket with versioning**
```
resource "flashblade_bucket" "replica" {
  name       = "${var.tenant_name}-data"
  account    = flashblade_object_store_account.tenant.name
  versioning = "enabled"
  destroy_eradicate_on_delete = false
}
```

**Step 3: S3 replication target**
```
resource "flashblade_target" "s3_endpoint" {
  name    = "external-s3"
  address = var.s3_target_address
}
```
Include a comment explaining that `ca_certificate_group` can be set for private S3-compatible endpoints with custom TLS certificates.

**Step 4: Remote credentials referencing the target**
```
resource "flashblade_object_store_remote_credentials" "s3_creds" {
  name              = "external-s3/s3-replication-creds"
  access_key_id     = var.s3_access_key_id
  secret_access_key = var.s3_secret_access_key
  target_name       = flashblade_target.s3_endpoint.name
}
```
Include a comment: "Name must be formatted as <target-name>/<credentials-name>. Use target_name (not remote_name) when the remote is an external S3 endpoint rather than another FlashBlade array."

**Step 5: Bucket replica link to external S3**
```
resource "flashblade_bucket_replica_link" "to_s3" {
  local_bucket_name       = flashblade_bucket.replica.name
  remote_bucket_name      = "${var.tenant_name}-data"
  remote_credentials_name = flashblade_object_store_remote_credentials.s3_creds.name
  paused                  = false
}
```
Include a comment explaining that `remote_bucket_name` is the bucket name on the external S3 endpoint.

**Outputs:**
- `target_name`: flashblade_target.s3_endpoint.name
- `target_address`: flashblade_target.s3_endpoint.address
- `target_status`: flashblade_target.s3_endpoint.status
- `replication_status`: flashblade_bucket_replica_link.to_s3.status

The file must be valid HCL (all blocks properly closed, all references valid). The workflow is single-provider (one FlashBlade, one external S3) — do NOT use provider aliases.
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && terraform fmt -check examples/workflows/s3-target-replication/main.tf 2>&1; test -f examples/workflows/s3-target-replication/main.tf && grep -q "flashblade_target" examples/workflows/s3-target-replication/main.tf && grep -q "target_name" examples/workflows/s3-target-replication/main.tf && grep -q "flashblade_bucket_replica_link" examples/workflows/s3-target-replication/main.tf</automated>
  </verify>
  <done>examples/workflows/s3-target-replication/main.tf exists, is valid HCL, references flashblade_target, uses target_name on remote credentials, and includes a bucket replica link to the external S3 endpoint.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate provider documentation with tfplugindocs</name>
  <files>docs/resources/target.md
docs/data-sources/target.md</files>
  <action>
Run `make docs` from the repository root. This executes `go generate ./...` which invokes tfplugindocs to regenerate all files in `docs/`.

Expected outcomes:
- `docs/resources/target.md` is created/updated — it must contain an "Import" section populated from `examples/resources/flashblade_target/import.sh`
- `docs/data-sources/target.md` is created/updated
- No other docs/ files are deleted or corrupted
- The command exits with code 0

If `make docs` fails, diagnose and fix the root cause (missing example file, syntax error in resource schema MarkdownDescription, etc.) before proceeding. Do NOT edit any file under `docs/` manually.
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && make docs 2>&1 | tail -5; test -f docs/resources/target.md && grep -q "Import" docs/resources/target.md && test -f docs/data-sources/target.md</automated>
  </verify>
  <done>make docs exits 0, docs/resources/target.md and docs/data-sources/target.md exist and contain an Import section, no docs/ files were manually edited.</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. `examples/resources/flashblade_target/import.sh` contains a single `terraform import flashblade_target.example s3-replication-target` command
2. `examples/workflows/s3-target-replication/main.tf` is valid HCL with `flashblade_target`, `flashblade_object_store_remote_credentials` (using `target_name`), and `flashblade_bucket_replica_link`
3. `make docs` exits 0 — no manual edits to `docs/`
4. `docs/resources/target.md` includes an Import section
5. `make test` still passes (no regressions from docs generation)

Run: `cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && make build && make docs && grep -q "Import" docs/resources/target.md && echo "PASS"`
</verification>

<success_criteria>
- DOC-01: import.sh for flashblade_target exists with correct `terraform import flashblade_target.NAME NAME` syntax
- DOC-02: examples/workflows/s3-target-replication/main.tf demonstrates target → remote credentials (target_name) → bucket replica link chain
- DOC-03: `make docs` completes without errors, docs/resources/target.md and docs/data-sources/target.md are generated without manual edits
</success_criteria>

<output>
After completion, create `.planning/phases/38-documentation-workflow/38-01-SUMMARY.md` using the template at `/home/gule/.claude/get-shit-done/templates/summary.md`.
</output>
