---
phase: 17-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tmp/test-purestorage/replication-acceptance/main.tf
  - tmp/test-purestorage/replication-acceptance/variables.tf
  - tmp/test-purestorage/replication-acceptance/outputs.tf
  - tmp/test-purestorage/replication-acceptance/README.md
autonomous: false
requirements:
  - WFL-03

must_haves:
  truths:
    - "Acceptance test HCL exercises the full replication lifecycle: credentials, replica link, pause/resume, destroy"
    - "Test can be skipped without blocking milestone completion if no FlashBlade pair is available"
    - "Human reviews the HCL before any live execution"
  artifacts:
    - path: "tmp/test-purestorage/replication-acceptance/main.tf"
      provides: "Full replication lifecycle acceptance test HCL"
      min_lines: 80
    - path: "tmp/test-purestorage/replication-acceptance/variables.tf"
      provides: "Input variables for array endpoints, tokens, and resource names"
    - path: "tmp/test-purestorage/replication-acceptance/outputs.tf"
      provides: "Outputs for verification of replication status"
  key_links:
    - from: "tmp/test-purestorage/replication-acceptance/main.tf"
      to: "flashblade_object_store_remote_credentials"
      via: "resource block"
      pattern: "flashblade_object_store_remote_credentials"
    - from: "tmp/test-purestorage/replication-acceptance/main.tf"
      to: "flashblade_bucket_replica_link"
      via: "resource block"
      pattern: "flashblade_bucket_replica_link"
---

<objective>
Write acceptance test HCL that exercises the full replication lifecycle on a live FlashBlade pair, then pause for human approval before any live execution. This plan produces the HCL configuration only -- it does NOT apply against live arrays without explicit user consent.

Purpose: WFL-03 requires a live acceptance test. The HCL must be ready to run but gated behind a human checkpoint. If no FlashBlade pair is available, the checkpoint can be denied without blocking the milestone.
Output: Complete acceptance test HCL in tmp/test-purestorage/replication-acceptance/
</objective>

<execution_context>
@/home/gule/.claude/get-shit-done/workflows/execute-plan.md
@/home/gule/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@examples/workflows/s3-bucket-replication/main.tf
@internal/provider/remote_credentials_resource.go
@internal/provider/bucket_replica_link_resource.go
@internal/provider/array_connection_data_source.go
@internal/provider/object_store_access_key_resource.go

<interfaces>
<!-- Resources used in acceptance test -->

flashblade_object_store_remote_credentials:
  - Required: name, access_key_id, secret_access_key, remote_name
  - Computed: id
  - Import: by name

flashblade_bucket_replica_link:
  - Required: local_bucket_name, remote_bucket_name
  - Optional: remote_credentials_name, paused, cascading_enabled
  - Computed: id, remote_name, direction, status, status_details, lag, recovery_point, object_backlog_count, object_backlog_total_size
  - Import: by "localBucket/remoteBucket"

flashblade_array_connection (data source):
  - Required: remote_name
  - Computed: id, status, remote_id, management_address, replication_addresses, encrypted, type, version

flashblade_object_store_access_key:
  - Required: object_store_account
  - Optional: secret_access_key (for cross-array sharing)
  - Computed: name, access_key_id, secret_access_key (when API-generated), created, enabled
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write replication acceptance test HCL</name>
  <files>
    tmp/test-purestorage/replication-acceptance/main.tf
    tmp/test-purestorage/replication-acceptance/variables.tf
    tmp/test-purestorage/replication-acceptance/outputs.tf
    tmp/test-purestorage/replication-acceptance/README.md
  </files>
  <action>
Create the acceptance test directory and HCL files:

1. `variables.tf`: Define input variables for:
   - `primary_endpoint`, `primary_api_token` (FlashBlade A)
   - `secondary_endpoint`, `secondary_api_token` (FlashBlade B)
   - `primary_array_name`, `secondary_array_name` (array connection remote names)
   - `account_name` (shared account name, default "repl-test")
   - `bucket_name` (shared bucket name, default "repl-test-bucket")
   All sensitive vars marked `sensitive = true`.

2. `main.tf`: Dual-provider configuration (primary + secondary) exercising the full lifecycle. Follow the pattern from `examples/workflows/s3-bucket-replication/main.tf` but simplified for testing:
   - Two provider blocks (primary, secondary) using the input variables
   - On primary: account, bucket (versioning=enabled), access key (API-generated secret)
   - On secondary: account, bucket (versioning=enabled), access key with explicit secret from primary
   - Data source: `flashblade_array_connection` on each side to verify connectivity
   - Remote credentials on primary pointing to secondary's key, and vice versa
   - Bucket replica link on primary (outbound to secondary bucket)
   - Second bucket replica link on secondary (outbound to primary bucket) -- bidirectional
   - Comment block explaining the test lifecycle steps

3. `outputs.tf`: Outputs for verification:
   - Primary replica link status, direction, paused
   - Secondary replica link status, direction, paused
   - Array connection status from both sides

4. `README.md`: Brief instructions:
   - Prerequisites (two FlashBlade arrays with established array connection)
   - How to run: `terraform init && terraform apply`
   - How to test pause/resume: edit paused=true, re-apply
   - How to clean up: `terraform destroy`
   - Note: This is a destructive test -- creates real resources on live arrays

Use `terraform fmt` compatible HCL. Include `terraform { required_providers }` block with the local provider source.
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade/tmp/test-purestorage/replication-acceptance && cat main.tf | head -5 && echo "---" && wc -l main.tf variables.tf outputs.tf</automated>
  </verify>
  <done>Acceptance test HCL files exist with dual-provider config, full resource chain, and output verification points.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review and optional live execution of acceptance test</name>
  <files>tmp/test-purestorage/replication-acceptance/main.tf</files>
  <action>
Present the completed acceptance test HCL for human review. The human decides whether to:
(a) Approve the HCL as correct and optionally run it against a live FlashBlade pair
(b) Skip live testing if no FlashBlade pair is available -- this does not block milestone completion
(c) Request changes if issues are found
  </action>
  <verify>Human confirms review by responding with "approved", "skip", or issue description.</verify>
  <done>Human has reviewed the acceptance test HCL. Either approved, skipped, or issues addressed.</done>
  <what-built>
Complete replication acceptance test HCL in tmp/test-purestorage/replication-acceptance/ that exercises:
- Dual-provider setup (primary + secondary FlashBlade)
- Object store accounts, buckets with versioning, shared access keys
- Remote credentials on both arrays
- Bidirectional bucket replica links
- Outputs for status verification
  </what-built>
  <how-to-verify>
1. Review the HCL in `tmp/test-purestorage/replication-acceptance/`
2. Verify it matches your FlashBlade topology (array names, connectivity)
3. If you have two FlashBlade arrays available:
   - Create a `terraform.tfvars` with your array endpoints and tokens
   - Run `terraform init && terraform plan` to verify syntax
   - Run `terraform apply` to execute the full lifecycle
   - Verify outputs show replication status
   - Run `terraform destroy` to clean up
4. If no FlashBlade pair is available, type "skip" -- this does not block milestone completion
  </how-to-verify>
  <resume-signal>Type "approved" if HCL looks correct, "skip" to defer live testing, or describe issues</resume-signal>
</task>

</tasks>

<verification>
```bash
# Files exist with reasonable content
ls -la tmp/test-purestorage/replication-acceptance/
wc -l tmp/test-purestorage/replication-acceptance/*.tf

# HCL is syntactically valid (does not require provider binary)
# Note: terraform fmt check is best-effort since provider may not be installed
```
</verification>

<success_criteria>
- Acceptance test HCL covers: dual provider, accounts, buckets, access keys, remote credentials, replica links (bidirectional)
- Variables parameterize all environment-specific values (endpoints, tokens, array names)
- Outputs expose replication status fields for verification
- README explains prerequisites and execution steps
- Human checkpoint allows skip if no FlashBlade pair available
</success_criteria>

<output>
After completion, create `.planning/phases/17-testing/17-02-SUMMARY.md`
</output>
