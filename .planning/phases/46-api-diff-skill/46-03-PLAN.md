---
phase: 46-api-diff-skill
plan: 03
type: execute
wave: 2
depends_on:
  - 46-01
files_modified:
  - .claude/skills/api-diff/SKILL.md
autonomous: true
requirements:
  - INTG-02

must_haves:
  truths:
    - "SKILL.md exists with valid YAML frontmatter (name + description fields)"
    - "SKILL.md documents both scripts with CLI signatures and examples"
    - "SKILL.md is consistent in format with swagger-to-reference/SKILL.md"
  artifacts:
    - path: ".claude/skills/api-diff/SKILL.md"
      provides: "Skill index for api-diff"
      contains: "name: api-diff"
  key_links:
    - from: ".claude/skills/api-diff/SKILL.md"
      to: "diff_swagger.py + generate_migration_plan.py"
      via: "Workflow steps referencing script paths"
      pattern: "diff_swagger\\.py|generate_migration_plan\\.py"
---

<objective>
Create SKILL.md for the api-diff skill — the human+Claude-readable index that describes when and how to use the skill.

Purpose: Completes INTG-02 requirement (skill SKILL.md with valid YAML frontmatter). Enables Claude to discover and invoke the skill correctly in future sessions.
Output: .claude/skills/api-diff/SKILL.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/46-api-diff-skill/46-CONTEXT.md
@.planning/phases/46-api-diff-skill/46-01-SUMMARY.md

<interfaces>
<!-- Reference format from swagger-to-reference/SKILL.md -->
SKILL.md sections (in order):
  1. YAML frontmatter block at top: fields "name" and "description"
  2. # skill-name  (h1 title)
  3. ## Purpose
  4. ## When to Use
  5. ## Prerequisites
  6. ## Workflow  (with ### Step N subsections)
  7. ## Output
  8. ## Troubleshooting

YAML frontmatter fields: name (string), description (quoted string)
NOTE: Do not use triple-dash separators inside code blocks within SKILL.md to avoid YAML parsing issues.
Use "name:" and "description:" as the only frontmatter lines, on separate lines, wrapped by triple-dash delimiters.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create api-diff SKILL.md</name>
  <files>.claude/skills/api-diff/SKILL.md</files>
  <action>
Create `.claude/skills/api-diff/SKILL.md` following the structure of `.claude/skills/swagger-to-reference/SKILL.md`.

The file MUST start with a YAML frontmatter block using triple-dash delimiters, containing exactly:
  name: api-diff
  description: "Compare two FlashBlade swagger.json files to produce a structured diff of endpoint and schema changes, annotate items as real_change/swagger_artifact/needs_verification, and generate a migration plan cross-referenced with ROADMAP.md."

Then write the following sections in order:

**# api-diff** (h1 title)

**## Purpose**
Detects breaking changes, new endpoints, and schema field additions between two FlashBlade API versions.
Produces a structured JSON diff consumed by the api-upgrade skill.
Annotates each diff item with confidence: real_change (confirmed), swagger_artifact (doc error), needs_verification (unknown).

**## When to Use**
Bullet list:
- New swagger file available for a new FlashBlade API version
- Determining which provider changes are needed before an upgrade
- Annotating diff items with known discrepancy status
- Generating a migration plan to update provider models and add new resources

**## Prerequisites**
Bullet list:
- swagger-OLD.json and swagger-NEW.json in project root (or at known paths)
- .claude/skills/_shared/swagger_utils.py present (from Phase 43 shared library)
- Python 3.10+ (stdlib only — no pip installs required)

**## Workflow**

### Step 1 — Run the diff

Show bash command:
  PYTHONPATH=.claude/skills python3 .claude/skills/api-diff/scripts/diff_swagger.py \
    swagger-2.22.json swagger-2.23.json \
    --format json \
    --output /tmp/diff-2.22-2.23.json

Include note: "Path prefix normalization is automatic — /api/2.22/buckets and /api/2.23/buckets are treated as the same endpoint."

### Step 2 — Review and annotate discrepancies (optional)

Explain: Edit .claude/skills/api-diff/references/known_discrepancies.json to add annotation overrides.
Show bash command for re-running with --discrepancies flag:
  PYTHONPATH=.claude/skills python3 .claude/skills/api-diff/scripts/diff_swagger.py \
    swagger-2.22.json swagger-2.23.json \
    --format json \
    --discrepancies .claude/skills/api-diff/references/known_discrepancies.json \
    --output /tmp/diff-annotated.json

### Step 3 — Generate migration plan

Show bash command:
  python3 .claude/skills/api-diff/scripts/generate_migration_plan.py \
    /tmp/diff-annotated.json \
    ROADMAP.md \
    --format markdown \
    --output /tmp/migration-plan.md

Note: "roadmap_gaps entries are new API endpoints that match existing ROADMAP.md Candidate entries — these should be prioritized for scheduling."

**## Output**
Describe each output:
- diff.json: structured diff with 6 categories (new/removed/modified endpoints and schemas). Each item has annotation field.
- migration-plan.json or migration-plan.md: 4-category actionable plan — update_models, new_resources, deprecated, roadmap_gaps.

**## Troubleshooting**

Markdown table with 3 columns (Problem | Cause | Fix):
Row 1: "All paths appear as new+removed" | "Version prefix not stripped" | "Use current diff_swagger.py — normalize_path() is called automatically"
Row 2: "ROADMAP.md parse error" | "File not found in working directory" | "Run from project root or pass absolute path"
Row 3: "ImportError: No module named swagger_utils" | "Missing PYTHONPATH" | "Prefix command with PYTHONPATH=.claude/skills"
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && python3 -c "
import pathlib
content = pathlib.Path('.claude/skills/api-diff/SKILL.md').read_text()
assert content.startswith('---'), 'Missing YAML frontmatter open'
lines = content.split('\n')
close_idx = lines.index('---', 1)
fm = '\n'.join(lines[1:close_idx])
assert 'name: api-diff' in fm, 'Missing name field in frontmatter'
assert 'description:' in fm, 'Missing description in frontmatter'
assert 'diff_swagger.py' in content, 'Missing diff_swagger.py reference'
assert 'generate_migration_plan.py' in content, 'Missing generate_migration_plan.py reference'
assert '## Workflow' in content, 'Missing Workflow section'
assert '## Troubleshooting' in content, 'Missing Troubleshooting section'
print('PASS: SKILL.md structure valid')
"</automated>
  </verify>
  <done>
    - .claude/skills/api-diff/SKILL.md exists
    - YAML frontmatter (first block) has name: api-diff and description fields only
    - File references both diff_swagger.py and generate_migration_plan.py with correct relative paths
    - All 7 sections present: Purpose, When to Use, Prerequisites, Workflow (3 steps), Output, Troubleshooting
    - Troubleshooting table covers path normalization and PYTHONPATH issues
  </done>
</task>

</tasks>

<verification>
```bash
cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade

# Validate SKILL.md structure
python3 -c "
import pathlib
content = pathlib.Path('.claude/skills/api-diff/SKILL.md').read_text()
lines = content.split('\n')
close_idx = lines.index('---', 1)
fm = '\n'.join(lines[1:close_idx])
print('Frontmatter fields:', [l.split(':')[0].strip() for l in fm.splitlines() if ':' in l])
print('Sections:', [l for l in lines if l.startswith('## ')])
"
```
</verification>

<success_criteria>
- SKILL.md exists with YAML frontmatter containing name and description
- 3-step workflow documented with copy-pasteable bash commands
- Troubleshooting covers path normalization (the primary gotcha for this skill)
- Format consistent with swagger-to-reference/SKILL.md
</success_criteria>

<output>
After completion, create `.planning/phases/46-api-diff-skill/46-03-SUMMARY.md`
</output>
