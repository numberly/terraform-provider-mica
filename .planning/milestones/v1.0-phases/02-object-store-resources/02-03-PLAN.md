---
phase: 02-object-store-resources
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - internal/client/object_store_access_keys.go
  - internal/testmock/handlers/object_store_access_keys.go
  - internal/provider/object_store_access_key_resource.go
  - internal/provider/object_store_access_key_resource_test.go
  - internal/provider/object_store_access_key_data_source.go
  - internal/provider/object_store_access_key_data_source_test.go
  - internal/provider/provider.go
autonomous: true
requirements:
  - OAK-01
  - OAK-02
  - OAK-03
  - OAK-04
  - OAK-05

must_haves:
  truths:
    - "Operator can create an access key for an object store account"
    - "Operator can delete an access key"
    - "Secret access key is Sensitive and preserved in state after creation (never overwritten by Read)"
    - "Access key has no import support (secret unavailable after creation)"
    - "Data source returns access key attributes by name (without secret)"
  artifacts:
    - path: "internal/client/object_store_access_keys.go"
      provides: "GetObjectStoreAccessKey, ListObjectStoreAccessKeys, PostObjectStoreAccessKey, DeleteObjectStoreAccessKey"
      exports: ["GetObjectStoreAccessKey", "PostObjectStoreAccessKey", "DeleteObjectStoreAccessKey"]
    - path: "internal/testmock/handlers/object_store_access_keys.go"
      provides: "RegisterObjectStoreAccessKeyHandlers"
      exports: ["RegisterObjectStoreAccessKeyHandlers"]
    - path: "internal/provider/object_store_access_key_resource.go"
      provides: "flashblade_object_store_access_key resource with Create + Read + Delete (no Update, no Import)"
      exports: ["NewAccessKeyResource"]
    - path: "internal/provider/object_store_access_key_data_source.go"
      provides: "flashblade_object_store_access_key data source"
      exports: ["NewAccessKeyDataSource"]
  key_links:
    - from: "internal/provider/object_store_access_key_resource.go"
      to: "internal/client/object_store_access_keys.go"
      via: "r.client.PostObjectStoreAccessKey / DeleteObjectStoreAccessKey"
      pattern: "r\\.client\\.(Post|Delete|Get)ObjectStoreAccessKey"
    - from: "internal/provider/object_store_access_key_resource.go"
      to: "state preservation"
      via: "Read does NOT overwrite SecretAccessKey — preserved from state"
      pattern: "data\\.SecretAccessKey"
---

<objective>
Access key resource: client methods, mock handler, resource with write-once secret pattern, and data source.

Purpose: Access key is the final resource in the object store chain. It is a unique resource type — immutable (ForceNew on all attributes), no Update method, no Import support, and the secret_access_key field is write-once (returned only at creation, preserved in state).
Output: Working flashblade_object_store_access_key resource and data source.
</objective>

<execution_context>
@/home/gule/.claude/get-shit-done/workflows/execute-plan.md
@/home/gule/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-object-store-resources/02-CONTEXT.md
@.planning/phases/02-object-store-resources/02-RESEARCH.md
@.planning/phases/02-object-store-resources/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs (models and account client) -->

From internal/client/models.go (added in Plan 01):
```go
type NamedReference struct {
    Name string `json:"name"`
    ID   string `json:"id,omitempty"`
}

type ObjectStoreAccessKey struct {
    Name            string         `json:"name"`
    AccessKeyID     string         `json:"access_key_id"`
    SecretAccessKey string         `json:"secret_access_key,omitempty"`
    Created         int64          `json:"created,omitempty"`
    Enabled         bool           `json:"enabled"`
    User            NamedReference `json:"user"`
}

type ObjectStoreAccessKeyPost struct {
    User NamedReference `json:"user"`
}
```

From internal/testmock/handlers/helpers.go (Plan 01):
```go
func WriteJSONListResponse(w http.ResponseWriter, statusCode int, items interface{})
func WriteJSONError(w http.ResponseWriter, statusCode int, message string)
```

From internal/testmock/handlers/object_store_accounts.go (Plan 01):
```go
func RegisterObjectStoreAccountHandlers(mux *http.ServeMux) *objectStoreAccountStore
```

From 02-RESEARCH.md — access key POST body:
```go
// user field: {name: "<account>/admin"}
// POST response includes secret_access_key (only time it's returned)
// GET response does NOT include secret_access_key
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Access key client methods + mock handler</name>
  <files>internal/client/object_store_access_keys.go, internal/testmock/handlers/object_store_access_keys.go</files>
  <action>
1. **internal/client/object_store_access_keys.go** — Access key client methods:
   - `ListObjectStoreAccessKeysOpts` struct: `Names []string`, `Filter string`
   - `GetObjectStoreAccessKey(ctx, name) (*ObjectStoreAccessKey, error)` — GET /object-store-access-keys?names={name}, synthesize 404 on empty items. NOTE: GET response will NOT contain secret_access_key.
   - `ListObjectStoreAccessKeys(ctx, opts) ([]ObjectStoreAccessKey, error)` — GET with query params
   - `PostObjectStoreAccessKey(ctx, body ObjectStoreAccessKeyPost) (*ObjectStoreAccessKey, error)` — POST /object-store-access-keys, body is `{user: {name: "<account>/admin"}}`. Response includes secret_access_key — caller MUST capture it immediately.
   - `DeleteObjectStoreAccessKey(ctx, name string) error` — DELETE /object-store-access-keys?names={name} (uses name, not id — per API pattern from research)
   - No Update method — access keys are immutable.
   - Zero terraform-plugin-framework imports.

2. **internal/testmock/handlers/object_store_access_keys.go** — Mock handler:
   - `accessKeyStore` struct: `mu sync.Mutex`, `byName map[string]*client.ObjectStoreAccessKey`, `accounts *objectStoreAccountStore` (for user/account validation)
   - `RegisterObjectStoreAccessKeyHandlers(mux *http.ServeMux, accounts *objectStoreAccountStore)` — takes account store to validate user's account exists
   - HandleFunc on `/api/2.22/object-store-access-keys`
   - GET: filter by `?names=`. Return access keys WITHOUT secret_access_key field (set to empty string in response — simulates real API behavior where secret is not returned on GET).
   - POST: parse body `{user: {name: "<account>/admin"}}`, validate account portion exists in account store. Generate a random access_key_id (e.g., `PSFBIKEYID` + random suffix), generate a random secret_access_key (e.g., 40-char random string). Store in map WITH secret. Return in response WITH secret (POST only returns it).
   - DELETE: `?names=` lookup, remove from store.
   - No PATCH handler — access keys are immutable.
   - Use `WriteJSONListResponse` and `WriteJSONError` from helpers.go.
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && go build ./internal/...</automated>
  </verify>
  <done>Access key client methods compile. Mock handler registers, generates keys with secrets on POST, and strips secrets on GET.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Access key resource + data source + provider registration + tests</name>
  <files>internal/provider/object_store_access_key_resource.go, internal/provider/object_store_access_key_resource_test.go, internal/provider/object_store_access_key_data_source.go, internal/provider/object_store_access_key_data_source_test.go, internal/provider/provider.go</files>
  <behavior>
    - TestUnit_AccessKey_Create: POST creates key, state contains secret_access_key + access_key_id
    - TestUnit_AccessKey_Delete: DELETE removes key
    - TestUnit_AccessKey_SecretPreserved: after Create, a refresh/plan does NOT overwrite secret_access_key with empty string (UseStateForUnknown preserves it)
    - TestUnit_AccessKey_ForceNew: changing object_store_account triggers destroy+recreate
    - TestUnit_AccessKey_NoImport: resource does NOT implement ImportState (verify at schema/interface level)
    - TestUnit_AccessKeyDataSource: data source reads key by name, secret_access_key is empty (Computed, not returned by GET)
  </behavior>
  <action>
1. **internal/provider/object_store_access_key_resource.go** — Stripped-down resource:
   - Struct: `objectStoreAccessKeyResource` with `client *client.FlashBladeClient`
   - Factory: `NewAccessKeyResource() resource.Resource`
   - Interfaces: `resource.Resource`, `resource.ResourceWithConfigure` — **NO** `resource.ResourceWithImportState` (per user decision: no import for access keys)
   - Model: `objectStoreAccessKeyModel` — Name (Computed), ObjectStoreAccount (Required), AccessKeyID (Computed), SecretAccessKey (Computed+Sensitive), Created (Computed), Enabled (Optional+Computed), Timeouts
   - Schema key attributes:
     - `object_store_account`: Required + RequiresReplace (ForceNew — immutable)
     - `name`: Computed + UseStateForUnknown (assigned by API, format: `<account>/admin/<key-id>`)
     - `access_key_id`: Computed + UseStateForUnknown
     - `secret_access_key`: Computed + Sensitive + UseStateForUnknown (write-once — CRITICAL)
     - `enabled`: Optional + Computed + RequiresReplace (ForceNew per user decision — any change recreates)
     - `created`: Computed + UseStateForUnknown
   - Configure: extract client
   - Create:
     - Build user name: `data.ObjectStoreAccount.ValueString() + "/admin"`
     - Call PostObjectStoreAccessKey with User={Name: userName}
     - Map ALL response fields to model including SecretAccessKey — this is the ONLY time it's available
   - Read:
     - Call GetObjectStoreAccessKey(ctx, name)
     - If not found -> RemoveResource
     - Map response fields EXCEPT SecretAccessKey — do NOT set `data.SecretAccessKey = types.StringValue("")`. Leave it untouched so UseStateForUnknown preserves the original value from Create.
     - Map: Name, AccessKeyID, Created, Enabled from API response
   - Update: NOT IMPLEMENTED — no Update method. All attributes are ForceNew so terraform-plugin-framework will never call Update (it destroys and recreates instead).
   - Delete: call DeleteObjectStoreAccessKey(ctx, name). Handle not-found gracefully (already deleted).

2. **internal/provider/object_store_access_key_data_source.go** — Data source:
   - Schema: name=Required, all other fields Computed. secret_access_key is Computed+Sensitive but will always be empty (API doesn't return it on GET).
   - Read: call GetObjectStoreAccessKey, map fields. SecretAccessKey will be empty string from API — that's expected for data source.

3. **internal/provider/provider.go** — Append access key registrations:
   - Add `NewAccessKeyResource` to Resources() slice
   - Add `NewAccessKeyDataSource` to DataSources() slice

4. **Tests** — TDD RED/GREEN:
   - Setup: NewMockServer + register account handlers + register access key handlers
   - Pre-seed account in mock before access key tests
   - **Critical test: SecretPreserved** — Create key, verify secret in state, then run a second apply/refresh step, verify secret is still in state and not empty. This validates the UseStateForUnknown + Read-does-not-overwrite pattern.
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && go test ./internal/provider/ -run "TestUnit_AccessKey" -count=1 -timeout 60s -v</automated>
  </verify>
  <done>Access key resource passes Create + Delete + SecretPreserved + ForceNew tests. No ImportState interface. Data source passes. `go test ./internal/... -count=1 -timeout 60s` passes with no regressions.</done>
</task>

</tasks>

<verification>
- `go build ./internal/...` compiles
- `go test ./internal/provider/ -run "TestUnit_AccessKey" -count=1` — all access key tests pass
- `go test ./internal/... -count=1 -timeout 60s` — no regressions
- Verify access key resource does NOT implement ResourceWithImportState interface
- Verify secret_access_key is Sensitive in schema
</verification>

<success_criteria>
- Access key resource supports Create, Read, Delete (no Update, no Import)
- All attributes are ForceNew (RequiresReplace)
- secret_access_key is Sensitive and preserved in state (UseStateForUnknown, Read does not overwrite)
- No ImportState per user decision
- Data source returns key attributes (secret always empty from API)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-object-store-resources/02-03-SUMMARY.md`
</output>
