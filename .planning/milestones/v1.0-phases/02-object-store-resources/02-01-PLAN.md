---
phase: 02-object-store-resources
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/client/models.go
  - internal/client/object_store_accounts.go
  - internal/testmock/handlers/helpers.go
  - internal/testmock/handlers/object_store_accounts.go
  - internal/provider/object_store_account_resource.go
  - internal/provider/object_store_account_resource_test.go
  - internal/provider/object_store_account_data_source.go
  - internal/provider/object_store_account_data_source_test.go
  - internal/provider/provider.go
autonomous: true
requirements:
  - OSA-01
  - OSA-02
  - OSA-03
  - OSA-04
  - OSA-05

must_haves:
  truths:
    - "Operator can create an object store account by name via Terraform"
    - "Operator can update account mutable attributes (quota_limit, hard_limit_enabled)"
    - "Operator can destroy an account (simple DELETE, no soft-delete) with bucket-existence guard"
    - "Operator can import an existing account by name with 0-diff plan after import"
    - "Data source returns account attributes by name"
  artifacts:
    - path: "internal/client/models.go"
      provides: "ObjectStoreAccount, ObjectStoreAccountPost, ObjectStoreAccountPatch, Bucket, BucketPost, BucketPatch, ObjectStoreAccessKey, ObjectStoreAccessKeyPost, NamedReference model structs"
      contains: "ObjectStoreAccount"
    - path: "internal/client/object_store_accounts.go"
      provides: "GetObjectStoreAccount, ListObjectStoreAccounts, PostObjectStoreAccount, PatchObjectStoreAccount, DeleteObjectStoreAccount"
      exports: ["GetObjectStoreAccount", "PostObjectStoreAccount", "PatchObjectStoreAccount", "DeleteObjectStoreAccount"]
    - path: "internal/testmock/handlers/object_store_accounts.go"
      provides: "RegisterObjectStoreAccountHandlers, objectStoreAccountStore"
      exports: ["RegisterObjectStoreAccountHandlers"]
    - path: "internal/provider/object_store_account_resource.go"
      provides: "flashblade_object_store_account resource with CRUD + Import"
      exports: ["NewObjectStoreAccountResource"]
    - path: "internal/provider/object_store_account_data_source.go"
      provides: "flashblade_object_store_account data source"
      exports: ["NewObjectStoreAccountDataSource"]
  key_links:
    - from: "internal/provider/object_store_account_resource.go"
      to: "internal/client/object_store_accounts.go"
      via: "r.client.PostObjectStoreAccount / PatchObjectStoreAccount / DeleteObjectStoreAccount"
      pattern: "r\\.client\\.(Post|Patch|Delete|Get)ObjectStoreAccount"
    - from: "internal/provider/provider.go"
      to: "internal/provider/object_store_account_resource.go"
      via: "Resources() registration"
      pattern: "NewObjectStoreAccountResource"
---

<objective>
Object store account: models, client CRUD, mock handler, resource, data source, and provider registration.

Purpose: Establishes the first resource in the Phase 2 dependency chain. Account must exist before buckets or access keys. Also adds ALL model structs for Phase 2 (Bucket, AccessKey) so Plans 02 and 03 can work without touching models.go.
Output: Working flashblade_object_store_account resource and data source with full CRUD lifecycle.
</objective>

<execution_context>
@/home/gule/.claude/get-shit-done/workflows/execute-plan.md
@/home/gule/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-object-store-resources/02-CONTEXT.md
@.planning/phases/02-object-store-resources/02-RESEARCH.md

<interfaces>
<!-- Existing client layer pattern from Phase 1 -->

From internal/client/models.go:
```go
type ListResponse[T any] struct {
    Items             []T    `json:"items"`
    ContinuationToken string `json:"continuation_token,omitempty"`
    TotalItemCount    int    `json:"total_item_count,omitempty"`
}
```

From internal/client/filesystems.go (pattern to replicate):
```go
func (c *FlashBladeClient) GetFileSystem(ctx context.Context, name string) (*FileSystem, error)
func (c *FlashBladeClient) ListFileSystems(ctx context.Context, opts ListFileSystemsOpts) ([]FileSystem, error)
func (c *FlashBladeClient) PostFileSystem(ctx context.Context, body FileSystemPost) (*FileSystem, error)
func (c *FlashBladeClient) PatchFileSystem(ctx context.Context, id string, body FileSystemPatch) (*FileSystem, error)
func (c *FlashBladeClient) DeleteFileSystem(ctx context.Context, id string) error
```

From internal/provider/provider.go (registration point — lines 267-277):
```go
func (p *FlashBladeProvider) Resources(_ context.Context) []func() resource.Resource {
    return []func() resource.Resource{
        NewFilesystemResource,
    }
}
func (p *FlashBladeProvider) DataSources(_ context.Context) []func() datasource.DataSource {
    return []func() datasource.DataSource{
        NewFilesystemDataSource,
    }
}
```

From internal/testmock/handlers/filesystems.go (mock pattern):
```go
func RegisterFileSystemHandlers(mux *http.ServeMux)
// writeListResponse and writeError are unexported, typed to FileSystem
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Model structs + account client CRUD + mock handler + generic helpers</name>
  <files>internal/client/models.go, internal/client/object_store_accounts.go, internal/testmock/handlers/helpers.go, internal/testmock/handlers/object_store_accounts.go</files>
  <action>
1. **internal/client/models.go** — Append ALL Phase 2 model structs (so Plans 02/03 don't need to touch this file):
   - `NamedReference` struct: `Name string`, `ID string` (json tags)
   - `ObjectStoreAccount` struct: `ID`, `Name`, `Created` (int64), `QuotaLimit` (string), `HardLimitEnabled` (bool), `ObjectCount` (int64), `Space` (Space, reuse existing)
   - `ObjectStoreAccountPost` struct: mutable fields only (`QuotaLimit`, `HardLimitEnabled`)
   - `ObjectStoreAccountPatch` struct: pointer fields for PATCH semantics (`QuotaLimit *string`, `HardLimitEnabled *bool`)
   - `Bucket` struct: `ID`, `Name`, `Account` (NamedReference), `Created`, `Destroyed` (bool), `TimeRemaining` (int64), `Versioning` (string), `QuotaLimit` (string), `HardLimitEnabled` (bool), `ObjectCount` (int64), `BucketType` (string), `RetentionLock` (string), `Space` (Space)
   - `BucketPost` struct: `Account` (NamedReference), `Versioning`, `QuotaLimit`, `HardLimitEnabled`, `RetentionLock`
   - `BucketPatch` struct: pointer fields (`Destroyed *bool`, `Versioning *string`, `QuotaLimit *string`, `HardLimitEnabled *bool`, `RetentionLock *string`)
   - `ObjectStoreAccessKey` struct: `Name`, `AccessKeyID`, `SecretAccessKey`, `Created` (int64), `Enabled` (bool), `User` (NamedReference)
   - `ObjectStoreAccessKeyPost` struct: `User` (NamedReference)
   - All json tags must match API field names exactly (snake_case). Use `omitempty` on optional/pointer fields.

2. **internal/client/object_store_accounts.go** — Account CRUD methods on FlashBladeClient:
   - `ListObjectStoreAccountsOpts` struct with `Names []string`, `Filter string`
   - `GetObjectStoreAccount(ctx, name) (*ObjectStoreAccount, error)` — GET /object-store-accounts?names={name}, synthesize 404 on empty items (same as GetFileSystem pattern)
   - `ListObjectStoreAccounts(ctx, opts) ([]ObjectStoreAccount, error)` — GET with query params
   - `PostObjectStoreAccount(ctx, name string, body ObjectStoreAccountPost) (*ObjectStoreAccount, error)` — POST /object-store-accounts?names={name} with body for optional fields. NOTE: name is a query param on POST, not in body.
   - `PatchObjectStoreAccount(ctx, name string, body ObjectStoreAccountPatch) (*ObjectStoreAccount, error)` — PATCH /object-store-accounts?names={name}
   - `DeleteObjectStoreAccount(ctx, name string) error` — DELETE /object-store-accounts?names={name} (single-phase, no soft-delete)
   - Zero terraform-plugin-framework imports. Pure Go + context + fmt + net/url.

3. **internal/testmock/handlers/helpers.go** — Extract generic helpers from filesystems.go pattern:
   - `WriteJSONListResponse(w http.ResponseWriter, statusCode int, items interface{})` — generic version using `interface{}` or `any` for the items slice, wraps in `{"items": ..., "total_item_count": ...}` envelope
   - `WriteJSONError(w http.ResponseWriter, statusCode int, message string)` — generic error response
   - These are exported helpers so all handler files can use them. Update filesystems.go to use these helpers instead of its local unexported versions (remove `writeListResponse` and `writeError` from filesystems.go, replace calls with `WriteJSONListResponse` and `WriteJSONError`).

4. **internal/testmock/handlers/object_store_accounts.go** — Mock handler:
   - `objectStoreAccountStore` struct: `mu sync.Mutex`, `byName map[string]*client.ObjectStoreAccount`, `byID map[string]*client.ObjectStoreAccount`
   - `RegisterObjectStoreAccountHandlers(mux *http.ServeMux) *objectStoreAccountStore` — returns the store pointer so bucket handler can cross-reference it for account validation
   - Handle GET (with `?names=` filter), POST (name from `?names=` query param), PATCH (`?names=`), DELETE (`?names=`)
   - POST: generate UUID, set Created to time.Now().UnixMilli(), return in items envelope
   - DELETE: check if account has no buckets (this validation will be wired when bucket handler registers — for now just delete)
   - Thread-safe with mutex, same pattern as fileSystemStore
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && go build ./internal/...</automated>
  </verify>
  <done>All model structs compile. Account client methods compile. Mock handler registers on mux. Generic helpers used by both filesystem and account handlers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Object store account resource + data source + provider registration + tests</name>
  <files>internal/provider/object_store_account_resource.go, internal/provider/object_store_account_resource_test.go, internal/provider/object_store_account_data_source.go, internal/provider/object_store_account_data_source_test.go, internal/provider/provider.go</files>
  <behavior>
    - TestUnit_ObjectStoreAccount_Create: POST creates account with name, returns id + created + name
    - TestUnit_ObjectStoreAccount_Update: PATCH updates quota_limit and hard_limit_enabled
    - TestUnit_ObjectStoreAccount_Delete: DELETE removes account, subsequent plan shows recreate
    - TestUnit_ObjectStoreAccount_Import: import by name populates all attributes, subsequent plan shows 0 diff
    - TestUnit_ObjectStoreAccount_ForceNew: changing name triggers destroy+recreate (RequiresReplace)
    - TestUnit_ObjectStoreAccountDataSource: data source reads account by name, returns all attributes
  </behavior>
  <action>
1. **internal/provider/object_store_account_resource.go** — Full resource following filesystem_resource.go pattern:
   - Struct: `objectStoreAccountResource` with `client *client.FlashBladeClient`
   - Factory: `NewObjectStoreAccountResource() resource.Resource`
   - Interfaces: `resource.Resource`, `resource.ResourceWithConfigure`, `resource.ResourceWithImportState`
   - Model: `objectStoreAccountModel` — ID, Name, Created, QuotaLimit (types.String), HardLimitEnabled (types.Bool), ObjectCount (types.Int64), Space (nested computed), Timeouts
   - Schema: name=Required+RequiresReplace, id=Computed+UseStateForUnknown, created=Computed+UseStateForUnknown, quota_limit=Optional+Computed, hard_limit_enabled=Optional+Computed, object_count=Computed, space=Computed nested block
   - Configure: extract client from provider data (same as filesystem)
   - Create: call PostObjectStoreAccount(ctx, name, body), map response to model
   - Read: call GetObjectStoreAccount(ctx, name), detect not-found -> RemoveResource, drift log via tflog for quota_limit and hard_limit_enabled changes
   - Update: call PatchObjectStoreAccount with only changed fields (pointer-based PATCH)
   - Delete: single-phase DELETE (no soft-delete). Before DELETE, call ListBuckets filtering by account name — if any exist, return diagnostic: "account has existing buckets, destroy them first"
   - ImportState: set Name from import ID, return. Read will populate everything.
   - Include timeouts block (Optional, same pattern as filesystem)

2. **internal/provider/object_store_account_data_source.go** — Data source:
   - Follow filesystem_data_source.go pattern exactly
   - Schema: name=Required, all other fields Computed
   - Read: call GetObjectStoreAccount, map to model

3. **internal/provider/provider.go** — Register ALL Phase 2 resources and data sources:
   - Add to Resources(): `NewObjectStoreAccountResource`, `NewBucketResource`, `NewAccessKeyResource`
   - Add to DataSources(): `NewObjectStoreAccountDataSource`, `NewBucketDataSource`, `NewAccessKeyDataSource`
   - NOTE: Bucket and AccessKey factories will be created in Plans 02/03. For now, add placeholder stub files (`bucket_resource.go`, `object_store_access_key_resource.go`, etc.) with minimal factory functions that return empty structs satisfying `resource.Resource` interface — OR simply add only the account registrations now and let Plans 02/03 append their own. **Choose the simpler path: register only account now. Plans 02/03 will each append to provider.go.**
   - Actually, to avoid file conflicts: register ONLY account resource + data source in this plan. Plans 02/03 will add their own.

4. **Tests** — Following Phase 1 TDD pattern (RED then GREEN):
   - Use `testmock.NewMockServer()` + `handlers.RegisterObjectStoreAccountHandlers(ms.Mux)`
   - Use `resource.Test` from terraform-plugin-testing for lifecycle tests
   - Follow exact pattern from `filesystem_resource_test.go`: providerFactories setup, HCL config helpers
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && go test ./internal/provider/ -run "TestUnit_ObjectStoreAccount|TestUnit_ObjectStoreAccountDataSource" -count=1 -timeout 60s -v</automated>
  </verify>
  <done>Account resource passes all CRUD + import + drift tests. Data source test passes. Provider registers account resource and data source. `go test ./internal/... -count=1 -timeout 60s` passes with no regressions.</done>
</task>

</tasks>

<verification>
- `go build ./internal/...` compiles without errors
- `go test ./internal/provider/ -run "TestUnit_ObjectStoreAccount" -count=1` — all account tests pass
- `go test ./internal/... -count=1 -timeout 60s` — no regressions on Phase 1 tests
</verification>

<success_criteria>
- Object store account resource supports Create, Read, Update (quota/hard_limit), Delete (with bucket guard), and Import
- Name attribute has RequiresReplace (ForceNew)
- Delete is single-phase (no soft-delete) per user decision
- Data source reads account by name
- All tests pass including Phase 1 regressions
</success_criteria>

<output>
After completion, create `.planning/phases/02-object-store-resources/02-01-SUMMARY.md`
</output>
