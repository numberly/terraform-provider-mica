---
phase: 05-quality-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/client/errors.go
  - internal/client/errors_test.go
  - internal/provider/bucket_resource.go
  - internal/provider/quota_group_resource.go
  - internal/provider/quota_user_resource.go
  - internal/provider/bucket_resource_test.go
  - internal/provider/object_store_access_policy_rule_resource_test.go
  - internal/provider/network_access_policy_rule_resource_test.go
  - internal/provider/smb_share_policy_rule_resource_test.go
  - internal/provider/snapshot_policy_resource_test.go
  - internal/provider/snapshot_policy_rule_resource_test.go
  - internal/provider/nfs_export_policy_rule_resource_test.go
  - internal/provider/quota_group_resource_test.go
  - internal/provider/quota_user_resource_test.go
  - internal/provider/filesystem_resource_test.go
  - internal/provider/nfs_export_policy_resource_test.go
  - internal/provider/smb_share_policy_resource_test.go
  - internal/provider/array_dns_resource_test.go
  - internal/provider/array_ntp_resource_test.go
  - internal/provider/array_smtp_resource_test.go
  - internal/provider/network_access_policy_resource_test.go
  - internal/provider/object_store_access_policy_resource_test.go
  - internal/provider/object_store_access_key_resource_test.go
  - internal/provider/object_store_account_resource_test.go
autonomous: true
requirements:
  - QUA-01
  - QUA-02
  - QUA-03

must_haves:
  truths:
    - "Every resource with RequiresReplace in schema has a test asserting it"
    - "Every resource with UseStateForUnknown in schema has a test asserting it"
    - "The OAP rule effect validator rejects invalid values in a direct validator test"
    - "Bucket versioning validator rejects invalid values like 'invalid' and accepts 'none', 'enabled', 'suspended'"
    - "Quota group and quota user reject negative quota values at plan time"
    - "IsConflict and IsUnprocessable error helpers exist and are tested"
  artifacts:
    - path: "internal/client/errors.go"
      provides: "IsConflict and IsUnprocessable helpers"
      contains: "IsConflict"
    - path: "internal/client/errors_test.go"
      provides: "Unit tests for error helpers"
      contains: "TestUnit_IsConflict"
    - path: "internal/provider/bucket_resource.go"
      provides: "Versioning validator added to schema"
      contains: "stringvalidator.OneOf"
    - path: "internal/provider/quota_group_resource.go"
      provides: "Quota AtLeast(0) validator added"
      contains: "int64validator.AtLeast"
    - path: "internal/provider/quota_user_resource.go"
      provides: "Quota AtLeast(0) validator added"
      contains: "int64validator.AtLeast"
  key_links:
    - from: "internal/provider/*_resource_test.go"
      to: "internal/provider/*_resource.go"
      via: "Schema() call to inspect plan modifiers"
      pattern: "PlanModifiers"
---

<objective>
Add error helpers (IsConflict, IsUnprocessable) to the client layer, add missing validators to production schemas (bucket versioning, quota limits), then add plan modifier assertions and validator tests across all 19 resources.

Purpose: QUA-01 requires verified plan modifiers on all resources, QUA-02 requires validated input rejection (currently only OAP rule effect has a validator — bucket versioning and quota limits lack plan-time validation), QUA-03 requires unit test coverage for schema correctness. Currently 16/19 resources lack plan-modifier test assertions and only 1 of 19 resources has any validator.
Output: Error helper functions with tests, new validators on 3 resources, plan modifier assertion tests in every resource test file, validator rejection tests for all validated fields.
</objective>

<execution_context>
@/home/gule/.claude/get-shit-done/workflows/execute-plan.md
@/home/gule/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-quality-hardening/05-RESEARCH.md

@internal/client/errors.go
@internal/provider/object_store_access_key_resource_test.go
@internal/provider/bucket_resource.go
@internal/provider/quota_group_resource.go
@internal/provider/quota_user_resource.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add IsConflict/IsUnprocessable error helpers with tests</name>
  <files>internal/client/errors.go, internal/client/errors_test.go</files>
  <behavior>
    - TestUnit_IsConflict_True: IsConflict(&APIError{StatusCode: 409}) returns true
    - TestUnit_IsConflict_False: IsConflict(&APIError{StatusCode: 404}) returns false
    - TestUnit_IsConflict_Nil: IsConflict(nil) returns false
    - TestUnit_IsUnprocessable_True: IsUnprocessable(&APIError{StatusCode: 422}) returns true
    - TestUnit_IsUnprocessable_False: IsUnprocessable(&APIError{StatusCode: 404}) returns false
    - TestUnit_IsUnprocessable_Nil: IsUnprocessable(nil) returns false
  </behavior>
  <action>
    Write failing tests first in `internal/client/errors_test.go` (create file — it does not exist yet).

    Then add to `internal/client/errors.go`:
    - `IsConflict(err error) bool` — returns true when err is *APIError with StatusCode 409. Follow exact pattern of IsNotFound.
    - `IsUnprocessable(err error) bool` — returns true when err is *APIError with StatusCode 422. Same pattern.

    These helpers are consumed by provider resource Create/Update methods in Plan 02 error-path tests.
  </action>
  <verify>
    <automated>go test ./internal/client/... -run "TestUnit_Is(Conflict|Unprocessable)" -count=1 -v</automated>
  </verify>
  <done>IsConflict returns true for 409, false otherwise. IsUnprocessable returns true for 422, false otherwise. Both handle nil safely. All 6 tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add missing validators to production schemas and test all plan modifiers and validators</name>
  <files>
    internal/provider/bucket_resource.go,
    internal/provider/quota_group_resource.go,
    internal/provider/quota_user_resource.go,
    internal/provider/bucket_resource_test.go,
    internal/provider/object_store_access_policy_rule_resource_test.go,
    internal/provider/network_access_policy_rule_resource_test.go,
    internal/provider/smb_share_policy_rule_resource_test.go,
    internal/provider/snapshot_policy_resource_test.go,
    internal/provider/snapshot_policy_rule_resource_test.go,
    internal/provider/nfs_export_policy_rule_resource_test.go,
    internal/provider/quota_group_resource_test.go,
    internal/provider/quota_user_resource_test.go,
    internal/provider/filesystem_resource_test.go,
    internal/provider/nfs_export_policy_resource_test.go,
    internal/provider/smb_share_policy_resource_test.go,
    internal/provider/array_dns_resource_test.go,
    internal/provider/array_ntp_resource_test.go,
    internal/provider/array_smtp_resource_test.go,
    internal/provider/network_access_policy_resource_test.go,
    internal/provider/object_store_access_policy_resource_test.go,
    internal/provider/object_store_access_key_resource_test.go,
    internal/provider/object_store_account_resource_test.go
  </files>
  <behavior>
    Validator addition (production code):
    - bucket_resource.go `versioning` field: stringvalidator.OneOf("none", "enabled", "suspended") rejects "invalid"
    - quota_group_resource.go `quota` field: int64validator.AtLeast(0) rejects -1
    - quota_user_resource.go `quota` field: int64validator.AtLeast(0) rejects -1

    Validator tests:
    - TestUnit_OAPRule_EffectValidator: rejects "invalid", accepts "allow" and "deny"
    - TestUnit_Bucket_VersioningValidator: rejects "invalid", accepts "none", "enabled", "suspended"
    - TestUnit_QuotaGroup_QuotaValidator: rejects -1, accepts 0 and 1048576
    - TestUnit_QuotaUser_QuotaValidator: rejects -1, accepts 0 and 1048576

    PlanModifier tests:
    - All 19 resources have TestUnit_{Resource}_PlanModifiers asserting every RequiresReplace and UseStateForUnknown modifier
  </behavior>
  <action>
    **Step 1: Add validators to production schemas (QUA-02)**

    Per CONTEXT.md locked decision: "All resources validate input at plan time (invalid quota values, enum fields, required references)". Only 1 of 19 resources has any validator. Add validators to resources with obvious enum/range fields:

    1. `internal/provider/bucket_resource.go` — Add `Validators: []validator.String{stringvalidator.OneOf("none", "enabled", "suspended")}` to the `versioning` StringAttribute (description already says "'none', 'enabled', or 'suspended'"). Import `stringvalidator` if not already imported.

    2. `internal/provider/quota_group_resource.go` — Add `Validators: []validator.Int64{int64validator.AtLeast(0)}` to the `quota` Int64Attribute. Quota is in bytes, negative values are nonsensical. Import `int64validator`.

    3. `internal/provider/quota_user_resource.go` — Same as quota_group: add `int64validator.AtLeast(0)` to `quota` field.

    Other resources (filesystem, nfs_export_policy, smb_share_policy, array_*, network_access_policy, snapshot_policy) do not have clear enum/range fields that warrant validators — skip them per checker guidance.

    **Step 2: Write validator tests first (RED)**

    Write failing tests for each new and existing validator:

    - `TestUnit_OAPRule_EffectValidator` in `object_store_access_policy_rule_resource_test.go`:
      Get schema, extract `effect` attribute `Validators`, call `ValidateString` with "invalid" (expect error), "allow" (expect no error), "deny" (expect no error).

    - `TestUnit_Bucket_VersioningValidator` in `bucket_resource_test.go`:
      Get schema, extract `versioning` attribute `Validators`, call `ValidateString` with "invalid" (expect error), "none"/"enabled"/"suspended" (expect no error).

    - `TestUnit_QuotaGroup_QuotaValidator` in `quota_group_resource_test.go`:
      Get schema, extract `quota` attribute `Validators`, call `ValidateInt64` with -1 (expect error), 0 (expect no error), 1048576 (expect no error).

    - `TestUnit_QuotaUser_QuotaValidator` in `quota_user_resource_test.go`:
      Same pattern as quota_group.

    **Step 3: Plan modifier assertions for all 19 resources**

    For each of the 19 resource test files, add a `TestUnit_{Resource}_PlanModifiers` function.

    **Pattern to follow** (from existing `object_store_access_key_resource_test.go`):
    1. Call the resource's schema helper (e.g., `bucketResourceSchema(t)`)
    2. For each field with `RequiresReplace` in the production schema: assert the attribute's `PlanModifiers` slice contains a RequiresReplace modifier
    3. For each field with `UseStateForUnknown` in the production schema: assert UseStateForUnknown modifier is present
    4. Use type assertion to cast `schema.Attribute` to the concrete type (e.g., `schema.StringAttribute`)

    **Resources needing RequiresReplace assertions** (from RESEARCH.md):
    - bucket: `name`, `account`
    - object_store_access_policy_rule: `effect`, `policy_name`, `name`
    - network_access_policy_rule: `policy_name`
    - smb_share_policy_rule: `policy_name`
    - snapshot_policy: `name`
    - snapshot_policy_rule: `policy_name`
    - nfs_export_policy_rule: `policy_name` (check schema first)
    - quota_group: `file_system_name`, `gid`
    - quota_user: `file_system_name`, `uid`

    **Resources needing UseStateForUnknown assertions** (all resources have `id` as computed):
    - Every resource: `id` field should have `UseStateForUnknown`
    - Check each resource schema for other Computed+stable fields

    **Resources with no RequiresReplace** (still need UseStateForUnknown on `id`):
    - filesystem, nfs_export_policy, smb_share_policy, array_dns, array_ntp, array_smtp, network_access_policy, object_store_access_policy

    **For object_store_access_key and object_store_account**: Already have some assertions — read existing tests first and add any MISSING modifier assertions (e.g., UseStateForUnknown on `id` if missing).

    Approach: read each resource's Schema() to discover the exact fields with modifiers before writing assertions. Do not guess — inspect the actual schema.
  </action>
  <verify>
    <automated>go test ./internal/provider/... -run "TestUnit.*(PlanModifier|Validator)" -count=1 -v</automated>
  </verify>
  <done>All 19 resources have a PlanModifiers test. Every RequiresReplace and UseStateForUnknown modifier in production schemas is covered by an assertion. Bucket versioning validator rejects "invalid" and accepts "none"/"enabled"/"suspended". Quota validators reject -1 and accept 0. OAP rule effect validator rejects "invalid" and accepts "allow"/"deny". All tests pass alongside existing 118 provider tests.</done>
</task>

</tasks>

<verification>
- `go test ./internal/... -count=1 -timeout 5m` — full suite passes (existing 147 + new tests)
- `go test ./internal/provider/... -run "TestUnit.*PlanModifier" -count=1` — plan modifier tests pass for all 19 resources
- `go test ./internal/provider/... -run "TestUnit.*Validator" -count=1` — validator rejection tests pass for OAP rule effect, bucket versioning, quota group, quota user
- `go test ./internal/client/... -run "TestUnit_Is" -count=1` — error helpers pass
</verification>

<success_criteria>
- IsConflict(409) and IsUnprocessable(422) helpers exist and are tested
- Bucket versioning field has stringvalidator.OneOf("none", "enabled", "suspended") in production schema
- Quota group and quota user quota fields have int64validator.AtLeast(0) in production schema
- Every resource file has a PlanModifiers test function
- Every RequiresReplace field has a test assertion
- Every UseStateForUnknown field has a test assertion
- All 4 validator tests pass (OAP effect, bucket versioning, quota group, quota user)
- Full test suite passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/05-quality-hardening/05-01-SUMMARY.md`
</output>
</content>
</invoke>