# Phase 5: Quality Hardening - Context

**Gathered:** 2026-03-28
**Status:** Ready for planning

<domain>
## Phase Boundary

Fill test coverage gaps across all resources, add retry/error/pagination testing, generate documentation via terraform-plugin-docs, create GitHub Actions CI workflow, and write project README. No new resources or features.

</domain>

<decisions>
## Implementation Decisions

### Test Coverage Targets
- **Full lifecycle mocked tests**: Every resource gets Create→Read→Update→Read→Delete mocked integration test + import→plan→0-diff test
- **Schema/validator unit tests**: Every resource gets schema correctness tests (required fields, plan modifiers, validators) — catch config regressions
- **Both are equally critical** — no prioritization compromise
- **HTTP retry tests**: Mock server returns 429/503 on first attempt, succeeds on retry. Verify client handles transparently.

### Edge Case Testing
- **API error handling**: Every API error (404, 409 conflict, 422 validation) produces a clear Terraform diagnostic — test all error paths
- **Pagination**: List operations with continuation_token return all results — test mock server pagination behavior
- NOT testing concurrent modifications (out of scope for unit tests — would need real array)

### CI Integration
- **GitHub Actions workflow** for automated testing on push/PR
- Existing GNUmakefile targets enhanced as needed

### Documentation
- **terraform-plugin-docs** for auto-generation from schema + examples/ directory
- **Comprehensive examples**: Multiple HCL examples per resource — basic, advanced, import, and common patterns
- **Standard provider README**: Installation, configuration, quick start, resource list, contributing guide

### Claude's Discretion
- Which specific tests are missing vs already covered from Phases 1-4
- Test file organization (one file per resource vs grouped)
- terraform-plugin-docs template customization
- GitHub Actions workflow structure (matrix, caching, etc.)
- README structure and content depth

</decisions>

<specifics>
## Specific Ideas

- 147 tests already exist from Phases 1-4 — Phase 5 fills gaps, doesn't rebuild
- terraform-plugin-docs requires example HCL in `examples/` directory — some already exist from Phase 1
- The ops team needs reliable diagnostics when things go wrong — error handling tests are critical
- GitHub Actions should run `go test ./... -count=1` on every PR

</specifics>

<code_context>
## Existing Code Insights

### Reusable Assets
- 147 existing unit tests across `internal/provider/` and `internal/client/`
- Mock server with handlers for all resource families in `internal/testmock/`
- `GNUmakefile` with `make build`, `make test`, `make testacc`, `make lint`
- `examples/provider/provider.tf` and `examples/resources/flashblade_file_system/resource.tf` already exist
- `.goreleaser.yml` for release builds

### Established Patterns
- TDD RED/GREEN per task
- Mock-based tests with `httptest.NewServer`
- `testProviderFactory` shared across all test files
- Schema tests follow `resp.Schema.Attributes["field"]` pattern

### Integration Points
- `tools/tools.go` — pin terraform-plugin-docs tool
- `examples/` directory — terraform-plugin-docs reads this
- `.github/workflows/` — GitHub Actions CI
- `docs/` — generated by terraform-plugin-docs

</code_context>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope

</deferred>

---

*Phase: 05-quality-hardening*
*Context gathered: 2026-03-28*
