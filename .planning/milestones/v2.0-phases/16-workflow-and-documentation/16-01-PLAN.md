---
phase: 16-workflow-and-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/workflows/s3-bucket-replication/main.tf
  - examples/resources/flashblade_object_store_remote_credentials/resource.tf
  - examples/resources/flashblade_object_store_remote_credentials/import.sh
  - examples/resources/flashblade_bucket_replica_link/resource.tf
  - examples/resources/flashblade_bucket_replica_link/import.sh
  - examples/data-sources/flashblade_array_connection/data-source.tf
  - README.md
autonomous: true
requirements:
  - WFL-01
  - DOC-01
  - DOC-02
  - DOC-03

must_haves:
  truths:
    - "Operator can copy the replication workflow example and adapt it for their dual-FlashBlade setup"
    - "Every new resource and data source has HCL examples visible in generated docs"
    - "tfplugindocs generates without errors including all new resources"
    - "README reflects accurate resource counts including replication category"
  artifacts:
    - path: "examples/workflows/s3-bucket-replication/main.tf"
      provides: "Complete dual-provider replication workflow"
      contains: "provider.*flashblade.*alias"
    - path: "examples/resources/flashblade_object_store_remote_credentials/resource.tf"
      provides: "Remote credentials HCL example"
      contains: "flashblade_object_store_remote_credentials"
    - path: "examples/resources/flashblade_object_store_remote_credentials/import.sh"
      provides: "Remote credentials import docs"
      contains: "terraform import"
    - path: "examples/resources/flashblade_bucket_replica_link/resource.tf"
      provides: "Bucket replica link HCL example"
      contains: "flashblade_bucket_replica_link"
    - path: "examples/resources/flashblade_bucket_replica_link/import.sh"
      provides: "Bucket replica link import docs"
      contains: "terraform import"
    - path: "examples/data-sources/flashblade_array_connection/data-source.tf"
      provides: "Array connection data source example"
      contains: "flashblade_array_connection"
    - path: "README.md"
      provides: "Updated resource tables with replication category"
      contains: "Replication"
  key_links:
    - from: "examples/resources/flashblade_object_store_remote_credentials/resource.tf"
      to: "internal/provider/remote_credentials_resource.go"
      via: "schema attribute names must match"
      pattern: "access_key_id|secret_access_key|remote_name"
    - from: "examples/resources/flashblade_bucket_replica_link/resource.tf"
      to: "internal/provider/bucket_replica_link_resource.go"
      via: "schema attribute names must match"
      pattern: "local_bucket_name|remote_bucket_name|remote_credentials_name"
---

<objective>
Create the dual-provider replication workflow example, HCL examples with import.sh for all new resources, regenerate docs, and update README with the replication category.

Purpose: Operators need copy-pasteable examples and Registry-ready documentation for the new replication resources before v2.0 ships.
Output: Workflow example, 3 resource/DS examples, regenerated docs/, updated README.
</objective>

<execution_context>
@/home/gule/.claude/get-shit-done/workflows/execute-plan.md
@/home/gule/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@internal/provider/remote_credentials_resource.go
@internal/provider/bucket_replica_link_resource.go
@internal/provider/array_connection_data_source.go
@examples/workflows/s3-tenant-full-stack/main.tf

<interfaces>
<!-- Resource schema attributes the executor needs for HCL examples -->

From remote_credentials_resource.go:
- name (Required, ForceNew)
- access_key_id (Required, Sensitive)
- secret_access_key (Required, Sensitive)
- remote_name (Required, ForceNew)
- id (Computed)
- timeouts (Optional)

From bucket_replica_link_resource.go:
- local_bucket_name (Required, ForceNew)
- remote_bucket_name (Required, ForceNew)
- remote_credentials_name (Optional, ForceNew)
- remote_name (Computed)
- paused (Optional, default false)
- cascading_enabled (Optional, default false, ForceNew)
- direction (Computed)
- status (Computed)
- status_details (Computed)
- lag (Computed)
- recovery_point (Computed)
- object_backlog_count (Computed)
- object_backlog_total_size (Computed)
- id (Computed)
- timeouts (Optional)

From array_connection_data_source.go:
- remote_name (Required)
- id (Computed)
- remote_id (Computed)
- status (Computed)
- management_address (Computed)
- replication_addresses (Computed, list of strings)
- encrypted (Computed)
- type (Computed)
- version (Computed)

Import ID formats:
- remote_credentials: name (e.g. "my-remote-creds")
- bucket_replica_link: "localBucket/remoteBucket" (composite ID)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HCL examples, import.sh, and replication workflow</name>
  <files>
    examples/resources/flashblade_object_store_remote_credentials/resource.tf,
    examples/resources/flashblade_object_store_remote_credentials/import.sh,
    examples/resources/flashblade_bucket_replica_link/resource.tf,
    examples/resources/flashblade_bucket_replica_link/import.sh,
    examples/data-sources/flashblade_array_connection/data-source.tf,
    examples/workflows/s3-bucket-replication/main.tf
  </files>
  <action>
Create 6 files following existing patterns in the codebase:

**1. `examples/resources/flashblade_object_store_remote_credentials/resource.tf`**
Simple example showing remote credentials creation with all required attributes (name, access_key_id, secret_access_key, remote_name). Use realistic example values.

**2. `examples/resources/flashblade_object_store_remote_credentials/import.sh`**
One-liner: `terraform import flashblade_object_store_remote_credentials.example my-remote-creds`

**3. `examples/resources/flashblade_bucket_replica_link/resource.tf`**
Example showing bucket replica link with local_bucket_name, remote_bucket_name, remote_credentials_name. Include a comment about paused default.

**4. `examples/resources/flashblade_bucket_replica_link/import.sh`**
One-liner using composite ID format: `terraform import flashblade_bucket_replica_link.example my-local-bucket/my-remote-bucket`

**5. `examples/data-sources/flashblade_array_connection/data-source.tf`**
Data source example reading by remote_name, with an output showing management_address. Follow existing DS example pattern (see syslog_server data-source.tf).

**6. `examples/workflows/s3-bucket-replication/main.tf`**
Complete dual-provider replication workflow per WFL-01 requirements. Structure following s3-tenant-full-stack pattern:
- Header comment block with architecture diagram
- terraform required_providers block
- Variables: primary_endpoint, secondary_endpoint, server_name_primary, server_name_secondary, tenant_name (plus api_token vars for both)
- Two provider aliases: `flashblade.primary` and `flashblade.secondary`
- On BOTH sides (primary + secondary):
  - Object store account (same tenant_name)
  - Bucket with versioning=enabled
  - S3 export policy + rule
  - Account export to server through S3 export policy
- Access key on primary (API generates secret)
- Access key on secondary with explicit secret_access_key from primary's output
- Remote credentials on both sides (primary points to secondary's key, secondary points to primary's key)
- Bucket replica link on both sides (bidirectional)
- Outputs: access_key_id, bucket names, replication status

Use `provider = flashblade.primary` / `provider = flashblade.secondary` meta-argument on each resource. Use clear section headers with comments explaining each step. Keep the workflow realistic and production-ready.
  </action>
  <verify>
    <automated>ls -la examples/resources/flashblade_object_store_remote_credentials/resource.tf examples/resources/flashblade_object_store_remote_credentials/import.sh examples/resources/flashblade_bucket_replica_link/resource.tf examples/resources/flashblade_bucket_replica_link/import.sh examples/data-sources/flashblade_array_connection/data-source.tf examples/workflows/s3-bucket-replication/main.tf && echo "All 6 files exist"</automated>
  </verify>
  <done>All 6 HCL example files and import.sh files exist with correct attribute names matching their respective resource schemas. Workflow demonstrates complete bidirectional replication with dual provider aliases.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate docs and update README</name>
  <files>
    docs/,
    README.md
  </files>
  <action>
**Step 1: Regenerate docs**
Run `go generate ./...` from the project root to regenerate all docs/ files. This uses tfplugindocs which reads examples/ directories for HCL snippets and import.sh files. Verify the command exits 0 and that docs for the new resources exist:
- `docs/resources/object_store_remote_credentials.md`
- `docs/resources/bucket_replica_link.md`
- `docs/data-sources/array_connection.md`

If docs already exist for remote_credentials/bucket_replica_link/array_connection from earlier phases, the regeneration will update them with the new examples.

**Step 2: Update README.md**
Add a "Replication" category section to the "Resources and Data Sources" tables, placed AFTER the "Array Administration" section and BEFORE the total count line. Format:

```
### Replication

| Resource | Data Source | Description |
|----------|:----------:|-------------|
| `flashblade_object_store_remote_credentials` | ✅ | S3 credentials for cross-array replication |
| `flashblade_bucket_replica_link` | ✅ | Bucket-to-bucket replica link (pause/resume) |
| — | `flashblade_array_connection` | Array connection status (read-only) |
```

Update the total count line from "28 resources, 21 data sources" to "30 resources, 24 data sources" (added 2 resources: remote_credentials, bucket_replica_link; added 3 data sources: array_connection, remote_credentials DS, bucket_replica_link DS).

Add the S3 Bucket Replication workflow to the Workflow Examples table:
```
| [S3 Bucket Replication](examples/workflows/s3-bucket-replication/) | Bidirectional cross-array S3 replication with shared credentials | remote_credentials, bucket_replica_link, array_connection, access_key, bucket |
```

Update the "Resource Coverage Roadmap" section: add a v2.0 table for the replication resources (remote_credentials and bucket_replica_link with full CRUD+import, array_connection as DS-only). Remove the "Bucket replica links" and "Array connections" entries from the v2+ Future table since they are now implemented.

Also update the v1.x Planned table — remove "Bucket replica links" row if present there.
  </action>
  <verify>
    <automated>ls docs/resources/object_store_remote_credentials.md docs/resources/bucket_replica_link.md docs/data-sources/array_connection.md && rg "Replication" README.md && rg "30 resources" README.md && echo "Docs regenerated and README updated"</automated>
  </verify>
  <done>tfplugindocs generated docs for all new resources without errors. README contains Replication category with correct resource/DS entries, updated total counts (30 resources, 24 data sources), replication workflow in workflow table, and v2.0 coverage table.</done>
</task>

</tasks>

<verification>
- All 6 example files exist and contain valid HCL referencing correct attribute names
- `go generate ./...` exits 0
- docs/ contains generated pages for remote_credentials, bucket_replica_link, array_connection
- README.md has Replication section with all 3 entries
- README.md total counts are accurate (30 resources, 24 data sources)
- Workflow example has dual provider aliases and bidirectional replica links
</verification>

<success_criteria>
- Complete copy-pasteable replication workflow in examples/workflows/s3-bucket-replication/
- HCL examples + import.sh for remote_credentials and bucket_replica_link
- Data source example for array_connection
- tfplugindocs generates cleanly with new resources
- README updated with replication category, workflow entry, and accurate counts
</success_criteria>

<output>
After completion, create `.planning/phases/16-workflow-and-documentation/16-01-SUMMARY.md`
</output>
