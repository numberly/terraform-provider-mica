---
phase: 21-dead-code-removal-modernization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/client/nfs_export_policies.go
  - internal/client/smb_share_policies.go
  - internal/client/smb_client_policies.go
  - internal/client/snapshot_policies.go
  - internal/client/s3_export_policies.go
  - internal/client/errors.go
  - internal/client/errors_test.go
  - internal/client/models_storage.go
  - internal/client/transport.go
  - internal/testmock/handlers/object_store_access_keys.go
  - internal/provider/array_dns_resource.go
  - internal/provider/array_ntp_resource.go
  - internal/provider/array_smtp_resource.go
  - internal/provider/bucket_replica_link_resource.go
  - internal/provider/bucket_resource.go
  - internal/provider/file_system_export_resource.go
  - internal/provider/filesystem_resource.go
  - internal/provider/network_access_policy_resource.go
  - internal/provider/network_access_policy_rule_resource.go
  - internal/provider/nfs_export_policy_resource.go
  - internal/provider/nfs_export_policy_rule_resource.go
  - internal/provider/object_store_access_key_resource.go
  - internal/provider/object_store_access_policy_resource.go
  - internal/provider/object_store_access_policy_rule_resource.go
  - internal/provider/object_store_account_export_resource.go
  - internal/provider/object_store_account_resource.go
  - internal/provider/object_store_virtual_host_resource.go
  - internal/provider/quota_group_resource.go
  - internal/provider/quota_user_resource.go
  - internal/provider/remote_credentials_resource.go
  - internal/provider/s3_export_policy_resource.go
  - internal/provider/s3_export_policy_rule_resource.go
  - internal/provider/server_resource.go
  - internal/provider/smb_client_policy_resource.go
  - internal/provider/smb_client_policy_rule_resource.go
  - internal/provider/smb_share_policy_resource.go
  - internal/provider/smb_share_policy_rule_resource.go
  - internal/provider/snapshot_policy_resource.go
  - internal/provider/snapshot_policy_rule_resource.go
  - internal/provider/syslog_server_resource.go
autonomous: true
requirements: [DCR-01, DCR-02, DCR-03, DCR-04, MOD-01]

must_haves:
  truths:
    - "5 unused List* policy functions and their Opts types are removed from client package"
    - "IsUnprocessable function and its tests are removed from errors.go"
    - "SourceReference type is replaced by NamedReference in FileSystem struct"
    - "30 empty UpgradeState implementations and their interface assertions are removed"
    - "math/rand is replaced with math/rand/v2 in transport.go and test mock"
    - "go build ./... compiles without errors"
    - "go test ./... passes without errors"
  artifacts:
    - path: "internal/client/nfs_export_policies.go"
      provides: "No ListNfsExportPolicies or ListNfsExportPoliciesOpts"
    - path: "internal/client/errors.go"
      provides: "No IsUnprocessable function"
    - path: "internal/client/models_storage.go"
      provides: "No SourceReference type, Source field uses *NamedReference"
    - path: "internal/client/transport.go"
      provides: "math/rand/v2 import instead of math/rand"
  key_links:
    - from: "internal/client/models_storage.go"
      to: "internal/client/models_common.go"
      via: "NamedReference type replaces SourceReference"
      pattern: "Source.*NamedReference"
    - from: "internal/client/transport.go"
      to: "math/rand/v2"
      via: "import alias"
      pattern: "math/rand/v2"
---

<objective>
Remove dead code and modernize stdlib usage across the codebase.

Purpose: Clean up unused code identified by the v2.0.1 audit -- 5 unused List* policy functions, the unused IsUnprocessable helper, the redundant SourceReference type, 30 empty UpgradeState stubs, and the deprecated math/rand import. This makes the codebase leaner and avoids go vet deprecation warnings.

Output: All dead code removed, math/rand/v2 in use, `go build ./...` and `go test ./...` still pass.
</objective>

<execution_context>
@/home/gule/.claude/get-shit-done/workflows/execute-plan.md
@/home/gule/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused List* functions, IsUnprocessable, and SourceReference</name>
  <files>
    internal/client/nfs_export_policies.go
    internal/client/smb_share_policies.go
    internal/client/smb_client_policies.go
    internal/client/snapshot_policies.go
    internal/client/s3_export_policies.go
    internal/client/errors.go
    internal/client/errors_test.go
    internal/client/models_storage.go
  </files>
  <action>
**DCR-01: Remove 5 unused List* functions and their Opts types.**

In each of these 5 files, remove the `List*Opts` struct and the `List*` function (but keep all other functions like Get*, Create*, Update*, Delete*, ListRules, ListMembers):

1. `internal/client/nfs_export_policies.go`: Remove `ListNfsExportPoliciesOpts` (lines 10-16) and `ListNfsExportPolicies` function (line 26+). Keep GetNfsExportPolicy, ListNfsExportPolicyRules, ListNfsExportPolicyMembers, and all CRUD functions.
2. `internal/client/smb_share_policies.go`: Remove `ListSmbSharePoliciesOpts` (lines 10-16) and `ListSmbSharePolicies` function (line 26+). Keep GetSmbSharePolicy, ListSmbSharePolicyRules, ListSmbSharePolicyMembers, and all CRUD functions.
3. `internal/client/smb_client_policies.go`: Remove `ListSmbClientPoliciesOpts` (lines 10-16) and `ListSmbClientPolicies` function (line 26+). Keep GetSmbClientPolicy, ListSmbClientPolicyRules, ListSmbClientPolicyMembers, and all CRUD functions.
4. `internal/client/snapshot_policies.go`: Remove `ListSnapshotPoliciesOpts` (lines 10-16) and `ListSnapshotPolicies` function (line 26+). Keep GetSnapshotPolicy, ListSnapshotPolicyMembers, and all CRUD functions.
5. `internal/client/s3_export_policies.go`: Remove `ListS3ExportPoliciesOpts` (lines 10-16) and `ListS3ExportPolicies` function (line 26+). Keep GetS3ExportPolicy, ListS3ExportPolicyRules, and all CRUD functions.

After removal, check if the `"strings"` import is still needed in each file (it was used by `strings.Join` in the List function's opts handling). Remove unused imports.

**DCR-02: Remove IsUnprocessable from errors.go.**

In `internal/client/errors.go`: Remove the `IsUnprocessable` function (lines 102-112). It is not called by any provider resource.

In `internal/client/errors_test.go`: Remove ALL test functions that reference IsUnprocessable:
- `TestUnit_IsUnprocessable_True` (line 43)
- `TestUnit_IsUnprocessable_False` (line 51)
- `TestUnit_IsUnprocessable_Nil` (line 59)
- `TestUnit_IsUnprocessable_WrappedError` (line 182)

**DCR-03: Replace SourceReference with NamedReference.**

In `internal/client/models_storage.go`:
1. Remove the `SourceReference` struct definition (lines 39-43) entirely.
2. Change the `Source` field in the `FileSystem` struct (line 61) from `*SourceReference` to `*NamedReference`. The JSON tag `json:"source,omitempty"` stays the same. Both types have identical fields (`ID string` and `Name string` with same json tags), so API serialization is unchanged.
3. Remove the `"encoding/json"` import if it becomes unused after this change (check first -- it is currently the only import and is NOT used by any code in the file, so remove it).

After all changes, verify: `go build ./...` compiles cleanly.
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && go build ./... && echo "BUILD OK" && rg 'ListNfsExportPoliciesOpts|ListSmbSharePoliciesOpts|ListSmbClientPoliciesOpts|ListSnapshotPoliciesOpts|ListS3ExportPoliciesOpts' internal/client/ && echo "CLEAN: no List*Opts remnants" && rg 'IsUnprocessable' internal/client/errors.go internal/client/errors_test.go && echo "CLEAN: no IsUnprocessable remnants" && rg 'SourceReference' internal/client/ && echo "CLEAN: no SourceReference remnants"</automated>
  </verify>
  <done>
    - 5 List* functions and their Opts types removed from the 5 policy client files
    - IsUnprocessable removed from errors.go and all 4 related tests removed from errors_test.go
    - SourceReference type removed, FileSystem.Source uses *NamedReference
    - `go build ./...` compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove empty UpgradeState implementations and update math/rand to v2</name>
  <files>
    internal/provider/array_dns_resource.go
    internal/provider/array_ntp_resource.go
    internal/provider/array_smtp_resource.go
    internal/provider/bucket_replica_link_resource.go
    internal/provider/bucket_resource.go
    internal/provider/file_system_export_resource.go
    internal/provider/filesystem_resource.go
    internal/provider/network_access_policy_resource.go
    internal/provider/network_access_policy_rule_resource.go
    internal/provider/nfs_export_policy_resource.go
    internal/provider/nfs_export_policy_rule_resource.go
    internal/provider/object_store_access_key_resource.go
    internal/provider/object_store_access_policy_resource.go
    internal/provider/object_store_access_policy_rule_resource.go
    internal/provider/object_store_account_export_resource.go
    internal/provider/object_store_account_resource.go
    internal/provider/object_store_virtual_host_resource.go
    internal/provider/quota_group_resource.go
    internal/provider/quota_user_resource.go
    internal/provider/remote_credentials_resource.go
    internal/provider/s3_export_policy_resource.go
    internal/provider/s3_export_policy_rule_resource.go
    internal/provider/server_resource.go
    internal/provider/smb_client_policy_resource.go
    internal/provider/smb_client_policy_rule_resource.go
    internal/provider/smb_share_policy_resource.go
    internal/provider/smb_share_policy_rule_resource.go
    internal/provider/snapshot_policy_resource.go
    internal/provider/snapshot_policy_rule_resource.go
    internal/provider/syslog_server_resource.go
    internal/client/transport.go
    internal/testmock/handlers/object_store_access_keys.go
  </files>
  <action>
**DCR-04: Remove 30 empty UpgradeState implementations.**

In ALL 30 resource files listed above, remove these two items:

1. The compile-time interface assertion line:
   ```go
   var _ resource.ResourceWithUpgradeState = &xxxResource{}
   ```

2. The empty `UpgradeState` method:
   ```go
   func (r *xxxResource) UpgradeState(_ context.Context) map[int64]resource.StateUpgrader {
       return map[int64]resource.StateUpgrader{}
   }
   ```

Also remove the `SchemaVersion` from the Schema() method in each file. Find the line `Version: 0,` inside the `Schema()` method return and remove it. The `Version` field defaults to 0 when omitted, but since we're removing UpgradeState entirely, keeping Version: 0 with no upgrader is misleading. Remove it for clarity.

After removal, check if the `"context"` import is still used in each file (it will be -- it's used by CRUD methods). Also check if the `resource` import from `github.com/hashicorp/terraform-plugin-framework/resource` is still used (it will be -- it's used by other interface assertions and method signatures). Do NOT remove imports that are still in use.

**MOD-01: Replace math/rand with math/rand/v2.**

In `internal/client/transport.go`:
1. Change import from `mathrand "math/rand"` to `mathrand "math/rand/v2"`
2. The usage `mathrand.Float64()` has the same signature in v2 -- no code changes needed beyond the import.

In `internal/testmock/handlers/object_store_access_keys.go`:
1. Change import from `"math/rand"` to `"math/rand/v2"`
2. Check usage of `rand.*` in this file -- the v2 API is compatible for common functions like `rand.Intn`, `rand.Int63`, etc. The v2 package removes `rand.Seed()` (not needed since Go 1.20+ auto-seeds). If `rand.New(rand.NewSource(...))` is used, replace with `rand.New(rand.NewPCG(seed1, seed2))` -- but check first, it likely just uses the global functions.

After all changes: `go build ./...` and `go test ./...` must pass.
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && go build ./... && go vet ./... && go test ./... -count=1 -timeout 120s 2>&1 | tail -20 && echo "ALL PASS" && test $(rg -l 'ResourceWithUpgradeState' internal/provider/*_resource.go 2>/dev/null | wc -l) -eq 0 && echo "CLEAN: no UpgradeState remnants" && rg 'math/rand"' internal/ --glob '*.go' && echo "CLEAN: no old math/rand imports"</automated>
  </verify>
  <done>
    - 30 empty UpgradeState methods, their interface assertions, and Version: 0 lines removed from all resource files
    - math/rand replaced with math/rand/v2 in transport.go and object_store_access_keys.go mock handler
    - `go build ./...` compiles, `go vet ./...` clean, `go test ./...` passes
  </done>
</task>

</tasks>

<verification>
1. `go build ./...` compiles without errors
2. `go vet ./...` shows no warnings (especially no math/rand deprecation)
3. `go test ./... -count=1` passes all tests
4. `rg 'ListNfsExportPoliciesOpts|ListSmbSharePoliciesOpts|ListSmbClientPoliciesOpts|ListSnapshotPoliciesOpts|ListS3ExportPoliciesOpts' internal/client/` -- no matches
5. `rg 'IsUnprocessable' internal/client/errors.go internal/client/errors_test.go` -- no matches
6. `rg 'SourceReference' internal/client/` -- no matches
7. `rg -l 'ResourceWithUpgradeState' internal/provider/*_resource.go` -- no matches
8. `rg 'math/rand"' internal/ --glob '*.go'` -- no matches (only math/rand/v2)
</verification>

<success_criteria>
- All 5 requirement IDs addressed: DCR-01 (List*), DCR-02 (IsUnprocessable), DCR-03 (SourceReference), DCR-04 (UpgradeState), MOD-01 (math/rand/v2)
- Zero compilation errors, zero test failures
- No dead code remnants found by grep verification
</success_criteria>

<output>
After completion, create `.planning/phases/21-dead-code-removal-modernization/21-01-SUMMARY.md`
</output>
