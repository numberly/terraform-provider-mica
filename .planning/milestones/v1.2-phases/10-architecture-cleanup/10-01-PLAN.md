---
phase: 10-architecture-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/client/models.go
  - internal/client/models_common.go
  - internal/client/models_storage.go
  - internal/client/models_policies.go
  - internal/client/models_exports.go
  - internal/client/models_admin.go
autonomous: true
requirements: [ARC-01]

must_haves:
  truths:
    - "models.go no longer exists (fully replaced by domain files)"
    - "go build ./... compiles without errors"
    - "go test ./... passes unchanged (zero test modifications)"
  artifacts:
    - path: "internal/client/models_common.go"
      provides: "Space, NamedReference, NumericIDReference, ListResponse, VersionResponse, PolicyMember"
      contains: "type Space struct"
    - path: "internal/client/models_storage.go"
      provides: "FileSystem, Bucket, ObjectStoreAccount, ObjectStoreAccessKey and their Post/Patch variants"
      contains: "type FileSystem struct"
    - path: "internal/client/models_policies.go"
      provides: "All NFS, SMB share, SMB client, Snapshot, OAP, NAP, S3 export policy, Quota model structs"
      contains: "type NfsExportPolicy struct"
    - path: "internal/client/models_exports.go"
      provides: "FileSystemExport, ObjectStoreAccountExport, ObjectStoreVirtualHost, Server and their Post/Patch variants"
      contains: "type FileSystemExport struct"
    - path: "internal/client/models_admin.go"
      provides: "ArrayDns, ArrayInfo, ArrayNtpPatch, SmtpServer, AlertWatcher, SyslogServer and their Post/Patch variants"
      contains: "type ArrayDns struct"
  key_links:
    - from: "internal/client/models_*.go"
      to: "internal/provider/*.go"
      via: "import client package — all types referenced as client.TypeName"
      pattern: "client\\.(FileSystem|Bucket|NfsExport)"
---

<objective>
Split the monolithic models.go (858 lines) into 5 domain-specific files within the same `client` package.

Purpose: Improve code navigability and reduce merge conflicts when adding or modifying model structs. Future phases (v1.3+) benefit from smaller, domain-scoped files.
Output: 5 new files replace 1 monolithic file. Zero logic changes, zero import changes for consumers.
</objective>

<execution_context>
@/home/gule/.claude/get-shit-done/workflows/execute-plan.md
@/home/gule/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@internal/client/models.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 5 domain files from models.go and delete the original</name>
  <files>internal/client/models_common.go, internal/client/models_storage.go, internal/client/models_policies.go, internal/client/models_exports.go, internal/client/models_admin.go, internal/client/models.go</files>
  <action>
Split internal/client/models.go into 5 files. All files use `package client`. Only models_common.go needs `import "encoding/json"` (for ListResponse generic). models_policies.go also needs `import "encoding/json"` (for ObjectStoreAccessPolicyRule.Conditions and ObjectStoreAccessPolicyRulePost/Patch).

**models_common.go** (shared types used across domains):
- Space (lines 6-13)
- ListResponse[T] (lines 97-101)
- VersionResponse (lines 104-106)
- NamedReference (lines 111-114)
- NumericIDReference (lines 571-574)
- PolicyMember (lines 202-206)

**models_storage.go** (file systems, object store accounts, buckets, access keys):
- NFSConfig, SMBConfig, HTTPConfig, DefaultQuotas, MultiProtocol, SourceReference (lines 16-53)
- FileSystem, FileSystemPost, FileSystemPatch (lines 56-94)
- ObjectStoreAccount, ObjectStoreAccountPost, ObjectStoreAccountPatch (lines 117-145) -- needs `import "encoding/json"` for json.RawMessage in ObjectStoreAccountPost
- Bucket, BucketPost, BucketPatch (lines 148-181)
- ObjectStoreAccessKey, ObjectStoreAccessKeyPost (lines 184-196)

**models_policies.go** (all policy families + quota):
- NfsExportPolicy, NfsExportPolicyPost, NfsExportPolicyPatch (lines 208-228)
- NfsExportPolicyRule, NfsExportPolicyRuleInPolicy, NfsExportPolicyRulePost, NfsExportPolicyRulePatch (lines 231-295)
- SmbSharePolicy, SmbSharePolicyPost, SmbSharePolicyPatch (lines 298-316)
- SmbSharePolicyRule, SmbSharePolicyRuleInPolicy, SmbSharePolicyRulePost, SmbSharePolicyRulePatch (lines 319-352)
- SmbClientPolicy, SmbClientPolicyPost, SmbClientPolicyPatch (lines 355-378)
- SmbClientPolicyRule, SmbClientPolicyRuleInPolicy, SmbClientPolicyRulePost, SmbClientPolicyRulePatch (lines 381-414)
- SnapshotPolicy, SnapshotPolicyPost, SnapshotPolicyPatch (lines 417-439)
- SnapshotPolicyRuleInPolicy, SnapshotPolicyRulePost, SnapshotPolicyRuleRemove (lines 442-468)
- ObjectStoreAccessPolicy, ObjectStoreAccessPolicyPost, ObjectStoreAccessPolicyPatch (lines 473-497)
- ObjectStoreAccessPolicyRule, ObjectStoreAccessPolicyRulePost, ObjectStoreAccessPolicyRulePatch (lines 500-523) -- needs `import "encoding/json"` for json.RawMessage
- NetworkAccessPolicy, NetworkAccessPolicyPatch (lines 526-540)
- NetworkAccessPolicyRule, NetworkAccessPolicyRulePost, NetworkAccessPolicyRulePatch (lines 543-568)
- QuotaUser, QuotaUserPost, QuotaUserPatch (lines 577-593)
- QuotaGroup, QuotaGroupPost, QuotaGroupPatch (lines 596-612)
- S3ExportPolicy, S3ExportPolicyPost, S3ExportPolicyPatch (lines 771-789)
- S3ExportPolicyRule, S3ExportPolicyRulePost, S3ExportPolicyRulePatch (lines 792-814)

**models_exports.go** (server, file system exports, account exports, virtual hosts):
- ServerDNS, Server, ServerPost, ServerPatch (lines 691-714)
- FileSystemExport, FileSystemExportPost, FileSystemExportPatch (lines 717-743)
- ObjectStoreAccountExport, ObjectStoreAccountExportPost, ObjectStoreAccountExportPatch (lines 746-766)
- ObjectStoreVirtualHost, ObjectStoreVirtualHostPost, ObjectStoreVirtualHostPatch (lines 817-835)

**models_admin.go** (array-level configuration):
- ArrayDns, ArrayDnsPost, ArrayDnsPatch (lines 615-638)
- ArrayInfo, ArrayNtpPatch (lines 641-651)
- SmtpServer, SmtpServerPatch (lines 654-667)
- AlertWatcher, AlertWatcherPost, AlertWatcherPatch (lines 671-688)
- SyslogServer, SyslogServerPost, SyslogServerPatch (lines 838-858)

After creating all 5 files, DELETE internal/client/models.go entirely. Preserve every comment and doc string verbatim. Do NOT change any struct field, tag, or type.
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && go build ./... && go vet ./...</automated>
  </verify>
  <done>models.go is gone. 5 domain files exist. `go build ./...` and `go vet ./...` pass.</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite to confirm zero regressions</name>
  <files></files>
  <action>
Run `go test ./...` to confirm all existing tests pass without modification. No test files should need changes since all types remain in the same `client` package with the same exported names. If any test fails, investigate and fix the split (likely a missing import or misplaced type).
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && go test ./... 2>&1 | tail -30</automated>
  </verify>
  <done>All tests pass. Zero test modifications required.</done>
</task>

</tasks>

<verification>
1. `ls internal/client/models.go` returns "No such file"
2. `ls internal/client/models_*.go` returns 5 files
3. `go build ./...` exits 0
4. `go test ./...` exits 0
5. `wc -l internal/client/models_*.go` total matches original 858 lines (minus/plus package declarations)
</verification>

<success_criteria>
- models.go replaced by 5 domain files
- All types remain exported with identical names and signatures
- go build, go vet, and go test all pass with zero changes to any file outside internal/client/
</success_criteria>

<output>
After completion, create `.planning/phases/10-architecture-cleanup/10-01-SUMMARY.md`
</output>
