---
phase: 11-test-hardening-and-validators
plan: 03
type: execute
wave: 2
depends_on: ["11-02"]
files_modified:
  - internal/provider/smb_client_policy_resource_test.go
  - internal/provider/syslog_server_resource_test.go
  - internal/provider/s3_export_policy_resource_test.go
  - internal/provider/server_resource_test.go
  - internal/provider/object_store_virtual_host_resource_test.go
  - internal/provider/s3_export_policy_rule_resource_test.go
  - internal/provider/smb_client_policy_rule_resource_test.go
  - internal/provider/file_system_export_resource_test.go
  - internal/provider/object_store_account_export_resource_test.go
autonomous: true
requirements:
  - TST-01
  - TST-03

must_haves:
  truths:
    - "Create -> Read -> compare shows 0 attribute drift for each resource family"
    - "SMB client policy Update test verifies enabled toggle and rename"
    - "Syslog server Update test verifies URI change"
    - "S3 export policy Update test verifies enabled toggle"
    - "Server Update test already exists — this plan adds idempotence only"
  artifacts:
    - path: "internal/provider/smb_client_policy_resource_test.go"
      provides: "Idempotence test + dedicated Update test"
      contains: "Idempoten"
    - path: "internal/provider/syslog_server_resource_test.go"
      provides: "Idempotence test + dedicated Update test"
      contains: "Idempoten"
    - path: "internal/provider/s3_export_policy_resource_test.go"
      provides: "Idempotence test"
      contains: "Idempoten"
    - path: "internal/provider/s3_export_policy_rule_resource_test.go"
      provides: "Idempotence test"
      contains: "Idempoten"
    - path: "internal/provider/server_resource_test.go"
      provides: "Idempotence test"
      contains: "Idempoten"
    - path: "internal/provider/object_store_virtual_host_resource_test.go"
      provides: "Idempotence test"
      contains: "Idempoten"
    - path: "internal/provider/smb_client_policy_rule_resource_test.go"
      provides: "Idempotence test"
      contains: "Idempoten"
    - path: "internal/provider/file_system_export_resource_test.go"
      provides: "Idempotence test"
      contains: "Idempoten"
    - path: "internal/provider/object_store_account_export_resource_test.go"
      provides: "Idempotence test"
      contains: "Idempoten"
  key_links:
    - from: "internal/provider/*_resource_test.go"
      to: "internal/testmock/handlers/*.go"
      via: "mock server registration in each test"
      pattern: "handlers\\.Register"
---

<objective>
Add idempotence tests for all v1.1 resource families and standalone Update lifecycle tests for resources that only had combined CRUD tests.

Purpose: Idempotence tests catch drift bugs (like the writable field issue in Phase 9) before release. Standalone Update tests exercise specific field-change paths that CRUD tests only cover superficially.
Output: 9 idempotence tests + 3 standalone Update tests across v1.1 resource test files.
</objective>

<execution_context>
@/home/gule/.claude/get-shit-done/workflows/execute-plan.md
@/home/gule/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@internal/provider/filesystem_resource_test.go (reference: TestUnit_FileSystem_Idempotent at line 730 — the pattern to follow for idempotence tests)
@internal/provider/bucket_resource_test.go (reference: TestUnit_Bucket_Update at line 206 — the pattern to follow for standalone Update tests)

<interfaces>
Idempotence test pattern (from filesystem_resource_test.go):
1. Create mock server, register handlers
2. Create resource via r.Create()
3. Read back via r.Read() using createResp.State
4. Extract before/after models from createResp.State and readResp.State
5. Compare key fields — assert no attribute changed

Standalone Update test pattern (from bucket_resource_test.go):
1. Create mock server, register handlers
2. Create resource via r.Create()
3. Build updatePlan with changed field
4. Call r.Update() with Plan=updatePlan, State=createResp.State
5. Assert updated field has new value

Both patterns already exist in all v1.0 test files. v1.1 resources use the same test helpers (newTestXxxResource, buildXxxType, etc.) but lack these specific test functions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add idempotence tests for all v1.1 resources</name>
  <files>internal/provider/smb_client_policy_resource_test.go, internal/provider/smb_client_policy_rule_resource_test.go, internal/provider/syslog_server_resource_test.go, internal/provider/s3_export_policy_resource_test.go, internal/provider/s3_export_policy_rule_resource_test.go, internal/provider/object_store_virtual_host_resource_test.go, internal/provider/server_resource_test.go, internal/provider/file_system_export_resource_test.go, internal/provider/object_store_account_export_resource_test.go</files>
  <action>
Add one `TestUnit_Xxx_Idempotent` function to each of the 9 test files listed. Follow the EXACT pattern from `TestUnit_FileSystem_Idempotent` (line 730 of filesystem_resource_test.go):

1. Create mock server, register the resource's handlers
2. Create the resource using the existing test helper (e.g., `smbClientPolicyPlanWithNameAndEnabled`)
3. Call r.Read() with the post-create state
4. Extract before model (from createResp.State) and after model (from readResp.State)
5. Compare all user-visible attributes — any difference is a drift bug

**For each resource, compare at minimum these fields:**

- **smb_client_policy**: ID, Name, Enabled, Version
- **smb_client_policy_rule**: ID, Name, Client, Encryption, Permission
- **syslog_server**: ID, Name, URI
- **s3_export_policy**: ID, Name, Enabled
- **s3_export_policy_rule**: ID, Name, PolicyName, Effect, Index
- **virtual_host**: ID, Name, Hostname
- **server**: ID, Name (server already has Update test — only needs idempotence)
- **file_system_export**: ID, Name, FilesystemName, Enabled
- **account_export**: ID, Name, AccountName, Enabled

Read each test file first to find the existing helper functions (newTestXxxResource, buildXxxType, xxxPlanWithName, etc.) and reuse them.

IMPORTANT: Append the new test function at the END of each file, before any closing comments. Do not modify existing test functions.
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && go test ./internal/provider/ -run "TestUnit.*Idempotent" -v -count=1 -timeout 120s</automated>
  </verify>
  <done>9 idempotence tests pass. Each verifies that Create followed by Read produces no attribute drift.</done>
</task>

<task type="auto">
  <name>Task 2: Add standalone Update lifecycle tests for SMB client policy, syslog server, and S3 export policy</name>
  <files>internal/provider/smb_client_policy_resource_test.go, internal/provider/syslog_server_resource_test.go, internal/provider/s3_export_policy_resource_test.go</files>
  <action>
Add dedicated `TestUnit_Xxx_Update` functions to 3 resource test files. These are separate from the combined CRUD tests and exercise specific update scenarios.

Follow the pattern from `TestUnit_Bucket_Update` (line 206 of bucket_resource_test.go): Create, then build an update plan with one changed field, call Update, verify the changed field.

**smb_client_policy_resource_test.go — TestUnit_SmbClientPolicy_Update:**
- Create policy with enabled=true
- Update to enabled=false
- Assert enabled is false in the response state
- Assert name is unchanged

**syslog_server_resource_test.go — TestUnit_SyslogServer_Update:**
- Create syslog server with URI "tcp://syslog1.example.com:514"
- Update URI to "tcp://syslog2.example.com:514"
- Assert URI changed in response state
- Assert name is unchanged

**s3_export_policy_resource_test.go — TestUnit_S3ExportPolicy_Update:**
- Create S3 export policy with enabled=true
- Update to enabled=false
- Assert enabled is false in response state
- Assert name is unchanged

Read each test file first to identify the existing helper functions and model types. Reuse them. The CRUD tests already exercise Update but only as part of a longer sequence — these standalone tests make the Update path explicitly testable and debuggable.

NOTE: Virtual host already has TestUnit_ObjectStoreVirtualHost_UpdateHostname (line 270), so no new Update test needed there. Server already has TestUnit_Server_Update (line 219).

Append new test functions at the end of each file.
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && go test ./internal/provider/ -run "TestUnit_SmbClientPolicy_Update|TestUnit_SyslogServer_Update|TestUnit_S3ExportPolicy_Update" -v -count=1</automated>
  </verify>
  <done>3 standalone Update tests pass. Each verifies a specific mutable field change via the Update method.</done>
</task>

</tasks>

<verification>
```bash
cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade
go test ./internal/provider/ -run "TestUnit.*Idempotent|TestUnit_SmbClientPolicy_Update|TestUnit_SyslogServer_Update|TestUnit_S3ExportPolicy_Update" -v -count=1 -timeout 120s
# Also run full suite to ensure no regressions:
go test ./internal/... -count=1 -timeout 120s
```
</verification>

<success_criteria>
- 9 idempotence tests exist and pass (one per v1.1 resource)
- 3 standalone Update tests exist and pass
- Full test suite passes with no regressions
- No existing test functions modified
</success_criteria>

<output>
After completion, create `.planning/phases/11-test-hardening-and-validators/11-03-SUMMARY.md`
</output>
