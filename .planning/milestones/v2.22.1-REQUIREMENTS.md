# Milestone v2.22.1 Requirements — Directory Service – Array Management

**Status:** ✅ ALL SATISFIED
**Archived:** 2026-04-17
**Source of truth:** `.planning/phases/49-directory-service-management/49-VERIFICATION.md` (Requirements Coverage table)

> **Note on provenance.** v2.22.1 was a single-phase retrofit milestone; the 17 requirements were captured per-phase in Phase 49's plans and are authoritatively tracked in `49-VERIFICATION.md`. `.planning/REQUIREMENTS.md` had already been rewritten for v2.22.2 before this milestone was closed, so that file is NOT the source for v2.22.1 requirements. This archive mirrors the Phase 49 VERIFICATION.md table.

## Traceability

| Req ID  | Description | Plan(s) | Status | Evidence |
|---------|-------------|---------|--------|----------|
| DSM-01  | Configure `uris`, `base_dn`, `bind_user` via `flashblade_directory_service_management` | 49-01, 49-03 | ✓ Complete | Resource schema has all 3 fields; `DirectoryServicePatch` sends them via PATCH |
| DSM-02  | `bind_password` sensitive write-only, never returned nor in plan diffs | 49-01, 49-03 | ✓ Complete | `Sensitive=true` + `UseStateForUnknown`; `mapDirectoryServiceToModel` never touches `BindPassword` |
| DSM-03  | `ca_certificate` + `ca_certificate_group` by `NamedReference` name; clearable by omission | 49-01, 49-03 | ✓ Complete | `**NamedReference` in `DirectoryServicePatch`: nil-outer=omit, non-nil-outer+nil-inner=clear |
| DSM-04  | `user_login_attribute`, `user_object_class`, `ssh_public_key_attribute` in management sub-object | 49-01, 49-03 | ✓ Complete | `DirectoryServiceManagementPatch` struct; mapped in schema; tested in lifecycle |
| DSM-05  | `enabled` boolean attribute | 49-01, 49-03 | ✓ Complete | `BoolAttribute` in schema; `DirectoryServicePatch.Enabled *bool`; drift detected |
| DSM-06  | Import by literal `management`; `nullTimeoutsValue()`; `bind_password` empty | 49-03 | ✓ Complete | ImportState: `nullTimeoutsValue()` + `types.StringValue("")` for `bind_password`; Import test passes |
| DSM-07  | Drift detection on ≥ 10 fields via `tflog.Debug {resource, field, was, now}` | 49-03 | ✓ Complete | 10 drift calls: `enabled`, `uris`, `base_dn`, `bind_user`, `ca_certificate`, `ca_certificate_group`, `user_login_attribute`, `user_object_class`, `ssh_public_key_attribute`, `services` |
| DSM-08  | Read-only data source `flashblade_directory_service_management` | 49-04 | ✓ Complete | `directoryServiceManagementDataSource` with computed-only schema; Basic test passes |
| DOC-01  | `resource.tf` + `import.sh` with canonical `terraform import … management` | 49-05 | ✓ Complete | Both files present; exact import string verified |
| DOC-02  | `data-source.tf` with singleton (no args) data block | 49-05 | ✓ Complete | `data "flashblade_directory_service_management" "current" {}` zero args |
| DOC-03  | `make docs` regenerates both docs; no manual edits | 49-05 | ✓ Complete | Both docs present; `make docs` idempotent (no diff after re-run) |
| QA-01   | ≥ 4 `TestUnit_DirectoryServiceManagement_*` client tests | 49-01 | ✓ Complete | 4 tests: Get_Found, Get_NotFound, Patch_Uris, Patch_CACertificateGroup |
| QA-02   | ≥ 3 `TestUnit_DirectoryServiceManagementResource_*` tests | 49-03 | ✓ Complete | Lifecycle, Import, DriftDetection |
| QA-03   | ≥ 1 `TestUnit_DirectoryServiceManagementDataSource_Basic` | 49-04 | ✓ Complete | 1 test, 9-field assertions |
| QA-04   | `make test` ≥ 787 | 49-05 | ✓ Complete | 798 passing (current) vs 787 threshold |
| QA-05   | `make lint` clean; all 4 interface assertions; empty `UpgradeState` at Version 0 | 49-03, 49-05 | ✓ Complete | 0 lint issues; assertions at lines 21-24; `UpgradeState` returns `map[int64]resource.StateUpgrader{}` |
| QA-06   | `ROADMAP.md` row moved to Implemented/Array Administration; counters refreshed; `CONVENTIONS.md` baseline updated | 49-05 | ✓ Complete | Row present; Last updated 2026-04-17; version v2.22.1; old Candidate row removed; baseline 798 |

**Totals:** 17/17 satisfied · 0 partial · 0 unsatisfied · 0 orphaned.

## Notes

- Requirement IDs used three prefixes: `DSM-` (Directory Service Management, 8 functional), `DOC-` (documentation, 3), `QA-` (quality gates, 6).
- All requirements were validated programmatically (unit tests + lint + docs regen). No live-array acceptance testing was required to close the milestone; a real LDAP round-trip is documented as optional human verification in `49-VERIFICATION.md`.
- No requirement was dropped, adjusted, or deferred.

---

_Archived from Phase 49 VERIFICATION.md on 2026-04-17 during `/gsd:complete-milestone 2.22.1`._
