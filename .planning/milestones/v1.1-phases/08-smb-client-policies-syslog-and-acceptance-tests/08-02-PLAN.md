---
phase: 08-smb-client-policies-syslog-and-acceptance-tests
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - internal/client/models.go
  - internal/client/syslog_servers.go
  - internal/testmock/handlers/syslog_servers.go
  - internal/provider/syslog_server_resource.go
  - internal/provider/syslog_server_data_source.go
  - internal/provider/syslog_server_resource_test.go
  - internal/provider/provider.go
autonomous: true
requirements:
  - SYS-01
  - SYS-02
  - SYS-03

must_haves:
  truths:
    - "Operator can create a syslog server with URI, services, and sources"
    - "Operator can update syslog server URI, services, and sources in-place"
    - "Operator can import an existing syslog server into Terraform state with no diff"
    - "Syslog server data source reads server by name"
    - "Null-vs-empty drift does not occur on services and sources list attributes"
  artifacts:
    - path: "internal/client/models.go"
      provides: "SyslogServer, SyslogServerPost, SyslogServerPatch structs"
      contains: "type SyslogServer struct"
    - path: "internal/client/syslog_servers.go"
      provides: "Client CRUD methods for syslog servers"
      exports: ["GetSyslogServer", "ListSyslogServers", "PostSyslogServer", "PatchSyslogServer", "DeleteSyslogServer"]
    - path: "internal/testmock/handlers/syslog_servers.go"
      provides: "Mock handler for syslog servers"
      exports: ["RegisterSyslogServerHandlers"]
    - path: "internal/provider/syslog_server_resource.go"
      provides: "flashblade_syslog_server resource"
      exports: ["NewSyslogServerResource"]
    - path: "internal/provider/syslog_server_data_source.go"
      provides: "flashblade_syslog_server data source"
      exports: ["NewSyslogServerDataSource"]
    - path: "internal/provider/syslog_server_resource_test.go"
      provides: "Unit tests for syslog server lifecycle and import"
  key_links:
    - from: "internal/provider/syslog_server_resource.go"
      to: "internal/client/syslog_servers.go"
      via: "client.PostSyslogServer, GetSyslogServer, PatchSyslogServer, DeleteSyslogServer"
      pattern: "r\\.client\\.(Post|Get|Patch|Delete)SyslogServer"
    - from: "internal/provider/syslog_server_resource.go"
      to: "services/sources list attributes"
      via: "listdefault.StaticValue for null-vs-empty"
      pattern: "listdefault\\.StaticValue"
    - from: "internal/provider/provider.go"
      to: "internal/provider/syslog_server_resource.go"
      via: "NewSyslogServerResource registration"
      pattern: "NewSyslogServerResource"
---

<objective>
Implement the syslog server resource: resource (CRUD + import), data source, client methods, mock handler, and unit tests.

Purpose: Syslog servers receive audit and management logs from the FlashBlade. This is a standard named resource (not singleton) with simple flat schema — URI, services list, sources list.

Output: 7 files — models appended to models.go, client CRUD, mock handler, resource, data source, provider registration update, and test file.
</objective>

<execution_context>
@/home/gule/.claude/get-shit-done/workflows/execute-plan.md
@/home/gule/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-smb-client-policies-syslog-and-acceptance-tests/08-01-SUMMARY.md

@internal/provider/smb_share_policy_resource.go
@internal/provider/server_resource.go
@internal/client/models.go
@internal/provider/provider.go

<interfaces>
<!-- Syslog server is a flat named resource. Follow server_resource.go for list attribute handling. -->

From 08-RESEARCH.md — syslog server model:
```go
type SyslogServer struct {
    ID       string   `json:"id,omitempty"`
    Name     string   `json:"name,omitempty"`
    URI      string   `json:"uri,omitempty"`
    Services []string `json:"services,omitempty"`
    Sources  []string `json:"sources,omitempty"`
}
type SyslogServerPost struct {
    URI      string   `json:"uri,omitempty"`
    Services []string `json:"services,omitempty"`
    Sources  []string `json:"sources,omitempty"`
}
type SyslogServerPatch struct {
    URI      *string   `json:"uri,omitempty"`
    Services *[]string `json:"services,omitempty"`
    Sources  *[]string `json:"sources,omitempty"`
}
```

API endpoints:
- POST /syslog-servers?names={name}
- GET /syslog-servers?names={name}
- PATCH /syslog-servers?names={name}
- DELETE /syslog-servers?names={name}

Key patterns from existing code:
- `listdefault.StaticValue(types.ListValueMust(types.StringType, []attr.Value{}))` for null-vs-empty (Phase 7 virtual host pattern)
- Name is RequiresReplace (syslog server name is not renameable)
- Services valid values: "data-audit", "management"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Syslog server models, client CRUD, and mock handler</name>
  <files>internal/client/models.go, internal/client/syslog_servers.go, internal/testmock/handlers/syslog_servers.go</files>
  <action>
1. **Add model structs to `internal/client/models.go`** (append after the SMB client policy structs added by Plan 01):

   - `SyslogServer` struct: `ID` (string), `Name` (string), `URI` (string), `Services` ([]string), `Sources` ([]string)
   - `SyslogServerPost`: `URI` (string), `Services` ([]string), `Sources` ([]string)
   - `SyslogServerPatch`: `URI` (*string), `Services` (*[]string), `Sources` (*[]string)

2. **Create `internal/client/syslog_servers.go`**:
   - `ListSyslogServersOpts` struct with `Names []string` and `Filter string`
   - `GetSyslogServer(ctx, name)` — GET /syslog-servers?names=, synthesize 404 if empty items
   - `ListSyslogServers(ctx, opts)` — paginated GET /syslog-servers with names/filter params
   - `PostSyslogServer(ctx, name, body)` — POST /syslog-servers?names=
   - `PatchSyslogServer(ctx, name, body)` — PATCH /syslog-servers?names=
   - `DeleteSyslogServer(ctx, name)` — DELETE /syslog-servers?names=
   - Mirror the exact pattern from `smb_share_policies.go` or `smb_client_policies.go` (just created).

3. **Create `internal/testmock/handlers/syslog_servers.go`**:
   - `syslogServerStore` with `servers map[string]*client.SyslogServer` and `sync.Mutex`
   - `RegisterSyslogServerHandlers(mux)` registering `/api/2.22/syslog-servers`
   - GET: filter by `?names=`, return all if no filter
   - POST: require `?names=`, create with UUID id, store services/sources (default to empty slice if nil)
   - PATCH: `rawPatch map[string]json.RawMessage` for true PATCH semantics, handle `uri`, `services`, `sources`
   - DELETE: standard delete by name, 404 if not found
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && go build ./...</automated>
  </verify>
  <done>All 3 files compile. Syslog models in models.go, client has 5 methods, mock handler registers 1 route with full CRUD.</done>
</task>

<task type="auto">
  <name>Task 2: Syslog server resource, data source, provider registration, and unit tests</name>
  <files>internal/provider/syslog_server_resource.go, internal/provider/syslog_server_data_source.go, internal/provider/syslog_server_resource_test.go, internal/provider/provider.go</files>
  <action>
1. **Create `internal/provider/syslog_server_resource.go`**:
   - `syslogServerModel`: `ID` (types.String), `Name` (types.String), `URI` (types.String), `Services` (types.List of StringType), `Sources` (types.List of StringType), `Timeouts`
   - Schema attributes:
     - `id`: Computed, UseStateForUnknown
     - `name`: Required, RequiresReplace (not renameable per research)
     - `uri`: Required. Description: "Syslog server URI in format PROTOCOL://HOST:PORT (e.g. udp://syslog.example.com:514)"
     - `services`: Optional, Computed, ListAttribute of StringType. Use `listdefault.StaticValue(types.ListValueMust(types.StringType, []attr.Value{}))` to prevent null-vs-empty drift (Pitfall 5).
     - `sources`: Optional, Computed, ListAttribute of StringType. Same `listdefault.StaticValue` treatment.
     - `timeouts`: standard CRUD timeouts
   - Create: extract URI string, services/sources as []string from types.List, build `SyslogServerPost`, call `PostSyslogServer(ctx, name, body)`, readIntoState
   - Read: call `GetSyslogServer`, map to model. Drift detection on `uri`.
   - Update: compare plan vs state for `uri`, `services`, `sources`. Build `SyslogServerPatch` with pointer fields. For list comparison, convert types.List to []string and compare. Call `PatchSyslogServer(ctx, name, patch)`, readIntoState.
   - Delete: call `DeleteSyslogServer`. Handle 404 gracefully.
   - ImportState: initialize null timeouts, set Name, readIntoState.
   - `mapSyslogServerToModel`: map ID, Name, URI as types.StringValue. Map Services and Sources as `types.ListValueMust(types.StringType, ...)` — convert []string to []attr.Value. Handle nil slices by mapping to empty list (not null).
   - Helper: `stringSliceFromList(list types.List) []string` to extract Go string slice from types.List.

2. **Create `internal/provider/syslog_server_data_source.go`**:
   - Model: `ID`, `Name`, `URI`, `Services`, `Sources` (all Computed except Name Required)
   - Read: call `GetSyslogServer`, map to model

3. **Register in `internal/provider/provider.go`**:
   - Add `NewSyslogServerResource` to Resources()
   - Add `NewSyslogServerDataSource` to DataSources()

4. **Create `internal/provider/syslog_server_resource_test.go`**:
   - `TestUnit_SyslogServer_CRUD`: Create syslog server with name="test-syslog", uri="udp://syslog.example.com:514", services=["data-audit"]. Update uri to "tcp://syslog.example.com:6514" and services to ["data-audit","management"]. Delete. Verify 0-diff after each apply.
   - `TestUnit_SyslogServer_Import`: Create then import by name, verify 0-diff plan.
   - `TestUnit_SyslogServer_DataSource`: Create server then read via data source.
   - Register mock handler via `handlers.RegisterSyslogServerHandlers(mux)` in test setup.
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && go test ./internal/provider/ -run "TestUnit_SyslogServer" -count=1 -v -timeout 2m</automated>
  </verify>
  <done>All syslog server unit tests pass. Resource and data source registered in provider. CRUD lifecycle, import, and data source work. No null-vs-empty drift on services/sources.</done>
</task>

</tasks>

<verification>
```bash
# Full build
cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && go build ./...

# Syslog server tests
go test ./internal/provider/ -run "TestUnit_SyslogServer" -count=1 -v -timeout 2m

# Full test suite (no regressions)
go test ./internal/... -count=1 -timeout 5m
```
</verification>

<success_criteria>
- All 7 files created/updated and compile
- Syslog server CRUD lifecycle test passes (create with URI/services/sources, update URI+services, delete)
- Syslog server import test passes (import by name, 0-diff plan)
- Data source test passes (read server by name)
- No null-vs-empty drift on services and sources (listdefault.StaticValue applied)
- Full test suite shows no regressions
- Provider registers NewSyslogServerResource and NewSyslogServerDataSource
</success_criteria>

<output>
After completion, create `.planning/phases/08-smb-client-policies-syslog-and-acceptance-tests/08-02-SUMMARY.md`
</output>
