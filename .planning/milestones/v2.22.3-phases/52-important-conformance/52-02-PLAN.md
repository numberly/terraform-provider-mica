---
phase: 52-important-conformance
plan: 02
type: execute
wave: 2
depends_on: ["52-01"]
files_modified:
  - internal/provider/bucket_access_policy_resource.go
  - internal/provider/bucket_access_policy_rule_resource.go
  - internal/provider/bucket_audit_filter_resource.go
  - internal/provider/network_interface_resource.go
  - internal/provider/object_store_user_policy_resource.go
  - internal/provider/qos_policy_member_resource.go
  - internal/provider/tls_policy_member_resource.go
autonomous: true
requirements: [R-007]
must_haves:
  truths:
    - "Every resource in internal/provider/*_resource.go declares all four mandatory interface assertions (Resource, ResourceWithConfigure, ResourceWithImportState, ResourceWithUpgradeState) — except documented exceptions."
    - "Each listed resource has a no-op UpgradeState method returning an empty map."
    - "No resource has its SchemaVersion bumped in this plan (assertion-only compliance)."
  artifacts:
    - path: "internal/provider/bucket_access_policy_resource.go"
      provides: "4th interface assertion + no-op UpgradeState"
      contains: "ResourceWithUpgradeState"
    - path: "internal/provider/bucket_access_policy_rule_resource.go"
      provides: "4th interface assertion + no-op UpgradeState"
      contains: "ResourceWithUpgradeState"
    - path: "internal/provider/bucket_audit_filter_resource.go"
      provides: "4th interface assertion + no-op UpgradeState"
      contains: "ResourceWithUpgradeState"
    - path: "internal/provider/network_interface_resource.go"
      provides: "4th interface assertion + no-op UpgradeState"
      contains: "ResourceWithUpgradeState"
    - path: "internal/provider/object_store_user_policy_resource.go"
      provides: "4th interface assertion + no-op UpgradeState"
      contains: "ResourceWithUpgradeState"
    - path: "internal/provider/qos_policy_member_resource.go"
      provides: "4th interface assertion + no-op UpgradeState"
      contains: "ResourceWithUpgradeState"
    - path: "internal/provider/tls_policy_member_resource.go"
      provides: "4th interface assertion + no-op UpgradeState"
      contains: "ResourceWithUpgradeState"
  key_links:
    - from: "each listed resource file"
      to: "terraform-plugin-framework ResourceWithUpgradeState interface"
      via: "compile-time interface assertion"
      pattern: "var _ resource\\.ResourceWithUpgradeState"
---

<objective>
Fix R-007: 9 resources were flagged as missing the `ResourceWithUpgradeState` assertion. Verification against the current tree shows:
- `subnet_resource.go` — **already has it** after Phase 51 (verified: line 26). SKIP.
- `qos_policy_resource.go` — handled by plan **52-01** (gets assertion + v0→v1 upgrader). SKIP here to avoid double-edit.
- Remaining **7** resources: add the 4th assertion + a no-op `UpgradeState` method returning `map[int64]resource.StateUpgrader{}`.

Purpose: Bring every regular resource into full convention compliance (all 4 interfaces) without artificial schema bumps.
Output: 7 edited files, each gaining one assertion line and one method.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-important-conformance/52-CONTEXT.md
@CONVENTIONS.md
@internal/provider/bucket_access_policy_resource.go
@internal/provider/bucket_access_policy_rule_resource.go
@internal/provider/bucket_audit_filter_resource.go
@internal/provider/network_interface_resource.go
@internal/provider/object_store_user_policy_resource.go
@internal/provider/qos_policy_member_resource.go
@internal/provider/tls_policy_member_resource.go

<interfaces>
Canonical empty-upgrader pattern (from `internal/provider/object_store_access_key_resource.go:127–130` and `nfs_export_policy_rule_resource.go:173–176`):

```go
// UpgradeState returns state upgraders for schema migrations.
func (r *xxxResource) UpgradeState(_ context.Context) map[int64]resource.StateUpgrader {
    return map[int64]resource.StateUpgrader{}
}
```

Assertion placement (below existing `var _ resource.ResourceWithImportState = ...` line):
```go
var _ resource.ResourceWithUpgradeState = &xxxResource{}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify subnet_resource.go is already compliant; re-confirm scope is 7 files</name>
  <files>(no write) internal/provider/subnet_resource.go, internal/provider/qos_policy_resource.go</files>
  <action>
Pre-flight check (informational, no edits):

```bash
rg -n 'ResourceWithUpgradeState' internal/provider/subnet_resource.go internal/provider/qos_policy_resource.go
```

Expected:
- `subnet_resource.go` line 26 shows the assertion — SKIP (handled in Phase 51).
- `qos_policy_resource.go` — assertion is added by plan 52-01 in the same phase. SKIP here.

If either check fails to match expectation, STOP and flag to the user before proceeding.
  </action>
  <verify>
    <automated>rg -l 'ResourceWithUpgradeState = &(subnetResource|qosPolicyResource)\{\}' internal/provider/ | wc -l</automated>
  </verify>
  <done>
    Output is `2` (both files already carry the assertion). Proceed to Task 2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ResourceWithUpgradeState assertion + no-op UpgradeState method to the 7 remaining resources</name>
  <files>internal/provider/bucket_access_policy_resource.go, internal/provider/bucket_access_policy_rule_resource.go, internal/provider/bucket_audit_filter_resource.go, internal/provider/network_interface_resource.go, internal/provider/object_store_user_policy_resource.go, internal/provider/qos_policy_member_resource.go, internal/provider/tls_policy_member_resource.go</files>
  <behavior>
    - Each of the 7 files compiles after the edit.
    - `var _ resource.ResourceWithUpgradeState = &<resourceType>{}` is present on every file (one line, just below the existing `ResourceWithImportState` assertion — or wherever the block of assertions lives).
    - Each file defines a `UpgradeState(_ context.Context) map[int64]resource.StateUpgrader` method returning `map[int64]resource.StateUpgrader{}`.
    - No `Schema.Version` is changed in this plan.
  </behavior>
  <action>
For each of the 7 files, apply the same two-edit pattern. Concrete receivers per file:

| File | Receiver type |
|---|---|
| `bucket_access_policy_resource.go` | `*bucketAccessPolicyResource` |
| `bucket_access_policy_rule_resource.go` | `*bucketAccessPolicyRuleResource` |
| `bucket_audit_filter_resource.go` | `*bucketAuditFilterResource` |
| `network_interface_resource.go` | `*networkInterfaceResource` |
| `object_store_user_policy_resource.go` | `*objectStoreUserPolicyResource` |
| `qos_policy_member_resource.go` | `*qosPolicyMemberResource` |
| `tls_policy_member_resource.go` | `*tlsPolicyMemberResource` |

**Edit A (add assertion):** After the last existing `var _ resource.Resource...` line, append:
```go
var _ resource.ResourceWithUpgradeState = &<receiverType without pointer>{}
```

Exact line numbers of the anchor (read via Serena `find_symbol` if needed):
- `bucket_access_policy_resource.go` — after line 21
- `bucket_access_policy_rule_resource.go` — after line 22
- `bucket_audit_filter_resource.go` — after line 23
- `network_interface_resource.go` — after line 28 (keep it after `ResourceWithConfigValidators`)
- `object_store_user_policy_resource.go` — after line 23
- `qos_policy_member_resource.go` — after line 21
- `tls_policy_member_resource.go` — after line 21

**Edit B (add method):** Place the no-op `UpgradeState` method next to other lifecycle hooks (right after `Configure`, or at end of file if Configure is not the last method — match whatever structure the file already has). Verify the file already imports `context` and `"github.com/hashicorp/terraform-plugin-framework/resource"` (every resource does; no import changes expected).

Example (network_interface_resource.go) — append:
```go
// UpgradeState returns state upgraders for schema migrations.
func (r *networkInterfaceResource) UpgradeState(_ context.Context) map[int64]resource.StateUpgrader {
    return map[int64]resource.StateUpgrader{}
}
```

**Do NOT:**
- Bump `Schema.Version` anywhere in this task.
- Touch `subnet_resource.go` or `qos_policy_resource.go` (handled elsewhere).
- Add new tests (no-op method has nothing meaningful to assert; covered by the compile-time interface assertion).
  </action>
  <verify>
    <automated>go build ./internal/provider/... && rg -c 'ResourceWithUpgradeState = &' internal/provider/ | awk -F: '{s+=$2} END {print s}'</automated>
  </verify>
  <done>
    - `go build ./internal/provider/...` succeeds.
    - Grep count of `ResourceWithUpgradeState = &` in `internal/provider/` is at least the prior count + 7.
    - Every one of the 7 files has a `func (r *...) UpgradeState(` declaration.
  </done>
</task>

</tasks>

<verification>
- `make test` passes; total count unchanged (no new tests in this plan).
- `make lint` clean.
- `rg -L -l 'ResourceWithUpgradeState' internal/provider/*_resource.go` returns only `object_store_access_key_resource.go`? **No — access key already has it.** Every `*_resource.go` file should now match. If any non-matched file exists (excluding intentional exceptions), flag.
</verification>

<success_criteria>
- R-007 closed: all regular resources declare the 4 mandatory interface assertions.
- 7 files edited, 0 tests added, 0 tests removed.
- Baseline test count preserved (827).
</success_criteria>

<commit>
```
fix(52-02): add ResourceWithUpgradeState assertion on 7 resources

CONVENTIONS.md §Resource Implementation mandates all four framework
interface assertions on every resource. Seven resources were missing
ResourceWithUpgradeState. Each now declares the assertion and a no-op
UpgradeState method returning an empty upgrader map.

Files updated:
- bucket_access_policy_resource.go
- bucket_access_policy_rule_resource.go
- bucket_audit_filter_resource.go
- network_interface_resource.go
- object_store_user_policy_resource.go
- qos_policy_member_resource.go
- tls_policy_member_resource.go

subnet_resource.go already carries the assertion since Phase 51;
qos_policy_resource.go is handled by 52-01 (which adds a real upgrader).

Refs: R-007, D-52-02
```

Commit with `git commit --no-verify`. No Co-Authored-By trailer.
</commit>

<output>
After completion, create `.planning/phases/52-important-conformance/52-02-SUMMARY.md`.
</output>
