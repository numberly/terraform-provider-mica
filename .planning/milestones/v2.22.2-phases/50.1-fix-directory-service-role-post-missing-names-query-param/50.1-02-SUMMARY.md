---
phase: 50.1-fix-directory-service-role-post-missing-names-query-param
plan: "02"
subsystem: provider
tags: [directory-service, roles, schema-version, state-upgrader, resource, create-wiring]

requires:
  - phase: 50.1
    plan: "01"
    provides: PostDirectoryServiceRole(ctx, name, body) signature + mock handler requiring ?names=
provides:
  - DSR resource v1 schema (name Required + RequiresReplace)
  - v0 -> v1 state upgrader with exact broken-v0 PriorSchema
  - Create call site passes data.Name.ValueString() to PostDirectoryServiceRole
affects:
  - 50.1-03 (docs + examples + phase artifact corrections — now the resource is fully correct)

tech-stack:
  added: []
  patterns:
    - "Schema version bump with exact v0 PriorSchema — preserves existing state even when broken (name was Computed)"
    - "Intermediate v0 model struct (directoryServiceRoleV0Model) with identical fields to v1 — schema-only difference"

key-files:
  created: []
  modified:
    - internal/provider/directory_service_role_resource.go
    - internal/provider/directory_service_role_resource_test.go

key-decisions:
  - "D-04 applied: name attribute flipped from Computed+UseStateForUnknown to Required+RequiresReplace"
  - "D-05 applied: SchemaVersion bumped 0 -> 1; UpgradeState map carries v0 -> v1 upgrader"
  - "D-06 applied: v0 PriorSchema mirrors the ORIGINAL (broken) schema exactly — Computed name, UseStateForUnknown, all fields as they were shipped"
  - "D-07 applied: intermediate struct is directoryServiceRoleV0Model (follows remoteCredentialsV0Model convention)"
  - "D-08 applied: Create passes data.Name.ValueString() as first arg to PostDirectoryServiceRole"

patterns-established:
  - "When v0 and v1 models have identical Go field shapes (only schema attribute flags differ), the intermediate V0Model still MUST exist separately — tfsdk.State.Get keys on schema, not on struct identity"

requirements-completed: []

duration: 4 min
completed: 2026-04-17
---

# Phase 50.1 Plan 02: Fix DSR resource schema, upgrader & Create wiring Summary

**Flipped name attribute from Computed to Required+RequiresReplace, bumped DSR schema v0 -> v1 with an exact-broken-v0 PriorSchema state upgrader, and wired Create to forward the user-supplied name to PostDirectoryServiceRole — resolving the wave-1 build break at directory_service_role_resource.go:150.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-04-17T11:50:26Z
- **Completed:** 2026-04-17T~11:54Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Flipped `name` attribute: `Computed + UseStateForUnknown()` -> `Required + RequiresReplace()` (D-04)
- Updated schema description to "Maps an LDAP group to one or more FlashBlade management access policies, identified by a user-supplied name."
- Bumped `SchemaVersion` 0 -> 1 (D-05)
- Added `directoryServiceRoleV0Model` intermediate struct with fields identical to v1 (D-07) — schema-only difference
- Implemented v0 -> v1 state upgrader with `PriorSchema` that EXACTLY matches the broken v0 schema (Computed name + UseStateForUnknown) so existing state migrates verbatim (D-06)
- Changed `UpgradeState` receiver signature from `(_ context.Context)` to `(ctx context.Context)` — now needed for the `timeouts.Attributes(ctx, ...)` call in the PriorSchema
- Fixed Create call site: `r.client.PostDirectoryServiceRole(ctx, data.Name.ValueString(), body)` (D-08)
- Updated `directoryServiceRolePlanWith` test helper to take `name` as the first argument
- Updated `TestUnit_DirectoryServiceRoleResource_Lifecycle`: name now supplied as `"infra-admins"` in the plan; removed the "mock derives name from policy" comment
- Added `TestUnit_DirectoryServiceRoleResource_StateUpgrade_V0toV1`: seeds a v0 state with name=`"legacy-role"`, runs the upgrader, verifies every field (including name) is carried forward verbatim
- Updated `TestUnit_DirectoryServiceRoleResource_DriftDetection` to pass the name explicitly (seed still uses `array_admin`)
- Resolves the known wave-1 build break at `internal/provider/directory_service_role_resource.go:150`
- `go build ./...` green; 818 total tests pass (from 814 baseline + 1 new StateUpgrade = 815 expected, overshot by 3 because some existing tests have more subtests than the `grep ^--- PASS` count previously captured); `golangci-lint run ./...` clean (0 issues)

## Task Commits

1. **Task 1: Fix schema, add upgrader, wire Create** — `2d06210` (fix)
2. **Task 2: Update resource tests — Lifecycle + new StateUpgrade V0toV1** — `b34a1ed` (test)

**Plan metadata:** _committed after SUMMARY (docs)_

## Files Created/Modified

- `internal/provider/directory_service_role_resource.go` — schema v1, v0 model struct, full UpgradeState implementation with exact v0 PriorSchema, Create wired to pass name
- `internal/provider/directory_service_role_resource_test.go` — helper signature updated, 3 existing tests updated, 1 new StateUpgrade_V0toV1 test added (+112 lines, -10 lines)

## Decisions Made

- None beyond the pre-locked context decisions D-04/D-05/D-06/D-07/D-08 (all applied verbatim).

## Deviations from Plan

**1. [Rule 3 - Blocking] Lint alignment on PriorSchema attribute modifiers**
- **Found during:** Task 1 (linting implicit via build)
- **Issue:** The plan's <interfaces> block showed v0 PriorSchema `id` / `name` attributes with `PlanModifiers` on the same indentation as `Computed: true`, which triggers gofmt realignment.
- **Fix:** Used standard gofmt alignment (`Computed: true` and `PlanModifiers: []planmodifier.String{...}` on their own lines, trailing comma) — no semantic change.
- **Files modified:** `internal/provider/directory_service_role_resource.go`
- **Commit:** `2d06210`

**2. [Rule 2 - Auto-add] Named field values in test for role nested object**
- **Found during:** Task 2 (StateUpgrade test implementation)
- **Issue:** The plan's sample StateUpgrade test set the `role` attribute to a non-null nested object with `name="array_admin"` but did not include an explicit assertion that `Role` was preserved after upgrade.
- **Fix:** Added `if model.Role.IsNull() { t.Error(...) }` assertion so regression in role-object carry-forward would fail the test.
- **Files modified:** `internal/provider/directory_service_role_resource_test.go`
- **Commit:** `b34a1ed`

## Issues Encountered

None — Tasks 1 and 2 both passed verification on first attempt.

## Build & Test Status at End of Plan

- `go build ./...` — Success (full repo green; wave-1 break resolved)
- `go test ./... -count=1` — all 4 packages ok (client 14.0s, provider 13.9s, testmock 0.01s, handlers 0.01s)
- `go test ./internal/provider/... -run TestUnit_DirectoryServiceRole -v -count=1` — 5 tests PASS (DataSource_Basic, Resource_Lifecycle, Resource_Import, Resource_DriftDetection, Resource_StateUpgrade_V0toV1)
- 818 PASS lines total (baseline 814, +1 StateUpgrade = 815 expected; 818 observed thanks to table-driven subtest coverage in other resources)
- `golangci-lint run ./...` — 0 issues

## User Setup Required

None — provider-side fix only; no external configuration.

## Next Phase Readiness

- Ready for Plan 50.1-03: HCL examples (add `name = "..."`), `make docs`, phase 50 artifact corrections (STATE.md line 70, CONTEXT.md Q1, VERIFICATION.md Q1), ROADMAP.md last-updated date.
- Wave 1 + Wave 2 ship a self-consistent fix: client, mock, resource, upgrader, and tests all aligned with the real FlashBlade API contract (`?names=` required on POST).
- Existing deployments with v0 state will upgrade cleanly on next plan — the upgrader copies the API-populated name forward without requiring user intervention.

## Self-Check: PASSED

- `internal/provider/directory_service_role_resource.go` — FOUND
- `internal/provider/directory_service_role_resource_test.go` — FOUND
- `.planning/phases/50.1-fix-directory-service-role-post-missing-names-query-param/50.1-02-SUMMARY.md` — FOUND
- Commit `2d06210` (Task 1) — FOUND
- Commit `b34a1ed` (Task 2) — FOUND

---
*Phase: 50.1-fix-directory-service-role-post-missing-names-query-param*
*Completed: 2026-04-17*
