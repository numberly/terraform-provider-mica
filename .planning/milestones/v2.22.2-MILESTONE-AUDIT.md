---
milestone: v2.22.2
milestone_name: "Directory Service Roles & Role Mappings"
audited: 2026-04-17T13:10:00Z
status: tech_debt
scores:
  requirements: 22/22
  phases: 2/2
  integration: 1/1
  flows: 1/1
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 50-directory-service-roles-role-mappings
    items:
      - "Stale doc comment in internal/client/models_admin.go:218 — says 'Name is server-generated (no names query param on POST per D-03 in 50-CONTEXT.md)', contradicts the 50.1 fix. Misleading for next contributor. (affects DSR-01 context)"
      - "ROADMAP.md line 100 Notes column still says 'server-generated name; v2.22.2'. Counters were bumped by 50-05 but row Notes describe pre-50.1 behavior. (affects QA-08, DOC-03)"
  - phase: 50.1-fix-directory-service-role-post-missing-names-query-param
    items:
      - "CONVENTIONS.md line 516 checklist still says 'total count ≥ 716 baseline' while line 451 was correctly bumped to 818. Internal inconsistency. (affects QA-06)"
nyquist:
  compliant_phases: 0
  partial_phases: 0
  missing_phases: 2
  overall: missing
  phases:
    - phase: 50
      validation_md: missing
      action: "/gsd:validate-phase 50"
    - phase: 50.1
      validation_md: missing
      action: "/gsd:validate-phase 50.1"
---

# Milestone v2.22.2 — Audit Report

**Name:** Directory Service Roles & Role Mappings
**Status:** tech_debt (no blockers — 3 doc-drift items)
**Audited:** 2026-04-17
**Auditor:** Claude (gsd-audit-milestone orchestrator)

---

## Summary

All 22 v2.22.2 requirements satisfied with evidence. Both phase VERIFICATIONs passed.
Cross-phase wiring clean (50 → 50.1 schema v0→v1 upgrade sound; E2E flow
DSR→DSRM→destroy round-trips cleanly; composite ID import symmetric).

Three non-functional doc-drift items need cleanup. Nyquist VALIDATION.md missing on
both phases (workflow enables Nyquist by default — phases shipped without formal
coverage scoring).

## Scope

| Phase | Title | Plans | Status | VERIFICATION |
|---|---|---|---|---|
| 50 | Directory Service Roles & Role Mappings | 5 | Complete | passed (6/6) |
| 50.1 | Fix DSR POST missing ?names= | 3 | Complete | passed (10/10) |

## Requirements Coverage (22/22)

### DSR (6/6 satisfied)

| REQ-ID | Phase | Status | Evidence |
|---|---|---|---|
| DSR-01 | 50, 50.1 | SATISFIED | `PostDirectoryServiceRole` with `?names=` query; user-supplied name; Create succeeds with HTTP 200 |
| DSR-02 | 50 | SATISFIED | PATCH on group/group_base without replace; role triggers RequiresReplace |
| DSR-03 | 50 | SATISFIED | `DELETE /directory-services/roles?names=<name>` |
| DSR-04 | 50 | SATISFIED | ImportState by name; timeouts init to null |
| DSR-05 | 50 | SATISFIED | Drift detection on group, group_base, role.name, management_access_policies list |
| DSR-06 | 50 | SATISFIED | Data source with required `name` attribute; `TestUnit_DirectoryServiceRoleDataSource_Basic` passes |

### DSRM (5/5 satisfied)

| REQ-ID | Phase | Status | Evidence |
|---|---|---|---|
| DSRM-01 | 50 | SATISFIED | `POST /management-access-policies/directory-services/roles` with `?policy_names=&member_names=` |
| DSRM-02 | 50 | SATISFIED | `DELETE` with both policy_names and member_names; association only |
| DSRM-03 | 50 | SATISFIED | Composite ID `<role_name>/<policy_name>` (D-05 supersedes original `:` format) |
| DSRM-04 | 50 | SATISFIED | policy + role both trigger RequiresReplace |
| DSRM-05 | 50 | SATISFIED | Read with policy_names + role_names; empty list → RemoveResource |

### DOC (3/3 satisfied)

| REQ-ID | Phase | Status | Evidence |
|---|---|---|---|
| DOC-01 | 50-05 | SATISFIED | `examples/resources/flashblade_directory_service_role/resource.tf` + `import.sh` + data-source.tf |
| DOC-02 | 50-05 | SATISFIED | `examples/resources/.../role_membership/import.sh` uses `role_name/policy_name` per D-05 |
| DOC-03 | 50-05 | SATISFIED | `make docs` idempotent; 3 new docs generated |

### QA (8/8 satisfied)

| REQ-ID | Phase | Status | Evidence |
|---|---|---|---|
| QA-01 | 50 | SATISFIED | 4 `TestUnit_DirectoryServiceRole_*` tests |
| QA-02 | 50 | SATISFIED | 3 `TestUnit_ManagementAccessPolicyDirectoryServiceRoleMembership_*` tests |
| QA-03 | 50 | SATISFIED | 3 `TestUnit_DirectoryServiceRoleResource_*` tests (Lifecycle, Import, DriftDetection) |
| QA-04 | 50 | SATISFIED | 3 membership resource tests (Lifecycle, Import, MissingAssociation) |
| QA-05 | 50 | SATISFIED | `TestUnit_DirectoryServiceRoleDataSource_Basic` |
| QA-06 | 50, 50.1 | SATISFIED | 818 tests pass (baseline ≥ 812 per REQUIREMENTS) |
| QA-07 | 50 | SATISFIED | `make lint` clean; 4 interface assertions present |
| QA-08 | 50-05 | SATISFIED | ROADMAP lines 100-101 show Done; counters bumped to 44/75% |

## Integration Check

Independent check via `gsd-integration-checker` confirmed:

- **E2E flow DSR → DSRM → destroy clean:** composite ID symmetric on round-trip,
  ImportState parses `role_name/policy_name` correctly (handles `pure:policy/array_admin`
  containing `/`), DSRM Delete severs link only, DSR Delete then succeeds.
- **Cross-phase wiring 50 → 50.1:** Schema v0 PriorSchema in
  `directory_service_role_resource.go:133-165` matches exactly what phase 50 shipped.
  Upgrader is sound (identical field shapes, direct type conversion).
- **Drift detection:** DSR Read + DSRM Read both call `RemoveResource` on 404.
- **No orphaned exports, no broken flows.**

## Tech Debt (3 items — all doc drift)

### G1 — Stale comment in `internal/client/models_admin.go:218`

Comment says `// Name is server-generated (no names query param on POST per D-03 in 50-CONTEXT.md)`.
This was accurate before 50.1 but is now misleading — Post emits `?names=<escaped>` and
the resource requires user-supplied `name`. Contradicts current behavior.

**Fix:** update comment to reflect 50.1 bugfix, or remove it (self-documenting code).

### G2 — Stale ROADMAP.md Notes column

ROADMAP.md line 100 Notes: `"server-generated name; v2.22.2"` — same regression as G1.
Counters were bumped by 50-05 finalization commit, but row Notes describe pre-50.1 semantics.

**Fix:** update Notes to `"LDAP group → management access policy mapping; user-supplied name (50.1); v2.22.2"`.

### G3 — CONVENTIONS.md baseline inconsistency

Line 451 correctly says `818`. Line 516 (checklist) still says `total count ≥ 716 baseline`.

**Fix:** update line 516 to `≥ 818 baseline`.

## Nyquist Compliance — MISSING on both phases

Project has `workflow.nyquist_validation: true` but neither phase shipped a
`VALIDATION.md`. Verification was programmatic (inside `VERIFICATION.md`) but formal
Nyquist coverage scoring was not produced.

| Phase | VALIDATION.md | Compliant | Action |
|---|---|---|---|
| 50 | missing | — | `/gsd:validate-phase 50` |
| 50.1 | missing | — | `/gsd:validate-phase 50.1` |

Same pattern as v2.22.1 audit (flagged as follow-up, non-blocking for archival).
Tracked as backlog.

## Verdict

**tech_debt** — all 22 requirements satisfied, 0 critical gaps, cross-phase
integration and E2E flow verified. Three non-functional doc-drift items + Nyquist
gaps for backlog. Not a blocker for milestone archival.

---

_Audited: 2026-04-17_
_Auditor: Claude (gsd-audit-milestone orchestrator)_
