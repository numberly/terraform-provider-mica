---
phase: 25-bucket-access-policies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/client/models_storage.go
  - internal/client/bucket_access_policies.go
  - internal/testmock/handlers/bucket_access_policies.go
autonomous: true
requirements: [BAP-01, BAP-02, BAP-03]

must_haves:
  truths:
    - "Client can create a bucket access policy with rules via POST"
    - "Client can read a bucket access policy by bucket name via GET"
    - "Client can delete a bucket access policy by bucket name via DELETE"
    - "Client can create, read, and delete individual bucket access policy rules"
    - "Mock handler stores and returns policy/rule data for unit tests"
  artifacts:
    - path: "internal/client/models_storage.go"
      provides: "BucketAccessPolicy, BucketAccessPolicyRule, BucketAccessPolicyPost, BucketAccessPolicyRulePost model structs"
      contains: "BucketAccessPolicy"
    - path: "internal/client/bucket_access_policies.go"
      provides: "GetBucketAccessPolicy, PostBucketAccessPolicy, DeleteBucketAccessPolicy, ListBucketAccessPolicyRules, PostBucketAccessPolicyRule, DeleteBucketAccessPolicyRule CRUD methods"
      exports: ["GetBucketAccessPolicy", "PostBucketAccessPolicy", "DeleteBucketAccessPolicy"]
    - path: "internal/testmock/handlers/bucket_access_policies.go"
      provides: "Mock handler for /buckets/bucket-access-policies and /buckets/bucket-access-policies/rules"
      contains: "RegisterBucketAccessPolicyHandlers"
  key_links:
    - from: "internal/client/bucket_access_policies.go"
      to: "internal/client/models_storage.go"
      via: "Uses BucketAccessPolicy/Rule model structs"
      pattern: "BucketAccessPolicy"
    - from: "internal/testmock/handlers/bucket_access_policies.go"
      to: "internal/client/models_storage.go"
      via: "Uses same model structs for in-memory store"
      pattern: "client\\.BucketAccessPolicy"
---

<objective>
Create client models, CRUD methods, and mock handler for bucket access policies and rules.

Purpose: Establish the data layer and test infrastructure so Wave 2 can build Terraform resources and data sources against it.
Output: Client package with full CRUD for `/buckets/bucket-access-policies` and `/buckets/bucket-access-policies/rules`, plus mock handler for unit tests.
</objective>

<execution_context>
@/home/gule/.claude/get-shit-done/workflows/execute-plan.md
@/home/gule/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@internal/client/models_storage.go
@internal/client/lifecycle_rules.go
@internal/testmock/handlers/lifecycle_rules.go
@FLASHBLADE_API.md

<interfaces>
<!-- Existing patterns to follow -->

From internal/client/models_common.go:
```go
type ListResponse[T any] struct {
    Items             []T    `json:"items"`
    ContinuationToken string `json:"continuation_token,omitempty"`
    TotalItemCount    int    `json:"total_item_count,omitempty"`
}
type NamedReference struct {
    Name string `json:"name,omitempty"`
    ID   string `json:"id,omitempty"`
}
```

From internal/client/lifecycle_rules.go (pattern to follow for CRUD):
```go
func (c *FlashBladeClient) GetLifecycleRule(ctx context.Context, bucketName string, ruleID string) (*LifecycleRule, error)
func (c *FlashBladeClient) PostLifecycleRule(ctx context.Context, body LifecycleRulePost, confirmDate bool) (*LifecycleRule, error)
func (c *FlashBladeClient) DeleteLifecycleRule(ctx context.Context, bucketName string, ruleID string) error
```

API endpoints (from FLASHBLADE_API.md):
- GET    /buckets/bucket-access-policies        — Params: bucket_ids, bucket_names
- POST   /buckets/bucket-access-policies        — Params: bucket_ids, bucket_names | Body: rules(array)
- DELETE /buckets/bucket-access-policies        — Params: bucket_ids, bucket_names
- GET    /buckets/bucket-access-policies/rules  — Params: bucket_ids, bucket_names
- POST   /buckets/bucket-access-policies/rules  — Params: bucket_ids, bucket_names | Body: actions, effect(ro), principals(object), resources
- DELETE /buckets/bucket-access-policies/rules  — Params: bucket_ids, bucket_names

API models:
- BucketAccessPolicy: bucket(object), enabled(bool), id(ro), is_local(ro bool), name(string), policy_type(ro), realms(ro array), rules(array)
- BucketAccessPolicyRule: actions(array), effect(ro string), name(ro string), policy(object), principals(object), resources(array)
- BucketAccessPolicyRulePost: actions(array), effect(ro string), principals(object), resources(array)

NOTE: No PATCH endpoints exist for bucket access policies or rules. Create and delete only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bucket access policy model structs to models_storage.go</name>
  <files>internal/client/models_storage.go</files>
  <action>
Append the following model structs to internal/client/models_storage.go, after the existing LifecycleRulePatch struct:

1. `BucketAccessPolicy` — GET response model:
   - `ID string` (json:"id,omitempty")
   - `Name string` (json:"name,omitempty")
   - `Bucket NamedReference` (json:"bucket")
   - `Enabled bool` (json:"enabled")
   - `IsLocal bool` (json:"is_local,omitempty")
   - `PolicyType string` (json:"policy_type,omitempty")
   - `Rules []BucketAccessPolicyRule` (json:"rules,omitempty")

2. `BucketAccessPolicyRule` — GET response model for individual rules:
   - `Name string` (json:"name,omitempty") — read-only, auto-generated by API
   - `Actions []string` (json:"actions")
   - `Effect string` (json:"effect") — read-only string, always "allow"
   - `Principals BucketAccessPolicyPrincipals` (json:"principals")
   - `Resources []string` (json:"resources")
   - `Policy *NamedReference` (json:"policy,omitempty")

3. `BucketAccessPolicyPrincipals` — nested object for rule principals:
   - `All []string` (json:"all,omitempty") — array of principal identifiers like "*"

4. `BucketAccessPolicyPost` — POST /buckets/bucket-access-policies body:
   - `Rules []BucketAccessPolicyRulePost` (json:"rules,omitempty")

5. `BucketAccessPolicyRulePost` — POST /buckets/bucket-access-policies/rules body:
   - `Actions []string` (json:"actions")
   - `Effect string` (json:"effect"` — must be "allow"
   - `Principals BucketAccessPolicyPrincipals` (json:"principals")
   - `Resources []string` (json:"resources")

Use the same comment style as existing models (doc comment before each type).
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && go build ./internal/client/...</automated>
  </verify>
  <done>All 5 model structs compile, follow existing naming conventions, and match the API schema fields.</done>
</task>

<task type="auto">
  <name>Task 2: Create bucket access policy client CRUD methods</name>
  <files>internal/client/bucket_access_policies.go</files>
  <action>
Create internal/client/bucket_access_policies.go following the lifecycle_rules.go pattern. The API base path is `/buckets/bucket-access-policies`. All operations use `bucket_names` as query parameter.

Methods to implement:

1. `GetBucketAccessPolicy(ctx, bucketName) (*BucketAccessPolicy, error)` — GET /buckets/bucket-access-policies?bucket_names={bucketName}. Use the same list+iterate pattern as GetLifecycleRule: fetch list response, iterate Items, return first match (policy is per-bucket, so there should be exactly one or zero). Return 404 APIError if not found.

2. `PostBucketAccessPolicy(ctx, bucketName, body BucketAccessPolicyPost) (*BucketAccessPolicy, error)` — POST /buckets/bucket-access-policies?bucket_names={bucketName}. Body contains rules array. Return first item from response.

3. `DeleteBucketAccessPolicy(ctx, bucketName) error` — DELETE /buckets/bucket-access-policies?bucket_names={bucketName}.

4. `ListBucketAccessPolicyRules(ctx, bucketName) ([]BucketAccessPolicyRule, error)` — GET /buckets/bucket-access-policies/rules?bucket_names={bucketName}. Follow pagination pattern from ListLifecycleRulesByBucket.

5. `GetBucketAccessPolicyRule(ctx, bucketName, ruleName) (*BucketAccessPolicyRule, error)` — GET /buckets/bucket-access-policies/rules?bucket_names={bucketName}&names={ruleName}. Return first item or 404 APIError.

6. `PostBucketAccessPolicyRule(ctx, bucketName, body BucketAccessPolicyRulePost) (*BucketAccessPolicyRule, error)` — POST /buckets/bucket-access-policies/rules?bucket_names={bucketName}. Return first item from response.

7. `DeleteBucketAccessPolicyRule(ctx, bucketName, ruleName) error` — DELETE /buckets/bucket-access-policies/rules?bucket_names={bucketName}&names={ruleName}.

Use url.QueryEscape for all parameter values. Follow existing error handling patterns (check len(resp.Items) == 0).
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && go build ./internal/client/...</automated>
  </verify>
  <done>All 7 CRUD methods compile, use correct API paths, handle pagination and not-found errors consistently with existing client methods.</done>
</task>

<task type="auto">
  <name>Task 3: Create mock handler for bucket access policies and rules</name>
  <files>internal/testmock/handlers/bucket_access_policies.go</files>
  <action>
Create internal/testmock/handlers/bucket_access_policies.go following the lifecycle_rules.go handler pattern exactly.

Two in-memory stores:
1. `bucketAccessPolicyStore` — keyed by bucket name, stores *client.BucketAccessPolicy
2. Rules stored inside the policy object's Rules slice

Registration function: `RegisterBucketAccessPolicyHandlers(mux *http.ServeMux) *bucketAccessPolicyStore`
- Register `/api/2.22/buckets/bucket-access-policies` handler (GET/POST/DELETE)
- Register `/api/2.22/buckets/bucket-access-policies/rules` handler (GET/POST/DELETE)

Policy handlers:
- GET: Filter by bucket_names query param. Return matching policies in ListResponse wrapper.
- POST: Parse bucket_names param. Create policy with auto-generated ID, set Bucket.Name from param, Enabled=true, IsLocal=true. Parse body for Rules. For each rule in body, auto-generate rule Name (e.g., "rule-1"), set Effect="allow", attach Policy reference. Store by bucket name.
- DELETE: Parse bucket_names param. Remove policy from store. Return 200 if found, 404 if not.

Rule handlers (separate endpoint):
- GET: Filter by bucket_names param. If names param also present, filter rules by name. Return matching rules.
- POST: Parse bucket_names param. Find policy for bucket. Parse body (BucketAccessPolicyRulePost). Auto-generate rule Name, set Effect="allow", attach Policy reference. Append to policy's Rules slice.
- DELETE: Parse bucket_names and names params. Find policy, remove matching rule from Rules slice.

Add `Seed(policy *client.BucketAccessPolicy)` method for test setup.

Use ValidateQueryParams helper from handlers/query_params.go. Use sync.Mutex for thread safety. Use the exact same JSON response format as lifecycle_rules.go handler (ListResponse wrapper with `items` array).
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && go build ./internal/testmock/...</automated>
  </verify>
  <done>Mock handler compiles, registers both policy and rule endpoints, supports GET/POST/DELETE for both, uses thread-safe in-memory store with Seed method.</done>
</task>

</tasks>

<verification>
```bash
cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade
go build ./...
go vet ./internal/client/... ./internal/testmock/...
```
</verification>

<success_criteria>
- All model structs match API schema (BucketAccessPolicy, BucketAccessPolicyRule, BucketAccessPolicyPrincipals, BucketAccessPolicyPost, BucketAccessPolicyRulePost)
- 7 client CRUD methods compile with correct API paths
- Mock handler registers both policy and rule endpoints
- `go build ./...` passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/25-bucket-access-policies/25-01-SUMMARY.md`
</output>
