---
phase: 58-release-pipeline-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pulumi/examples/target-py/Pulumi.yaml
  - pulumi/examples/target-py/__main__.py
  - pulumi/examples/target-go/Pulumi.yaml
  - pulumi/examples/target-go/main.go
  - pulumi/examples/remote_credentials-py/Pulumi.yaml
  - pulumi/examples/remote_credentials-py/__main__.py
  - pulumi/examples/remote_credentials-go/Pulumi.yaml
  - pulumi/examples/remote_credentials-go/main.go
  - pulumi/examples/bucket-py/Pulumi.yaml
  - pulumi/examples/bucket-py/__main__.py
  - pulumi/examples/bucket-go/Pulumi.yaml
  - pulumi/examples/bucket-go/main.go
autonomous: true
requirements:
  - TEST-02
  - DOCS-02
must_haves:
  truths:
    - "6 example directories exist under pulumi/examples/"
    - "Each example has a valid Pulumi.yaml with name, description, runtime"
    - "Python examples have __main__.py with correct pulumi_flashblade import"
    - "Go examples have main.go with correct module import path"
    - "Examples reference realistic FlashBlade resource configurations"
  artifacts:
    - path: "pulumi/examples/target-py/Pulumi.yaml"
      provides: "Python target example metadata"
    - path: "pulumi/examples/target-py/__main__.py"
      provides: "Python target example program"
    - path: "pulumi/examples/target-go/Pulumi.yaml"
      provides: "Go target example metadata"
    - path: "pulumi/examples/target-go/main.go"
      provides: "Go target example program"
    - path: "pulumi/examples/remote_credentials-py/Pulumi.yaml"
      provides: "Python remote_credentials example metadata"
    - path: "pulumi/examples/remote_credentials-py/__main__.py"
      provides: "Python remote_credentials example program"
    - path: "pulumi/examples/remote_credentials-go/Pulumi.yaml"
      provides: "Go remote_credentials example metadata"
    - path: "pulumi/examples/remote_credentials-go/main.go"
      provides: "Go remote_credentials example program"
    - path: "pulumi/examples/bucket-py/Pulumi.yaml"
      provides: "Python bucket example metadata"
    - path: "pulumi/examples/bucket-py/__main__.py"
      provides: "Python bucket example program"
    - path: "pulumi/examples/bucket-go/Pulumi.yaml"
      provides: "Go bucket example metadata"
    - path: "pulumi/examples/bucket-go/main.go"
      provides: "Go bucket example program"
  key_links:
    - from: "Python examples"
      to: "pulumi_flashblade package"
      via: "import pulumi_flashblade as flashblade"
      pattern: "import pulumi_flashblade"
    - from: "Go examples"
      to: "sdk/go module"
      via: "github.com/numberly/opentofu-provider-flashblade/pulumi/sdk/go"
      pattern: "github.com/numberly/opentofu-provider-flashblade/pulumi/sdk/go"
---

<objective>
Create 6 ProgramTest-style examples in `./pulumi/examples/`: target, remote_credentials, and bucket — each in Python and Go. Each example has a valid `Pulumi.yaml` and a working main program demonstrating the resource with provider config.

Purpose: Provide working consumer code that validates the SDK generation and serves as integration test fixtures (TEST-02) and documentation (DOCS-02).
Output: 6 example directories with Pulumi.yaml + main program files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md

## Source of truth for resource semantics

From `examples/resources/flashblade_target/resource.tf`:
```hcl
resource "flashblade_target" "primary" {
  name    = "s3-replication-target"
  address = "s3.us-east-1.amazonaws.com"
}
```

From `examples/resources/flashblade_object_store_remote_credentials/resource.tf`:
```hcl
resource "flashblade_object_store_remote_credentials" "example" {
  name              = "remote-flashblade/my-remote-creds"
  access_key_id     = "PSABSSZRHPMEDKHMAAJPJBMMCMOEHOPS"
  secret_access_key = var.remote_secret_access_key
  remote_name       = "remote-flashblade"
}
```

From `examples/resources/flashblade_bucket/resource.tf`:
```hcl
resource "flashblade_bucket" "example" {
  name    = "terraform-example-bucket"
  account = "my-account"
  quota_limit        = "107374182400"
  hard_limit_enabled = true
  versioning         = "enabled"
  destroy_eradicate_on_delete = false
}
```

## Pulumi token forms (from resources.go SingleModule)

All resources are in the `index` module:
- `flashblade:index:Target`
- `flashblade:index:ObjectStoreRemoteCredentials`
- `flashblade:index:Bucket`

## Python SDK import pattern

From `pulumi/sdk/python/setup.py`:
- Package name: `pulumi_flashblade`
- Import: `import pulumi_flashblade as flashblade`

## Go SDK import pattern

From `pulumi/sdk/go/go.mod`:
- Module: `github.com/numberly/opentofu-provider-flashblade/pulumi/sdk/go`
- Import path: `github.com/numberly/opentofu-provider-flashblade/pulumi/sdk/go/flashblade`

## Pulumi.yaml format

Standard Pulumi project file:
```yaml
name: <example-name>
description: <description>
runtime: python | go
```

## Provider config pattern

The TF provider uses nested auth config. In Pulumi:
- Python: `flashblade.Provider("flashblade", endpoint="...", auth={"api_token": "..."})`
- Go: `flashblade.NewProvider(ctx, "flashblade", &flashblade.ProviderArgs{Endpoint: pulumi.String("..."), Auth: ...})`

For examples, use placeholder values with comments indicating env var fallback.
</context>

<interfaces>
From `pulumi/sdk/python/pulumi_flashblade/__init__.py` (inferred from generated SDK):
- Resources are exported as top-level classes: `Target`, `Bucket`, `ObjectStoreRemoteCredentials`
- Provider class: `Provider`

From `pulumi/sdk/go/` (inferred from go.mod + standard bridge pattern):
- Package: `github.com/numberly/opentofu-provider-flashblade/pulumi/sdk/go/flashblade`
- Resource constructors: `NewTarget`, `NewBucket`, `NewObjectStoreRemoteCredentials`
- Provider constructor: `NewProvider`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create target examples (Python + Go)</name>
  <files>
    pulumi/examples/target-py/Pulumi.yaml
    pulumi/examples/target-py/__main__.py
    pulumi/examples/target-go/Pulumi.yaml
    pulumi/examples/target-go/main.go
  </files>
  <read_first>
    - /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade/examples/resources/flashblade_target/resource.tf (TF example source of truth)
    - /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade/pulumi/sdk/python/setup.py (Python package name)
    - /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade/pulumi/sdk/go/go.mod (Go module path)
  </read_first>
  <action>
Create 4 files for the target example:

**pulumi/examples/target-py/Pulumi.yaml:**
```yaml
name: flashblade-target-py
description: FlashBlade target resource example in Python
runtime: python
```

**pulumi/examples/target-py/__main__.py:**
```python
import pulumi
import pulumi_flashblade as flashblade

# Configure the FlashBlade provider.
# The endpoint and API token can also be set via environment variables:
#   FLASHBLADE_ENDPOINT, FLASHBLADE_AUTH_API_TOKEN
provider = flashblade.Provider("flashblade",
    endpoint="https://flashblade.example.com",
    auth={"api_token": "t.abc123"},
)

# Create a FlashBlade target (S3 replication endpoint).
target = flashblade.Target("primary",
    name="s3-replication-target",
    address="s3.us-east-1.amazonaws.com",
    opts=pulumi.ResourceOptions(provider=provider),
)

pulumi.export("target_name", target.name)
```

**pulumi/examples/target-go/Pulumi.yaml:**
```yaml
name: flashblade-target-go
description: FlashBlade target resource example in Go
runtime: go
```

**pulumi/examples/target-go/main.go:**
```go
package main

import (
	"github.com/pulumi/pulumi/sdk/v3/go/pulumi"
	"github.com/numberly/opentofu-provider-flashblade/pulumi/sdk/go/flashblade"
)

func main() {
	pulumi.Run(func(ctx *pulumi.Context) error {
		// Configure the FlashBlade provider.
		// The endpoint and API token can also be set via environment variables:
		//   FLASHBLADE_ENDPOINT, FLASHBLADE_AUTH_API_TOKEN
		provider, err := flashblade.NewProvider(ctx, "flashblade", &flashblade.ProviderArgs{
			Endpoint: pulumi.String("https://flashblade.example.com"),
			Auth:     pulumi.StringMap{"api_token": pulumi.String("t.abc123")},
		})
		if err != nil {
			return err
		}

		// Create a FlashBlade target (S3 replication endpoint).
		target, err := flashblade.NewTarget(ctx, "primary", &flashblade.TargetArgs{
			Name:    pulumi.String("s3-replication-target"),
			Address: pulumi.String("s3.us-east-1.amazonaws.com"),
		}, pulumi.Provider(provider))
		if err != nil {
			return err
		}

		ctx.Export("target_name", target.Name)
		return nil
	})
}
```
  </action>
  <acceptance_criteria>
    - `test -f pulumi/examples/target-py/Pulumi.yaml`
    - `test -f pulumi/examples/target-py/__main__.py`
    - `test -f pulumi/examples/target-go/Pulumi.yaml`
    - `test -f pulumi/examples/target-go/main.go`
    - `grep -q "runtime: python" pulumi/examples/target-py/Pulumi.yaml`
    - `grep -q "import pulumi_flashblade" pulumi/examples/target-py/__main__.py`
    - `grep -q "flashblade.Target" pulumi/examples/target-py/__main__.py`
    - `grep -q "runtime: go" pulumi/examples/target-go/Pulumi.yaml`
    - `grep -q "github.com/numberly/opentofu-provider-flashblade/pulumi/sdk/go/flashblade" pulumi/examples/target-go/main.go`
    - `grep -q "flashblade.NewTarget" pulumi/examples/target-go/main.go`
    - `grep -q "pulumi.Provider(provider)" pulumi/examples/target-go/main.go`
  </acceptance_criteria>
  <verify>
    <automated>ls pulumi/examples/target-py/Pulumi.yaml pulumi/examples/target-py/__main__.py pulumi/examples/target-go/Pulumi.yaml pulumi/examples/target-go/main.go >/dev/null 2>&1 && echo "all 4 files exist"</automated>
  </verify>
  <done>Target examples exist in both Python and Go with correct imports, provider config, resource creation, and output export.</done>
</task>

<task type="auto">
  <name>Task 2: Create remote_credentials examples (Python + Go)</name>
  <files>
    pulumi/examples/remote_credentials-py/Pulumi.yaml
    pulumi/examples/remote_credentials-py/__main__.py
    pulumi/examples/remote_credentials-go/Pulumi.yaml
    pulumi/examples/remote_credentials-go/main.go
  </files>
  <read_first>
    - /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade/examples/resources/flashblade_object_store_remote_credentials/resource.tf
    - /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade/pulumi/sdk/python/setup.py
    - /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade/pulumi/sdk/go/go.mod
  </read_first>
  <action>
Create 4 files for the remote_credentials example:

**pulumi/examples/remote_credentials-py/Pulumi.yaml:**
```yaml
name: flashblade-remote-credentials-py
description: FlashBlade remote credentials example in Python
runtime: python
```

**pulumi/examples/remote_credentials-py/__main__.py:**
```python
import pulumi
import pulumi_flashblade as flashblade

# Configure the FlashBlade provider.
provider = flashblade.Provider("flashblade",
    endpoint="https://flashblade.example.com",
    auth={"api_token": "t.abc123"},
)

# Create remote credentials for cross-array replication.
# Name must be formatted as <remote-name>/<credentials-name>.
# secret_access_key is sensitive and write-only.
creds = flashblade.ObjectStoreRemoteCredentials("example",
    name="remote-flashblade/my-remote-creds",
    access_key_id="PSABSSZRHPMEDKHMAAJPJBMMCMOEHOPS",
    secret_access_key="sensitive-value-set-via-config-secret",
    remote_name="remote-flashblade",
    opts=pulumi.ResourceOptions(provider=provider),
)

pulumi.export("credentials_name", creds.name)
```

**pulumi/examples/remote_credentials-go/Pulumi.yaml:**
```yaml
name: flashblade-remote-credentials-go
description: FlashBlade remote credentials example in Go
runtime: go
```

**pulumi/examples/remote_credentials-go/main.go:**
```go
package main

import (
	"github.com/pulumi/pulumi/sdk/v3/go/pulumi"
	"github.com/numberly/opentofu-provider-flashblade/pulumi/sdk/go/flashblade"
)

func main() {
	pulumi.Run(func(ctx *pulumi.Context) error {
		provider, err := flashblade.NewProvider(ctx, "flashblade", &flashblade.ProviderArgs{
			Endpoint: pulumi.String("https://flashblade.example.com"),
			Auth:     pulumi.StringMap{"api_token": pulumi.String("t.abc123")},
		})
		if err != nil {
			return err
		}

		// Create remote credentials for cross-array replication.
		// Name must be formatted as <remote-name>/<credentials-name>.
		// secret_access_key is sensitive and write-only.
		creds, err := flashblade.NewObjectStoreRemoteCredentials(ctx, "example", &flashblade.ObjectStoreRemoteCredentialsArgs{
			Name:            pulumi.String("remote-flashblade/my-remote-creds"),
			AccessKeyId:     pulumi.String("PSABSSZRHPMEDKHMAAJPJBMMCMOEHOPS"),
			SecretAccessKey: pulumi.String("sensitive-value-set-via-config-secret"),
			RemoteName:      pulumi.String("remote-flashblade"),
		}, pulumi.Provider(provider))
		if err != nil {
			return err
		}

		ctx.Export("credentials_name", creds.Name)
		return nil
	})
}
```
  </action>
  <acceptance_criteria>
    - `test -f pulumi/examples/remote_credentials-py/Pulumi.yaml`
    - `test -f pulumi/examples/remote_credentials-py/__main__.py`
    - `test -f pulumi/examples/remote_credentials-go/Pulumi.yaml`
    - `test -f pulumi/examples/remote_credentials-go/main.go`
    - `grep -q "runtime: python" pulumi/examples/remote_credentials-py/Pulumi.yaml`
    - `grep -q "flashblade.ObjectStoreRemoteCredentials" pulumi/examples/remote_credentials-py/__main__.py`
    - `grep -q "secret_access_key" pulumi/examples/remote_credentials-py/__main__.py`
    - `grep -q "runtime: go" pulumi/examples/remote_credentials-go/Pulumi.yaml`
    - `grep -q "flashblade.NewObjectStoreRemoteCredentials" pulumi/examples/remote_credentials-go/main.go`
    - `grep -q "SecretAccessKey" pulumi/examples/remote_credentials-go/main.go`
  </acceptance_criteria>
  <verify>
    <automated>ls pulumi/examples/remote_credentials-py/Pulumi.yaml pulumi/examples/remote_credentials-py/__main__.py pulumi/examples/remote_credentials-go/Pulumi.yaml pulumi/examples/remote_credentials-go/main.go >/dev/null 2>&1 && echo "all 4 files exist"</automated>
  </verify>
  <done>Remote credentials examples exist in both Python and Go with correct resource name format, sensitive field handling, and provider config.</done>
</task>

<task type="auto">
  <name>Task 3: Create bucket examples (Python + Go) with soft-delete timeout</name>
  <files>
    pulumi/examples/bucket-py/Pulumi.yaml
    pulumi/examples/bucket-py/__main__.py
    pulumi/examples/bucket-go/Pulumi.yaml
    pulumi/examples/bucket-go/main.go
  </files>
  <read_first>
    - /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade/examples/resources/flashblade_bucket/resource.tf
    - /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade/pulumi/sdk/python/setup.py
    - /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade/pulumi/sdk/go/go.mod
  </read_first>
  <action>
Create 4 files for the bucket example:

**pulumi/examples/bucket-py/Pulumi.yaml:**
```yaml
name: flashblade-bucket-py
description: FlashBlade bucket resource example in Python
runtime: python
```

**pulumi/examples/bucket-py/__main__.py:**
```python
import pulumi
import pulumi_flashblade as flashblade

# Configure the FlashBlade provider.
provider = flashblade.Provider("flashblade",
    endpoint="https://flashblade.example.com",
    auth={"api_token": "t.abc123"},
)

# Create a FlashBlade bucket with soft-delete (two-phase destroy).
# The default delete timeout is 30 minutes to allow for eradication polling.
# Set customTimeouts if your array requires more time.
bucket = flashblade.Bucket("example",
    name="pulumi-example-bucket",
    account="my-account",
    quota_limit="107374182400",
    hard_limit_enabled=True,
    versioning="enabled",
    destroy_eradicate_on_delete=False,
    opts=pulumi.ResourceOptions(
        provider=provider,
        custom_timeouts=pulumi.CustomTimeouts(
            create="20m",
            update="20m",
            delete="30m",
        ),
    ),
)

pulumi.export("bucket_name", bucket.name)
```

**pulumi/examples/bucket-go/Pulumi.yaml:**
```yaml
name: flashblade-bucket-go
description: FlashBlade bucket resource example in Go
runtime: go
```

**pulumi/examples/bucket-go/main.go:**
```go
package main

import (
	"github.com/pulumi/pulumi/sdk/v3/go/pulumi"
	"github.com/numberly/opentofu-provider-flashblade/pulumi/sdk/go/flashblade"
)

func main() {
	pulumi.Run(func(ctx *pulumi.Context) error {
		provider, err := flashblade.NewProvider(ctx, "flashblade", &flashblade.ProviderArgs{
			Endpoint: pulumi.String("https://flashblade.example.com"),
			Auth:     pulumi.StringMap{"api_token": pulumi.String("t.abc123")},
		})
		if err != nil {
			return err
		}

		// Create a FlashBlade bucket with soft-delete (two-phase destroy).
		// The default delete timeout is 30 minutes to allow for eradication polling.
		// Set customTimeouts if your array requires more time.
		bucket, err := flashblade.NewBucket(ctx, "example", &flashblade.BucketArgs{
			Name:                     pulumi.String("pulumi-example-bucket"),
			Account:                  pulumi.String("my-account"),
			QuotaLimit:               pulumi.String("107374182400"),
			HardLimitEnabled:         pulumi.Bool(true),
			Versioning:               pulumi.String("enabled"),
			DestroyEradicateOnDelete: pulumi.Bool(false),
		}, pulumi.Provider(provider), pulumi.Timeouts(&pulumi.CustomTimeouts{
			Create: "20m",
			Update: "20m",
			Delete: "30m",
		}))
		if err != nil {
			return err
		}

		ctx.Export("bucket_name", bucket.Name)
		return nil
	})
}
```
  </action>
  <acceptance_criteria>
    - `test -f pulumi/examples/bucket-py/Pulumi.yaml`
    - `test -f pulumi/examples/bucket-py/__main__.py`
    - `test -f pulumi/examples/bucket-go/Pulumi.yaml`
    - `test -f pulumi/examples/bucket-go/main.go`
    - `grep -q "runtime: python" pulumi/examples/bucket-py/Pulumi.yaml`
    - `grep -q "flashblade.Bucket" pulumi/examples/bucket-py/__main__.py`
    - `grep -q "custom_timeouts" pulumi/examples/bucket-py/__main__.py`
    - `grep -q "destroy_eradicate_on_delete" pulumi/examples/bucket-py/__main__.py`
    - `grep -q "runtime: go" pulumi/examples/bucket-go/Pulumi.yaml`
    - `grep -q "flashblade.NewBucket" pulumi/examples/bucket-go/main.go`
    - `grep -q "CustomTimeouts" pulumi/examples/bucket-go/main.go`
    - `grep -q "DestroyEradicateOnDelete" pulumi/examples/bucket-go/main.go`
  </acceptance_criteria>
  <verify>
    <automated>ls pulumi/examples/bucket-py/Pulumi.yaml pulumi/examples/bucket-py/__main__.py pulumi/examples/bucket-go/Pulumi.yaml pulumi/examples/bucket-go/main.go >/dev/null 2>&1 && echo "all 4 files exist"</automated>
  </verify>
  <done>Bucket examples exist in both Python and Go with soft-delete config, custom timeouts (20m create/update, 30m delete), and provider config.</done>
</task>

</tasks>

<verification>
1. All 6 example directories exist:
   - `ls pulumi/examples/target-py/ pulumi/examples/target-go/ pulumi/examples/remote_credentials-py/ pulumi/examples/remote_credentials-go/ pulumi/examples/bucket-py/ pulumi/examples/bucket-go/`
2. Each has Pulumi.yaml with correct runtime:
   - `grep "runtime:" pulumi/examples/*/Pulumi.yaml`
3. Python examples import pulumi_flashblade:
   - `grep "import pulumi_flashblade" pulumi/examples/*-py/__main__.py`
4. Go examples import the correct module path:
   - `grep "github.com/numberly/opentofu-provider-flashblade/pulumi/sdk/go/flashblade" pulumi/examples/*-go/main.go`
5. No `.gitkeep` remains as the only file in examples/:
   - `test $(ls pulumi/examples/ | wc -l) -ge 6`
</verification>

<success_criteria>
- 6 example directories exist under `pulumi/examples/`
- Each example has `Pulumi.yaml` with `name`, `description`, and `runtime`
- Python examples have `__main__.py` with `pulumi_flashblade` import and resource creation
- Go examples have `main.go` with correct module import and `pulumi.Run`
- Bucket examples demonstrate `customTimeouts` for soft-delete (30m delete)
- Remote credentials examples show sensitive field handling
</success_criteria>

<output>
After completion, create `.planning/phases/58-release-pipeline-docs/58-02-SUMMARY.md`
</output>
