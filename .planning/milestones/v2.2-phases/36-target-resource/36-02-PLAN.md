---
phase: 36-target-resource
plan: 02
type: execute
wave: 2
depends_on:
  - 36-01
files_modified:
  - internal/provider/target_resource.go
  - internal/provider/target_resource_test.go
  - internal/provider/target_data_source.go
  - internal/provider/target_data_source_test.go
  - internal/provider/provider.go
  - examples/resources/flashblade_target/resource.tf
  - examples/data-sources/flashblade_target/data-source.tf
autonomous: true
requirements:
  - TGT-01
  - TGT-02
  - TGT-03
  - TGT-04
  - TGT-05

must_haves:
  truths:
    - "Operator can create a target with name and address; apply then plan shows 0 diff"
    - "Operator can update address and ca_certificate_group; destroy removes the target"
    - "terraform import flashblade_target.x target-name populates all attributes; plan shows 0 diff"
    - "data.flashblade_target reads by name and exposes address, status, status_details"
    - "Read logs field-level changes via tflog when target is modified outside Terraform"
  artifacts:
    - path: "internal/provider/target_resource.go"
      provides: "flashblade_target CRUD resource with import and drift detection"
      exports: ["NewTargetResource"]
    - path: "internal/provider/target_data_source.go"
      provides: "data.flashblade_target data source reads target by name"
      exports: ["NewTargetDataSource"]
    - path: "internal/provider/target_resource_test.go"
      provides: "Mocked integration tests for resource lifecycle"
    - path: "internal/provider/target_data_source_test.go"
      provides: "Mocked integration test for data source"
    - path: "internal/provider/provider.go"
      provides: "NewTargetResource and NewTargetDataSource registered"
  key_links:
    - from: "internal/provider/target_resource.go"
      to: "internal/client/targets.go"
      via: "r.client.GetTarget / PostTarget / PatchTarget / DeleteTarget"
      pattern: "r\\.client\\.(Get|Post|Patch|Delete)Target"
    - from: "internal/provider/provider.go"
      to: "internal/provider/target_resource.go"
      via: "NewTargetResource in Resources() list"
    - from: "internal/provider/provider.go"
      to: "internal/provider/target_data_source.go"
      via: "NewTargetDataSource in DataSources() list"
---

<objective>
Implement the `flashblade_target` Terraform resource (full CRUD, import, drift detection) and its data source, wire them into the provider, and add HCL examples.

Purpose: Delivers requirements TGT-01 through TGT-05 — operators can fully manage external S3 endpoint targets through Terraform.
Output: target_resource.go, target_data_source.go, mocked tests, provider registration, HCL examples.
</objective>

<execution_context>
@/home/gule/.claude/get-shit-done/workflows/execute-plan.md
@/home/gule/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-target-resource/36-01-SUMMARY.md

<interfaces>
<!-- Types produced by plan 36-01 (internal/client/targets.go + models_storage.go) -->

```go
// Target is the GET/PATCH response struct.
type Target struct {
    ID                 string          `json:"id"`
    Name               string          `json:"name"`
    Address            string          `json:"address"`
    CACertificateGroup *NamedReference `json:"ca_certificate_group,omitempty"`
    Status             string          `json:"status,omitempty"`
    StatusDetails      string          `json:"status_details,omitempty"`
}

type TargetPost struct {
    Address string `json:"address"`
}

type TargetPatch struct {
    Address            *string          `json:"address,omitempty"`
    CACertificateGroup **NamedReference `json:"ca_certificate_group,omitempty"`
}

// FlashBladeClient methods:
func (c *FlashBladeClient) GetTarget(ctx context.Context, name string) (*Target, error)
func (c *FlashBladeClient) PostTarget(ctx context.Context, name string, body TargetPost) (*Target, error)
func (c *FlashBladeClient) PatchTarget(ctx context.Context, name string, body TargetPatch) (*Target, error)
func (c *FlashBladeClient) DeleteTarget(ctx context.Context, name string) error
```

<!-- Pattern reference: remote_credentials_resource.go (simple CRUD resource, name-based import) -->

Key patterns from remoteCredentialsResource:
- Schema: SchemaVersion: 0, UpgradeState returns empty map
- name: RequiresReplace, UseStateForUnknown on id
- ImportState: loads by name, sets data.Timeouts = nullTimeoutsValue()
- mapXxxToModel helper: maps API struct to tfsdk model, never sets write-only fields
- Drift detection: compare old vs new state, log via tflog.Debug("target drifted: field=address ...")

<!-- Pattern reference: testmock/handlers/targets.go from plan 36-01 -->

RegisterTargetHandlers(mux *http.ServeMux) *targetStore
store.Seed(t *client.Target)

<!-- Provider registration pattern from provider.go -->

// Resources() slice — append NewTargetResource
// DataSources() slice — append NewTargetDataSource
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement flashblade_target resource with drift detection</name>
  <files>internal/provider/target_resource.go, internal/provider/target_resource_test.go</files>
  <behavior>
    Resource schema (type name: flashblade_target):
    - id: Computed, UseStateForUnknown
    - name: Required, RequiresReplace
    - address: Required (mutable)
    - ca_certificate_group: Optional, Computed (mutable) — stores the cert group name as a string (not nested object, to keep schema flat); null when not set
    - status: Computed, UseStateForUnknown — read-only API field
    - status_details: Computed, UseStateForUnknown — read-only API field
    - timeouts: Create/Read/Update/Delete

    Create: PostTarget(ctx, name, TargetPost{Address: address}) → store all fields including status/status_details
    Read: GetTarget → drift detection logs any field that changed (address, ca_certificate_group, status, status_details) via tflog.Debug; updates state
    Update: PatchTarget with only changed fields (address and/or ca_certificate_group); re-read after patch
    Delete: DeleteTarget; ignore IsNotFound
    ImportState: GetTarget by req.ID; initialize nullTimeoutsValue(); set all fields; subsequent plan must show 0 diff

    Drift detection in Read: compare each field from prior state vs API response; for each difference log:
      tflog.Debug(ctx, "target field changed outside Terraform", map[string]any{"field": "address", "was": old, "now": new})

    Schema version: 0. UpgradeState returns empty map.

    Mocked integration tests (target_resource_test.go):
    - TestTargetResource_lifecycle: create → check plan converges (0 diff) → update address → check → destroy
    - TestTargetResource_import: create → import by name → check plan converges
    - TestTargetResource_driftDetection: create → Seed modified target in mock → refresh → check state updated
  </behavior>
  <action>
    Create internal/provider/target_resource.go following the remoteCredentialsResource pattern.

    Type name: `flashblade_target`

    ca_certificate_group handling:
    - Schema: schema.StringAttribute{Optional: true, Computed: true} — store only the group name string (e.g., "default")
    - In mapTargetToModel: if API returns non-nil CACertificateGroup, set field to StringValue(group.Name); else set to StringNull()
    - In Update: if plan.CACertificateGroup differs from state.CACertificateGroup:
      - if plan is null → set TargetPatch.CACertificateGroup = &(*NamedReference)(nil) (clear the field)
      - if plan has value → set TargetPatch.CACertificateGroup = &ptr where ptr points to NamedReference{Name: value}

    ImportState: use name (req.ID) not UUID — the API identifies targets by name. Set all fields from GetTarget response.

    Test file: package provider_test, use testNewMockedProvider() pattern from existing test files.
    RegisterTargetHandlers from internal/testmock/handlers/targets.go for mock server.

    Follow project CLAUDE.md: SchemaVersion + UpgradeState, UseStateForUnknown on computed stable fields, drift detection via tflog.
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && go test ./internal/provider/ -run TestTargetResource -v -count=1</automated>
  </verify>
  <done>All TestTargetResource* tests pass. `go build ./...` clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement flashblade_target data source, register both in provider, add HCL examples</name>
  <files>internal/provider/target_data_source.go, internal/provider/target_data_source_test.go, internal/provider/provider.go, examples/resources/flashblade_target/resource.tf, examples/data-sources/flashblade_target/data-source.tf</files>
  <behavior>
    Data source schema (type name: flashblade_target):
    - name: Required — used to look up the target
    - address: Computed
    - status: Computed
    - status_details: Computed
    - ca_certificate_group: Computed (string, optional — null when not set)
    - id: Computed (UUID from API)

    Read: GetTarget(ctx, name) → populate all fields; return error diagnostic on failure

    Mocked test (target_data_source_test.go):
    - TestTargetDataSource_basic: seed a target in mock → read via data source → check address, status, status_details match

    provider.go:
    - Add NewTargetResource to Resources() slice
    - Add NewTargetDataSource to DataSources() slice

    HCL examples:
    - examples/resources/flashblade_target/resource.tf: minimal resource block with name and address; comment showing optional ca_certificate_group usage
    - examples/data-sources/flashblade_target/data-source.tf: data source block reading an existing target by name
  </behavior>
  <action>
    Create internal/provider/target_data_source.go following existing data source patterns (e.g., remote_credentials_data_source.go or object_store_user_data_source.go).

    Data source does NOT expose timeouts (consistent with simple read-only data sources in this codebase — check remote_credentials_data_source.go for confirmation).

    Update internal/provider/provider.go:
    - In Resources(): append `NewTargetResource,` after NewObjectStoreUserPolicyResource line
    - In DataSources(): append `NewTargetDataSource,` after NewNetworkInterfaceDataSource line (end of list)

    Create examples directories and HCL files:
    - examples/resources/flashblade_target/resource.tf
    - examples/data-sources/flashblade_target/data-source.tf

    After all files are written, run `make docs` to regenerate docs/ — do NOT edit docs/ manually.

    Run `make build` and `make test` to confirm full suite still passes.
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && go test ./internal/provider/ -run "TestTargetDataSource|TestTargetResource" -v -count=1 && make build</automated>
  </verify>
  <done>
    - TestTargetDataSource_basic passes
    - NewTargetResource and NewTargetDataSource registered in provider.go
    - `make build` succeeds
    - examples/resources/flashblade_target/resource.tf and examples/data-sources/flashblade_target/data-source.tf exist
    - `make docs` completes without error
    - `go test ./... -count=1` shows no regressions (668+ tests pass)
  </done>
</task>

</tasks>

<verification>
```
cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade
make build
make test
make lint
make docs
```
All pass. Test count increases by at least 8 (6 client tests from plan 36-01 + 2-3 provider tests). ROADMAP.md updated to mark TGT-01 through TGT-05 as done.
</verification>

<success_criteria>
- flashblade_target resource: Create, Read (drift detection), Update, Delete, ImportState all implemented and tested
- data.flashblade_target: reads by name, exposes address / status / status_details / ca_certificate_group / id
- NewTargetResource and NewTargetDataSource registered in provider.go
- HCL examples exist in examples/resources/flashblade_target/ and examples/data-sources/flashblade_target/
- `make build && make test && make lint && make docs` all clean
- ROADMAP.md updated: TGT-01 through TGT-05 marked done with resource name flashblade_target
</success_criteria>

<output>
After completion, create `.planning/phases/36-target-resource/36-02-SUMMARY.md`
</output>
