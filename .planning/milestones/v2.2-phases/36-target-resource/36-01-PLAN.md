---
phase: 36-target-resource
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/client/models_storage.go
  - internal/client/targets.go
  - internal/client/targets_test.go
  - internal/testmock/handlers/targets.go
autonomous: true
requirements:
  - TGT-01
  - TGT-02
  - TGT-03
  - TGT-04
  - TGT-05

must_haves:
  truths:
    - "Client can create a target by name and address (POST /targets?names=)"
    - "Client can read a target by name (GET /targets?names=)"
    - "Client can update a target address and ca_certificate_group (PATCH /targets?names=)"
    - "Client can delete a target by name (DELETE /targets?names=)"
    - "Mock handler returns 404 on unknown name, 409 on duplicate POST"
  artifacts:
    - path: "internal/client/targets.go"
      provides: "Target CRUD client methods"
      exports: ["GetTarget", "PostTarget", "PatchTarget", "DeleteTarget"]
    - path: "internal/client/models_storage.go"
      provides: "Target model structs"
      contains: "type Target struct"
    - path: "internal/testmock/handlers/targets.go"
      provides: "Thread-safe in-memory mock for /api/2.22/targets"
    - path: "internal/client/targets_test.go"
      provides: "Unit tests covering all four CRUD methods"
  key_links:
    - from: "internal/client/targets.go"
      to: "internal/client/models_storage.go"
      via: "Target / TargetPost / TargetPatch structs"
    - from: "internal/client/targets_test.go"
      to: "internal/testmock/handlers/targets.go"
      via: "httptest.NewServer with RegisterTargetHandlers"
---

<objective>
Define Target model structs, implement client CRUD methods, build mock handler, and write unit tests.

Purpose: Provides the foundational client layer consumed by the provider resource and data source in plan 36-02.
Output: targets.go (client), targets_test.go (unit tests), handlers/targets.go (mock), Target* structs in models_storage.go.
</objective>

<execution_context>
@/home/gule/.claude/get-shit-done/workflows/execute-plan.md
@/home/gule/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key API contract from FLASHBLADE_API.md -->

GET  /api/2.22/targets                        → list all targets
GET  /api/2.22/targets?names={name}           → get single target by name
POST /api/2.22/targets?names={name}           → create target; body: address (string)
PATCH /api/2.22/targets?names={name}          → update; body fields: address (string), ca_certificate_group (object/null)
DELETE /api/2.22/targets?names={name}         → delete target

Response body fields (GET/PATCH):
  id (ro string), name (string), address (string),
  ca_certificate_group (object: {name string}),
  status (ro string), status_details (ro string)

<!-- Established model pattern from internal/client/models_storage.go -->

type ObjectStoreRemoteCredentials struct {
    ID              string           `json:"id"`
    Name            string           `json:"name"`
    AccessKeyID     string           `json:"access_key_id"`
    SecretAccessKey string           `json:"secret_access_key,omitempty"`
    Remote          NamedReference   `json:"remote"`
}

type ObjectStoreRemoteCredentialsPost struct {
    AccessKeyID     string `json:"access_key_id"`
    SecretAccessKey string `json:"secret_access_key"`
}

type ObjectStoreRemoteCredentialsPatch struct {
    AccessKeyID     *string `json:"access_key_id,omitempty"`
    SecretAccessKey *string `json:"secret_access_key,omitempty"`
}

// NamedReference is already defined in models_common.go:
type NamedReference struct {
    Name string `json:"name"`
}

<!-- Established client pattern from internal/client/remote_credentials.go -->

func (c *FlashBladeClient) GetRemoteCredentials(ctx context.Context, name string) (*ObjectStoreRemoteCredentials, error) {
    return getOneByName[ObjectStoreRemoteCredentials](c, ctx, "/object-store-remote-credentials?names="+url.QueryEscape(name), "remote credentials", name)
}

func (c *FlashBladeClient) PostRemoteCredentials(ctx context.Context, name string, remoteName string, body ObjectStoreRemoteCredentialsPost) (*ObjectStoreRemoteCredentials, error) {
    path := "/object-store-remote-credentials?names=" + url.QueryEscape(name) + "&remote_names=" + url.QueryEscape(remoteName)
    var resp ListResponse[ObjectStoreRemoteCredentials]
    if err := c.post(ctx, path, body, &resp); err != nil {
        return nil, err
    }
    if len(resp.Items) == 0 {
        return nil, fmt.Errorf("PostRemoteCredentials: empty response from server")
    }
    return &resp.Items[0], nil
}

<!-- Established mock handler pattern from internal/testmock/handlers/remote_credentials.go -->

type remoteCredentialsStore struct {
    mu     sync.Mutex
    byName map[string]*client.ObjectStoreRemoteCredentials
    nextID int
}

func RegisterRemoteCredentialsHandlers(mux *http.ServeMux) *remoteCredentialsStore { ... }
func (s *remoteCredentialsStore) Seed(cred *client.ObjectStoreRemoteCredentials) { ... }
func (s *remoteCredentialsStore) handle(w http.ResponseWriter, r *http.Request) { ... }
// handleGet validates query param "names" via ValidateQueryParams / RequireQueryParam helpers
// handlePost requires body.Address; checks for duplicate; stores new entry
// handlePatch patches only non-nil pointer fields on existing entry
// handleDelete removes entry or returns 404
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Target model structs in models_storage.go</name>
  <files>internal/client/models_storage.go</files>
  <behavior>
    - Target struct has: ID (string), Name (string), Address (string), CACertificateGroup (*NamedReference, omitempty), Status (string, omitempty), StatusDetails (string, omitempty)
    - TargetPost struct has: Address (string) — name is passed via ?names= query param
    - TargetPatch struct uses pointer fields for PATCH semantics: Address (*string), CACertificateGroup (**NamedReference) — both omitempty
    - JSON tags use snake_case matching API field names: id, name, address, ca_certificate_group, status, status_details
  </behavior>
  <action>
    Append three structs at the end of models_storage.go, after the existing remote credentials structs (around line 215). Do NOT add a new file — models_storage.go is the canonical location for all storage-domain models.

    Target GET/PATCH response:
    ```go
    type Target struct {
        ID                 string         `json:"id"`
        Name               string         `json:"name"`
        Address            string         `json:"address"`
        CACertificateGroup *NamedReference `json:"ca_certificate_group,omitempty"`
        Status             string         `json:"status,omitempty"`
        StatusDetails      string         `json:"status_details,omitempty"`
    }
    ```

    Target POST body (name via ?names=):
    ```go
    type TargetPost struct {
        Address string `json:"address"`
    }
    ```

    Target PATCH body (pointer fields = PATCH semantics):
    ```go
    type TargetPatch struct {
        Address            *string         `json:"address,omitempty"`
        CACertificateGroup **NamedReference `json:"ca_certificate_group,omitempty"`
    }
    ```

    Note on TargetPatch.CACertificateGroup: use `**NamedReference` so that a nil outer pointer means "omit field" and a non-nil outer pointer with nil inner means "set to null" (clearing the cert group). This matches PATCH semantics for nullable object fields in this codebase.
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && go build ./internal/client/...</automated>
  </verify>
  <done>models_storage.go compiles with Target, TargetPost, TargetPatch exported types.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement client CRUD methods in targets.go</name>
  <files>internal/client/targets.go</files>
  <behavior>
    - GetTarget(ctx, name) uses getOneByName[Target] helper — returns IsNotFound error on 404
    - PostTarget(ctx, name, body TargetPost) posts to /targets?names={name}, returns *Target
    - PatchTarget(ctx, name, body TargetPatch) patches /targets?names={name}, returns *Target
    - DeleteTarget(ctx, name) deletes /targets?names={name}, returns error
    - All paths use url.QueryEscape on name
    - Empty ListResponse from POST/PATCH returns a descriptive error ("PostTarget: empty response from server")
  </behavior>
  <action>
    Create internal/client/targets.go following the exact pattern of remote_credentials.go.

    Package: `package client`
    Imports: `context`, `fmt`, `net/url`

    Four functions:
    - GetTarget: `getOneByName[Target](c, ctx, "/targets?names="+url.QueryEscape(name), "target", name)`
    - PostTarget: POST to `/targets?names={name}` with TargetPost body
    - PatchTarget: PATCH to `/targets?names={name}` with TargetPatch body
    - DeleteTarget: DELETE `/targets?names={name}`

    No ListTargets method needed (data source reads by name, not listing all).
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && go build ./internal/client/...</automated>
  </verify>
  <done>targets.go compiles; GetTarget, PostTarget, PatchTarget, DeleteTarget are exported symbols.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build mock handler and write unit tests</name>
  <files>internal/testmock/handlers/targets.go, internal/client/targets_test.go</files>
  <behavior>
    Mock handler (handlers/targets.go):
    - targetStore: mutex + byName map[string]*client.Target + nextID int
    - RegisterTargetHandlers(mux) registers /api/2.22/targets
    - Seed(t *client.Target) for test setup
    - handleGet: ValidateQueryParams(["names"]); if ?names= given, filter by name (404 body if not found via WriteJSONError); else return all
    - handlePost: RequireQueryParam "names"; decode TargetPost; require Address non-empty; 409 if name exists; store and return item
    - handlePatch: RequireQueryParam "names"; decode TargetPatch; 404 if not found; apply non-nil pointer fields; return updated item
    - handleDelete: RequireQueryParam "names"; 404 if not found; delete; 200 empty

    Unit tests (targets_test.go):
    - TestGetTarget_found: seed a target, GetTarget returns correct fields
    - TestGetTarget_notFound: GetTarget on unknown name returns IsNotFound error
    - TestPostTarget: PostTarget creates target, returned fields match input
    - TestPatchTarget_address: PatchTarget with new address, returned address updated
    - TestPatchTarget_caCertGroup: PatchTarget with non-nil CACertificateGroup, returned group set
    - TestDeleteTarget: DeleteTarget removes entry; subsequent GetTarget returns IsNotFound
  </behavior>
  <action>
    Create internal/testmock/handlers/targets.go following the pattern of handlers/remote_credentials.go.

    Key differences from remote_credentials handler:
    - POST body requires Address (not AccessKeyID/SecretAccessKey)
    - CACertificateGroup is a nullable object field — PATCH handler must handle **NamedReference: if outer ptr non-nil and inner nil, set CACertificateGroup to nil on stored Target; if inner non-nil, set to the NamedReference value
    - status and status_details: mock handler sets status="connected" and status_details="" on POST, unchanged on PATCH

    Create internal/client/targets_test.go using package client_test.
    Test setup: spin up httptest.NewServer with RegisterTargetHandlers (following pattern of object_store_users_test.go or remote_credentials_test.go in the same package).
    Each test creates a fresh mux+server, runs the client operation, asserts the result.
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && go test ./internal/client/ -run TestGetTarget -v && go test ./internal/client/ -run TestPostTarget -v && go test ./internal/client/ -run TestPatchTarget -v && go test ./internal/client/ -run TestDeleteTarget -v</automated>
  </verify>
  <done>All 6 client unit tests pass. `go test ./internal/client/ -count=1` shows no failures.</done>
</task>

</tasks>

<verification>
```
cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade
go build ./...
go test ./internal/client/ -count=1 -run "Target"
make lint
```
All pass with no errors.
</verification>

<success_criteria>
- Target, TargetPost, TargetPatch structs exported from internal/client
- GetTarget, PostTarget, PatchTarget, DeleteTarget methods on FlashBladeClient
- Mock handler at internal/testmock/handlers/targets.go registered under /api/2.22/targets
- 6+ unit tests in internal/client/targets_test.go, all passing
- `go build ./...` and `make lint` clean
</success_criteria>

<output>
After completion, create `.planning/phases/36-target-resource/36-01-SUMMARY.md`
</output>
