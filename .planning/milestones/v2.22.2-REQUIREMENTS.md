# Milestone v2.22.2 Requirements — Directory Service Roles & Role Mappings

**Status:** ✅ ALL SATISFIED (22/22)
**Archived:** 2026-04-17
**Source of truth:** `.planning/phases/50-directory-service-roles-role-mappings/50-VERIFICATION.md` + `.planning/phases/50.1-fix-directory-service-role-post-missing-names-query-param/50.1-VERIFICATION.md`

## Traceability

### DSR — Directory Service Role resource (6/6)

| Req ID  | Description | Plan(s) | Status | Evidence |
|---------|-------------|---------|--------|----------|
| DSR-01  | Create role via `flashblade_directory_service_role` referencing a built-in role by name (`array_admin`, `storage_admin`, `ops_admin`, `readonly`), backed by `POST /directory-services/roles` | 50-01, 50-03, 50.1-01, 50.1-02 | ✓ Complete | `PostDirectoryServiceRole(ctx, name, body)` emits `?names=<escaped>`; resource requires user-supplied `name` (post-50.1 fix); live-array UAT on par5/pa7 confirmed HTTP 200 |
| DSR-02  | PATCH on `group` and `group_base` without resource replacement; change to `role` triggers `RequiresReplace` | 50-01, 50-03 | ✓ Complete | `DirectoryServiceRolePatch` struct; `RequiresReplace` plan modifier on `role.name` |
| DSR-03  | Destroy via `DELETE /directory-services/roles?names=<name>` | 50-01 | ✓ Complete | `DeleteDirectoryServiceRole(ctx, name)` emits `?names=`; mock handler 200 on match, 404 on miss |
| DSR-04  | Import by name; `nullTimeoutsValue()`; composite ID not needed (single-key resource) | 50-03 | ✓ Complete | `ImportState` uses `req.ID` as name; `nullTimeoutsValue()` initialized; TestUnit_DirectoryServiceRoleResource_Import passes |
| DSR-05  | Drift detection on `group`, `group_base`, `role.name`, `management_access_policies` list via `tflog.Debug {resource, field, was, now}` | 50-03 | ✓ Complete | 4 drift calls in Read; keys match convention |
| DSR-06  | Read-only data source `flashblade_directory_service_role` with required `name` lookup | 50-03 | ✓ Complete | `TestUnit_DirectoryServiceRoleDataSource_Basic` passes; all 6 fields populated |

### DSRM — Management Access Policy ↔ Directory Service Role membership (5/5)

| Req ID  | Description | Plan(s) | Status | Evidence |
|---------|-------------|---------|--------|----------|
| DSRM-01 | Create association via `POST /management-access-policies/directory-services/roles` with `?policy_names=&member_names=` query params | 50-01, 50-04 | ✓ Complete | `PostManagementAccessPolicyDirectoryServiceRoleMembership` emits both query params; note: `member_names` (not `role_names`) per real-array contract (fix in `05faac1`) |
| DSRM-02 | Destroy via `DELETE /management-access-policies/directory-services/roles` — severs link only, neither policy nor role deleted | 50-01, 50-04 | ✓ Complete | Client Delete + mock handler tested; subsequent plan on underlying DSR and policy shows 0 diff |
| DSRM-03 | Import by composite ID `<role_name>/<policy_name>` (slash separator per D-05 supersession) | 50-04 | ✓ Complete | `ImportState` parses `parseCompositeID(req.ID, 2)` with role first; handles role names containing `/` (e.g. `pure:policy/array_admin`) |
| DSRM-04 | Both `policy` and `role` trigger `RequiresReplace` on change (Update returns error) | 50-04 | ✓ Complete | Plan modifiers present; Update explicitly returns error |
| DSRM-05 | Read verifies association via `GET …?policy_names=<p>&member_names=<r>`; empty list → `resp.State.RemoveResource(ctx)` | 50-04 | ✓ Complete | `TestUnit_…_MissingAssociation` verifies RemoveResource on 404/empty |

### DOC — Documentation (3/3)

| Req ID  | Description | Plan(s) | Status | Evidence |
|---------|-------------|---------|--------|----------|
| DOC-01  | HCL examples for `flashblade_directory_service_role` (`resource.tf` + `import.sh` + `data-source.tf`) | 50-05, 50.1-03 | ✓ Complete | All 3 files present; `resource.tf` uses `name = "…"` (post-50.1); `import.sh` uses plain name |
| DOC-02  | HCL examples for `flashblade_management_access_policy_directory_service_role_membership` (`resource.tf` + `import.sh`) | 50-05 | ✓ Complete | `import.sh` format: `terraform import … <role_name>/<policy_name>`; D-05 supersession comment present |
| DOC-03  | `make docs` regenerates 3 new docs (role resource + data source + membership resource) with no manual edits | 50-05, 50.1-03 | ✓ Complete | `docs/resources/directory_service_role.md`, `docs/data-sources/directory_service_role.md`, `docs/resources/management_access_policy_directory_service_role_membership.md` all present and idempotent |

### QA — Quality gates (8/8)

| Req ID  | Description | Plan(s) | Status | Evidence |
|---------|-------------|---------|--------|----------|
| QA-01   | ≥ 4 `TestUnit_DirectoryServiceRole_*` client tests | 50-01 | ✓ Complete | 4 tests: Get_Found, Get_NotFound, Post (name + `?names=`), Patch |
| QA-02   | ≥ 3 `TestUnit_ManagementAccessPolicyDirectoryServiceRoleMembership_*` client tests | 50-01 | ✓ Complete | 3 tests covering Post/Get/Delete with query param assertions |
| QA-03   | ≥ 3 `TestUnit_DirectoryServiceRoleResource_*` tests (Lifecycle, Import, DriftDetection) | 50-03, 50.1-02 | ✓ Complete | 3 tests present + StateUpgrade_V0toV1 test added in 50.1 |
| QA-04   | ≥ 3 membership resource tests (Lifecycle, Import, MissingAssociation) | 50-04 | ✓ Complete | All 3 present and passing |
| QA-05   | ≥ 1 `TestUnit_DirectoryServiceRoleDataSource_Basic` | 50-03 | ✓ Complete | 1 test with 6-field assertions |
| QA-06   | `make test` ≥ 812 baseline | 50-05, 50.1-03 | ✓ Complete | 818 tests pass (20 above baseline from phase 50 + 50.1 combined) |
| QA-07   | `make lint` clean; all 4 interface assertions on resources; empty `UpgradeState` at Version 0 (DSR bumped to v1 in 50.1) | 50-05, 50.1-03 | ✓ Complete | 0 lint issues; assertions verified; DSR now at Version 1 with v0→v1 upgrader |
| QA-08   | `ROADMAP.md` updated (row in Implemented/Array Administration; counters refreshed 42→44, 73%→75%); `CONVENTIONS.md` baseline updated | 50-05, 50.1-03 | ✓ Complete | Both rows present at lines 100-101; Last updated 2026-04-17; Provider version v2.22.2; CONVENTIONS baseline 818 |

**Totals:** 22/22 satisfied · 0 partial · 0 unsatisfied · 0 orphaned.

## Notes

- Requirement IDs used four prefixes: `DSR-` (Directory Service Role, 6 functional), `DSRM-` (Role Membership, 5 functional), `DOC-` (documentation, 3), `QA-` (quality gates, 8).
- All requirements were validated programmatically (unit tests + lint + docs regen) AND via real-array UAT on par5/pa7 (commit `9441f26`). The live-array testing is what surfaced the `?names=` bug that led to Phase 50.1 being inserted.
- Supersessions during the milestone:
  - **D-03 → superseded by 50.1:** "Name is server-generated" design was wrong; DSR POST requires `?names=<name>` per real-array behavior.
  - **Composite ID separator `:` → superseded by D-05:** Changed to `/` because role names can contain `:` (e.g. `pure:policy/array_admin`).
- No requirement was dropped or adjusted in scope. All 22 requirements shipped as originally defined.

---

_Archived from Phase 50 + 50.1 VERIFICATION.md files on 2026-04-17 during `/gsd:complete-milestone v2.22.2`._
