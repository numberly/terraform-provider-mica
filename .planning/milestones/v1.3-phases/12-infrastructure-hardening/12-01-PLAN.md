---
phase: 12-infrastructure-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/provider/filesystem_resource.go
  - internal/provider/object_store_account_resource.go
  - internal/provider/bucket_resource.go
  - internal/provider/object_store_access_key_resource.go
  - internal/provider/nfs_export_policy_resource.go
  - internal/provider/nfs_export_policy_rule_resource.go
  - internal/provider/smb_share_policy_resource.go
  - internal/provider/smb_share_policy_rule_resource.go
  - internal/provider/smb_client_policy_resource.go
  - internal/provider/smb_client_policy_rule_resource.go
  - internal/provider/snapshot_policy_resource.go
  - internal/provider/snapshot_policy_rule_resource.go
  - internal/provider/network_access_policy_resource.go
  - internal/provider/network_access_policy_rule_resource.go
  - internal/provider/object_store_access_policy_resource.go
  - internal/provider/object_store_access_policy_rule_resource.go
  - internal/provider/quota_user_resource.go
  - internal/provider/quota_group_resource.go
  - internal/provider/array_dns_resource.go
  - internal/provider/array_ntp_resource.go
  - internal/provider/array_smtp_resource.go
  - internal/provider/file_system_export_resource.go
  - internal/provider/object_store_account_export_resource.go
  - internal/provider/server_resource.go
  - internal/provider/object_store_virtual_host_resource.go
  - internal/provider/s3_export_policy_resource.go
  - internal/provider/s3_export_policy_rule_resource.go
  - internal/provider/syslog_server_resource.go
autonomous: true
requirements: [MIG-01, MIG-02]

must_haves:
  truths:
    - "Every resource schema declares SchemaVersion: 0"
    - "Every resource implements UpgradeState returning an empty slice"
    - "go build ./... compiles without errors"
    - "go test ./... passes without failures"
  artifacts:
    - path: "internal/provider/*_resource.go"
      provides: "SchemaVersion 0 and UpgradeState on all 28 resources"
      contains: "SchemaVersion: 0"
  key_links:
    - from: "internal/provider/*_resource.go Schema()"
      to: "schema.Schema{}"
      via: "SchemaVersion field in struct literal"
      pattern: "SchemaVersion:\\s*0"
    - from: "internal/provider/*_resource.go"
      to: "resource.ResourceWithUpgradeState interface"
      via: "UpgradeState method returning empty map"
      pattern: "func \\(r \\*\\w+\\) UpgradeState"
---

<objective>
Add state migration framework (SchemaVersion 0 + UpgradeState) to all 28 resources.

Purpose: Prepares every resource for future schema migrations without performing any actual migration. Major providers (AWS, Cloudflare) set SchemaVersion from day one so that state upgrades can be added incrementally later without a breaking change.

Output: All 28 resource files updated with SchemaVersion: 0 in Schema() and an UpgradeState method returning an empty map.
</objective>

<execution_context>
@/home/gule/.claude/get-shit-done/workflows/execute-plan.md
@/home/gule/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@internal/provider/provider.go (full list of 28 resources at line 267-298)
@internal/provider/bucket_resource.go (example Schema() method pattern at line 68)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SchemaVersion 0 and UpgradeState to all 28 resources</name>
  <files>
    internal/provider/filesystem_resource.go
    internal/provider/object_store_account_resource.go
    internal/provider/bucket_resource.go
    internal/provider/object_store_access_key_resource.go
    internal/provider/nfs_export_policy_resource.go
    internal/provider/nfs_export_policy_rule_resource.go
    internal/provider/smb_share_policy_resource.go
    internal/provider/smb_share_policy_rule_resource.go
    internal/provider/smb_client_policy_resource.go
    internal/provider/smb_client_policy_rule_resource.go
    internal/provider/snapshot_policy_resource.go
    internal/provider/snapshot_policy_rule_resource.go
    internal/provider/network_access_policy_resource.go
    internal/provider/network_access_policy_rule_resource.go
    internal/provider/object_store_access_policy_resource.go
    internal/provider/object_store_access_policy_rule_resource.go
    internal/provider/quota_user_resource.go
    internal/provider/quota_group_resource.go
    internal/provider/array_dns_resource.go
    internal/provider/array_ntp_resource.go
    internal/provider/array_smtp_resource.go
    internal/provider/file_system_export_resource.go
    internal/provider/object_store_account_export_resource.go
    internal/provider/server_resource.go
    internal/provider/object_store_virtual_host_resource.go
    internal/provider/s3_export_policy_resource.go
    internal/provider/s3_export_policy_rule_resource.go
    internal/provider/syslog_server_resource.go
  </files>
  <action>
For each of the 28 resource files listed above, make two changes:

**Change 1 — SchemaVersion in Schema():**
In the Schema() method, add `Version: 0` to the `schema.Schema{}` struct literal. Place it right after the `Description` field. Example pattern from bucket_resource.go:

```go
resp.Schema = schema.Schema{
    Description: "Manages a FlashBlade object store bucket.",
    Version:     0,
    Attributes: map[string]schema.Attribute{
```

The field name is `Version` (not `SchemaVersion`) in the terraform-plugin-framework `schema.Schema` struct.

**Change 2 — UpgradeState method + interface assertion:**

Add a compile-time interface assertion near the existing `var _` lines at the top of each file:

```go
var _ resource.ResourceWithUpgradeState = &bucketResource{}
```

Add an `UpgradeState` method to the resource struct. Place it right after the Schema() method:

```go
func (r *bucketResource) UpgradeState(_ context.Context) map[int64]resource.StateUpgrader {
    return map[int64]resource.StateUpgrader{}
}
```

Replace `bucketResource` with the actual struct name for each file. The method returns an empty map — no actual state upgraders yet.

**Process:** Work through all 28 files systematically. The full list of resource files and their struct names can be derived from provider.go lines 267-298 (factory functions map 1:1 to struct names, e.g., NewBucketResource -> bucketResource).

**Do NOT change any other code in these files.** This is purely additive — add Version field, add interface var, add UpgradeState method.
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && go build ./... && go test ./... -count=1 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All 28 resource files have `Version: 0` in their schema.Schema struct
    - All 28 resource files have a `var _ resource.ResourceWithUpgradeState` assertion
    - All 28 resource files have an `UpgradeState` method returning an empty map
    - `go build ./...` compiles clean
    - `go test ./...` passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify completeness with grep audit</name>
  <files>None (read-only verification)</files>
  <action>
Run a completeness audit to verify every resource file was updated:

1. Count resource files with `Version:` in Schema: `rg -l 'Version:\s*0' internal/provider/*_resource.go | wc -l` — must be 28
2. Count resource files with UpgradeState method: `rg -l 'func \(r \*\w+\) UpgradeState' internal/provider/*_resource.go | wc -l` — must be 28
3. Count interface assertions: `rg -l 'ResourceWithUpgradeState' internal/provider/*_resource.go | wc -l` — must be 28
4. Verify no file was missed: diff the set of `*_resource.go` files against the set with Version field. Any difference = missed file.

If any count is not 28, fix the missing file(s) before marking done.
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && test $(rg -l 'Version:\s*0' internal/provider/*_resource.go | wc -l) -eq 28 && test $(rg -l 'func \(r \*\w+\) UpgradeState' internal/provider/*_resource.go | wc -l) -eq 28 && echo "PASS: all 28 resources updated"</automated>
  </verify>
  <done>
    - grep confirms exactly 28 resource files have Version: 0
    - grep confirms exactly 28 resource files have UpgradeState method
    - No resource file was missed
  </done>
</task>

</tasks>

<verification>
1. `go build ./...` compiles without errors
2. `go test ./...` passes without failures
3. All 28 resource files contain `Version: 0` in their Schema
4. All 28 resource files implement `ResourceWithUpgradeState`
</verification>

<success_criteria>
- Every one of the 28 resources listed in provider.go Resources() has SchemaVersion 0 and UpgradeState wired
- Existing tests continue to pass (no behavioral change)
- Code compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/12-infrastructure-hardening/12-01-SUMMARY.md`
</output>
