---
phase: 13-documentation-and-sensitive-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/resources/flashblade_file_system/import.sh
  - examples/resources/flashblade_file_system_export/import.sh
  - examples/resources/flashblade_object_store_account_export/import.sh
  - examples/resources/flashblade_object_store_virtual_host/import.sh
  - examples/resources/flashblade_s3_export_policy/import.sh
  - examples/resources/flashblade_s3_export_policy_rule/import.sh
  - examples/resources/flashblade_server/import.sh
  - examples/resources/flashblade_smb_client_policy/import.sh
  - examples/resources/flashblade_smb_client_policy_rule/import.sh
  - examples/resources/flashblade_syslog_server/import.sh
  - docs/
autonomous: true
requirements:
  - DOC-01
  - DOC-02

must_haves:
  truths:
    - "All 27 importable resources have an import.sh file in their examples directory"
    - "object_store_access_key has NO import.sh (import not supported)"
    - "tfplugindocs regenerates docs with import sections included"
    - "Each import.sh uses the correct ID format matching the resource's ImportState implementation"
  artifacts:
    - path: "examples/resources/flashblade_file_system/import.sh"
      provides: "Import example for file_system"
      contains: "terraform import flashblade_file_system"
    - path: "examples/resources/flashblade_s3_export_policy_rule/import.sh"
      provides: "Import example for s3_export_policy_rule (composite ID)"
      contains: "policy_name/rule_index"
    - path: "examples/resources/flashblade_smb_client_policy_rule/import.sh"
      provides: "Import example for smb_client_policy_rule (composite ID)"
      contains: "policy_name/rule_name"
    - path: "docs/resources/file_system.md"
      provides: "Generated doc with import section"
      contains: "import"
  key_links:
    - from: "examples/resources/*/import.sh"
      to: "docs/resources/*.md"
      via: "go generate (tfplugindocs)"
      pattern: "terraform import"
---

<objective>
Create import.sh example files for the 10 remaining importable resources (17 already exist) and regenerate provider documentation with tfplugindocs so all import sections appear in the Registry docs.

Purpose: DOC-01 + DOC-02 -- every importable resource gets Registry-ready import documentation
Output: 10 new import.sh files + regenerated docs/ directory
</objective>

<execution_context>
@/home/gule/.claude/get-shit-done/workflows/execute-plan.md
@/home/gule/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create import.sh files for 10 remaining importable resources</name>
  <files>
    examples/resources/flashblade_file_system/import.sh
    examples/resources/flashblade_file_system_export/import.sh
    examples/resources/flashblade_object_store_account_export/import.sh
    examples/resources/flashblade_object_store_virtual_host/import.sh
    examples/resources/flashblade_s3_export_policy/import.sh
    examples/resources/flashblade_s3_export_policy_rule/import.sh
    examples/resources/flashblade_server/import.sh
    examples/resources/flashblade_smb_client_policy/import.sh
    examples/resources/flashblade_smb_client_policy_rule/import.sh
    examples/resources/flashblade_syslog_server/import.sh
  </files>
  <action>
Create import.sh files for the 10 resources that are missing them. Follow the exact pattern used by existing import.sh files (one comment line + one terraform import line). Do NOT create import.sh for flashblade_object_store_access_key (it does not support import).

Each file must match the resource's ImportState implementation:

Simple name imports (import by name):
- flashblade_file_system: `terraform import flashblade_file_system.example my-filesystem`
- flashblade_s3_export_policy: `terraform import flashblade_s3_export_policy.example my-s3-policy`
- flashblade_server: `terraform import flashblade_server.example my-server`
- flashblade_smb_client_policy: `terraform import flashblade_smb_client_policy.example my-smb-client-policy`
- flashblade_syslog_server: `terraform import flashblade_syslog_server.example my-syslog-server`

Combined name imports (import by combined path):
- flashblade_file_system_export: `terraform import flashblade_file_system_export.example my-filesystem/my-export` (combined name format: filesystem/export_name)
- flashblade_object_store_account_export: `terraform import flashblade_object_store_account_export.example my-account/my-export` (combined name format: account/export_name)
- flashblade_object_store_virtual_host: `terraform import flashblade_object_store_virtual_host.example s3.example.com` (server-assigned name, typically the hostname)

Composite ID imports (import by policy_name/rule_identifier):
- flashblade_s3_export_policy_rule: comment `# Import using composite ID: policy_name/rule_index`, command `terraform import flashblade_s3_export_policy_rule.example my-s3-policy/0`
- flashblade_smb_client_policy_rule: comment `# Import using composite ID: policy_name/rule_name`, command `terraform import flashblade_smb_client_policy_rule.example my-smb-client-policy/rule-0`

For simple name imports, use comment: `# Import by name`
For combined name imports, use comment: `# Import by combined name: filesystem/export_name` (adjust per resource)
Each file must end with a trailing newline.
  </action>
  <verify>
    <automated>count=$(find examples/resources/ -name import.sh | wc -l); echo "import.sh count: $count"; test "$count" -eq 27 && echo "PASS: 27 import.sh files" || echo "FAIL: expected 27"; test ! -f examples/resources/flashblade_object_store_access_key/import.sh && echo "PASS: no import for access_key" || echo "FAIL: access_key should not have import.sh"</automated>
  </verify>
  <done>All 27 importable resources have import.sh files with correct syntax matching their ImportState implementation. flashblade_object_store_access_key has no import.sh.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate provider documentation with tfplugindocs</name>
  <files>docs/</files>
  <action>
Run `go generate ./...` from the project root to regenerate all provider documentation via tfplugindocs (the go:generate directive is in main.go). This will pick up the new import.sh files and add import sections to the generated docs.

After generation, verify:
1. Check that at least 5 resource doc files in docs/resources/ contain an "Import" section (grep for "import" or "terraform import")
2. Check that docs/resources/object_store_access_key.md does NOT contain an import section (since it has no import.sh)
3. Verify no errors from the generation command

If go generate fails, check that tfplugindocs is available: `go install github.com/hashicorp/terraform-plugin-docs/cmd/tfplugindocs@latest` then retry.
  </action>
  <verify>
    <automated>grep -l "terraform import" docs/resources/*.md | wc -l | xargs -I{} sh -c 'echo "docs with import: {}"; test {} -ge 27 && echo "PASS" || echo "FAIL: expected >= 27"'; grep -c "terraform import" docs/resources/object_store_access_key.md 2>/dev/null && echo "FAIL: access_key should not have import" || echo "PASS: access_key has no import section"</automated>
  </verify>
  <done>docs/ directory regenerated with import sections for all 27 importable resources. object_store_access_key doc has no import section.</done>
</task>

</tasks>

<verification>
1. `find examples/resources/ -name import.sh | wc -l` returns 27
2. `! test -f examples/resources/flashblade_object_store_access_key/import.sh` succeeds
3. `grep -rl "terraform import" docs/resources/ | wc -l` returns >= 27
4. `go build ./...` still compiles
</verification>

<success_criteria>
- 27 import.sh files exist (one per importable resource)
- No import.sh for object_store_access_key
- docs/ regenerated with import sections visible in resource docs
- go build passes
</success_criteria>

<output>
After completion, create `.planning/phases/13-documentation-and-sensitive-data/13-01-SUMMARY.md`
</output>
