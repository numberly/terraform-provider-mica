---
phase: 35-object-store-users
plan: 04
type: execute
wave: 3
depends_on:
  - 35-02
  - 35-03
files_modified:
  - internal/provider/object_store_user_resource_test.go
  - internal/provider/object_store_user_data_source_test.go
  - internal/provider/object_store_user_policy_resource_test.go
  - ROADMAP.md
autonomous: false
requirements:
  - OSU-01
  - OSU-02
  - OSU-03
  - OSU-04
  - OSU-05
  - OSU-06
  - OSU-07

must_haves:
  truths:
    - "Mocked provider tests exercise full lifecycle for the resource: create, read, plan-convergence, import, destroy"
    - "Mocked provider tests exercise the user_policy member: create, read, destroy"
    - "Data source test verifies that all attributes are populated from mock API"
    - "All tests pass with go test ./..."
    - "ROADMAP.md updated with object store user resources"
  artifacts:
    - path: "internal/provider/object_store_user_resource_test.go"
      provides: "TestMocked_ObjectStoreUser_Lifecycle and TestMocked_ObjectStoreUser_Import tests"
    - path: "internal/provider/object_store_user_data_source_test.go"
      provides: "TestMocked_ObjectStoreUser_DataSource test"
    - path: "internal/provider/object_store_user_policy_resource_test.go"
      provides: "TestMocked_ObjectStoreUserPolicy_Lifecycle test"
  key_links:
    - from: "internal/provider/object_store_user_resource_test.go"
      to: "testNewMockedProvider()"
      via: "provider setup in each test"
      pattern: "testNewMockedProvider"
    - from: "internal/provider/object_store_user_resource_test.go"
      to: "internal/testmock/handlers/object_store_users.go"
      via: "mock server handling /api/2.22/object-store-users"
      pattern: "object-store-users"
---

<objective>
Write mocked provider tests for all three resources/data sources implemented in Plans 02 and 03, then update ROADMAP.md.

Purpose: The three-tier testing policy requires mocked provider tests (resource.UnitTest with mock server) for every new resource and data source.
Output: Three test files covering full lifecycle scenarios, plus ROADMAP.md updated per project conventions.
</objective>

<execution_context>
@/home/gule/.claude/get-shit-done/workflows/execute-plan.md
@/home/gule/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/35-object-store-users/35-02-SUMMARY.md
@.planning/phases/35-object-store-users/35-03-SUMMARY.md
</context>

<interfaces>
<!-- Test helper patterns — from existing provider tests -->

From internal/provider/object_store_account_resource_test.go (representative pattern):
```go
func TestMocked_ObjectStoreAccount_Lifecycle(t *testing.T) {
    server, providerCfg := testNewMockedProvider(t)
    _ = server
    resource.UnitTest(t, resource.TestCase{
        ProtoV6ProviderFactories: providerCfg,
        Steps: []resource.TestStep{
            {
                Config: `...HCL...`,
                Check: resource.ComposeTestCheckFunc(
                    resource.TestCheckResourceAttr("flashblade_object_store_account.test", "name", "testaccount"),
                    resource.TestCheckResourceAttrSet("flashblade_object_store_account.test", "id"),
                ),
            },
        },
    })
}
```

Import test pattern (from existing resources):
```go
{
    ResourceName:      "flashblade_object_store_user.test",
    ImportState:       true,
    ImportStateVerify: true,
},
```

Plan convergence — add a second identical step after create to verify 0 diff:
```go
{
    Config:   sameHCL,
    PlanOnly: true,
    ExpectNonEmptyPlan: false,
},
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write mocked provider tests for resource and data source</name>
  <files>internal/provider/object_store_user_resource_test.go, internal/provider/object_store_user_data_source_test.go</files>
  <action>
    Create internal/provider/object_store_user_resource_test.go with two test functions:

    TestMocked_ObjectStoreUser_Lifecycle:
    Steps:
    1. Create: HCL `resource "flashblade_object_store_user" "test" { name = "testaccount/testuser" }` — check name attr and id is set
    2. Plan convergence: same HCL, PlanOnly: true, ExpectNonEmptyPlan: false — verifies Read produces no drift
    3. Import: ResourceName="flashblade_object_store_user.test", ImportState: true, ImportStateVerify: true

    TestMocked_ObjectStoreUser_FullAccess:
    Steps:
    1. Create: HCL with `full_access = true` — check full_access == "true"
    2. Plan convergence: same HCL, PlanOnly: true, ExpectNonEmptyPlan: false

    Create internal/provider/object_store_user_data_source_test.go:

    TestMocked_ObjectStoreUser_DataSource:
    - Pre-condition: create the user via resource first (in same config), then read it via data source
    - Or: if testNewMockedProvider mock pre-populates, just read directly; otherwise use depends_on pattern
    - Check data.flashblade_object_store_user.test.name, .id, .full_access are set correctly

    Use testNewMockedProvider(t) in every test function. Follow existing test files for import and package structure.
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && go test ./internal/provider/ -run TestMocked_ObjectStoreUser -v -count=1 2>&1</automated>
  </verify>
  <done>All TestMocked_ObjectStoreUser* tests pass with zero failures</done>
</task>

<task type="auto">
  <name>Task 2: Write mocked provider test for user-policy resource, update ROADMAP.md</name>
  <files>internal/provider/object_store_user_policy_resource_test.go, ROADMAP.md</files>
  <action>
    Create internal/provider/object_store_user_policy_resource_test.go:

    TestMocked_ObjectStoreUserPolicy_Lifecycle:
    - Config creates both a flashblade_object_store_user and a flashblade_object_store_user_policy in the same HCL block
    - Policy association references user by its name directly (no depends_on needed since policy resource uses user_name string, not resource ref)
    - HCL example:
      ```hcl
      resource "flashblade_object_store_user" "test" {
        name = "testaccount/policyuser"
      }
      resource "flashblade_object_store_user_policy" "test" {
        user_name   = "testaccount/policyuser"
        policy_name = "test-access-policy"
      }
      ```
    - NOTE: mock handler for /object-store-access-policies may need the policy to exist. Check if existing mock handlers require pre-existing policies. If they do, add a `flashblade_object_store_access_policy` resource to the HCL config too, or use depends_on. Look at existing policy member tests for the pattern.
    - Steps: create (check user_name and policy_name attrs), plan convergence (PlanOnly+ExpectNonEmptyPlan:false), import (ImportState:true, ImportStateVerify:true with ImportStateIdFunc returning "testaccount/policyuser/test-access-policy")

    Update ROADMAP.md per project conventions (CLAUDE.md "Roadmap Maintenance"):
    1. Find the "Object Store Users" entry in the "Not Implemented" section
    2. Move it to the appropriate "Implemented" subsection
    3. Fill in: Resource name `flashblade_object_store_user`, Data Source Yes, Status Done, Notes "CRD only (no PATCH); full_access optional; import supported"
    4. Add entry for `flashblade_object_store_user_policy` member resource: Data Source No, Status Done, Notes "user to policy association; import format: account/username/policyname"
    5. Update header counters (covered count, coverage percentage)
    6. Update "Last updated" date to 2026-03-31 and Provider version if applicable

    Do NOT run `make docs` in this plan — docs generation happens in the checkpoint verification step.
  </action>
  <verify>
    <automated>cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && go test ./internal/provider/ -run TestMocked_ObjectStoreUserPolicy -v -count=1 2>&1</automated>
  </verify>
  <done>TestMocked_ObjectStoreUserPolicy_Lifecycle passes; ROADMAP.md contains flashblade_object_store_user and flashblade_object_store_user_policy entries in the Implemented section</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify full object store users implementation</name>
  <action>Run go test ./... -count=1 and make lint to verify all tests pass and no new linting violations exist. Run make docs to regenerate Terraform documentation from the new resource schemas.</action>
  <what-built>
    Full object store users implementation:
    - client methods (typed GetObjectStoreUser, PostObjectStoreUser + user-policy association)
    - flashblade_object_store_user resource (Create/Read/Delete/Import, drift detection)
    - flashblade_object_store_user data source
    - flashblade_object_store_user_policy member resource (Create/Read/Delete/Import)
    - Mocked provider tests for all three
    - ROADMAP.md updated
  </what-built>
  <how-to-verify>
    1. Run full test suite: `cd /home/gule/Workspace/team-infrastructure/terraform-provider-flashblade && go test ./... -count=1` — expect 0 failures
    2. Run linter: `make lint` — expect 0 new violations
    3. Run docs generation: `make docs` — expect no errors
    4. Review ROADMAP.md: confirm flashblade_object_store_user and flashblade_object_store_user_policy appear in Implemented section
    5. Spot-check the resource.tf example: `cat examples/resources/flashblade_object_store_user/resource.tf`
  </how-to-verify>
  <resume-signal>Type "approved" to proceed, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
go test ./... -count=1 passes with zero failures
make lint passes with zero new violations
ROADMAP.md grep "flashblade_object_store_user" shows Implemented entries
</verification>

<success_criteria>
- TestMocked_ObjectStoreUser_Lifecycle, TestMocked_ObjectStoreUser_FullAccess pass
- TestMocked_ObjectStoreUser_DataSource passes
- TestMocked_ObjectStoreUserPolicy_Lifecycle passes (with import verification)
- ROADMAP.md updated with both new resources
- go test ./... passes; make lint passes
- All 7 OSU requirements demonstrably satisfied by the test coverage
</success_criteria>

<output>
After completion, create `.planning/phases/35-object-store-users/35-04-SUMMARY.md`
</output>
