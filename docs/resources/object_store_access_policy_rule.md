---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "flashblade_object_store_access_policy_rule Resource - flashblade"
subcategory: ""
description: |-
  Manages a rule within a FlashBlade object store access policy (IAM-style S3 policy rule).
---

# flashblade_object_store_access_policy_rule (Resource)

Manages a rule within a FlashBlade object store access policy (IAM-style S3 policy rule).

## Example Usage

```terraform
resource "flashblade_object_store_access_policy_rule" "example" {
  policy_name = "terraform-s3-policy"
  name        = "allow-read"
  effect      = "allow"
  actions     = ["s3:GetObject", "s3:ListBucket"]
  resources   = ["arn:aws:s3:::my-bucket", "arn:aws:s3:::my-bucket/*"]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `actions` (List of String) List of S3 actions this rule applies to (e.g. ['s3:GetObject', 's3:PutObject']).
- `effect` (String) The effect of the rule: 'allow' or 'deny'. Read-only after creation — changing this forces a new resource.
- `name` (String) The name of the rule. Changing this forces a new resource (rules cannot be renamed).
- `policy_name` (String) The name of the object store access policy this rule belongs to. Changing this forces a new resource.
- `resources` (List of String) List of ARN-like resource patterns this rule applies to.

### Optional

- `conditions` (String) JSON-encoded IAM conditions object (use jsonencode()). Null or empty if no conditions.
- `timeouts` (Attributes) (see [below for nested schema](#nestedatt--timeouts))

### Read-Only

- `id` (String) Synthetic identifier in the form 'policy_name/rule_name'.

<a id="nestedatt--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import using composite ID: policy_name/rule_name
terraform import flashblade_object_store_access_policy_rule.example my-s3-policy/allow-read
```
