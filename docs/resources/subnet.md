---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "flashblade_subnet Resource - flashblade"
subcategory: ""
description: |-
  Manages a FlashBlade subnet.
  Example Usage
  
  resource "flashblade_subnet" "data" {
    name     = "data-subnet"
    prefix   = "10.21.200.0/24"
    gateway  = "10.21.200.1"
    mtu      = 9000
    vlan     = 2100
    lag_name = "lag0"
  }
---

# flashblade_subnet (Resource)

Manages a FlashBlade subnet.

## Example Usage

```hcl
resource "flashblade_subnet" "data" {
  name     = "data-subnet"
  prefix   = "10.21.200.0/24"
  gateway  = "10.21.200.1"
  mtu      = 9000
  vlan     = 2100
  lag_name = "lag0"
}
```

## Example Usage

```terraform
resource "flashblade_subnet" "example" {
  name     = "my-subnet"
  prefix   = "10.0.0.0/24"
  gateway  = "10.0.0.1"
  mtu      = 9000
  vlan     = 100
  lag_name = "lag1"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) The name of the subnet. Changing this forces a new resource.
- `prefix` (String) IPv4 or IPv6 subnet address in CIDR notation (e.g. 10.21.200.0/24).

### Optional

- `gateway` (String) IPv4 or IPv6 gateway address for the subnet.
- `lag_name` (String) Name of the link aggregation group (LAG) this subnet is attached to.
- `mtu` (Number) Maximum transmission unit (MTU) in bytes. Defaults to 1500.
- `timeouts` (Attributes) (see [below for nested schema](#nestedatt--timeouts))
- `vlan` (Number) VLAN ID. 0 means untagged. Defaults to 0.

### Read-Only

- `enabled` (Boolean) Whether the subnet is enabled.
- `id` (String) The unique identifier of the subnet.
- `interfaces` (List of String) List of network interface names attached to this subnet.
- `services` (List of String) List of services associated with this subnet (e.g. data, replication).

<a id="nestedatt--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import by name
terraform import flashblade_subnet.example my-subnet
```
