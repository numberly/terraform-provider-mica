---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "flashblade_network_access_policy_rule Resource - flashblade"
subcategory: ""
description: |-
  Manages a rule within a FlashBlade network access policy.
---

# flashblade_network_access_policy_rule (Resource)

Manages a rule within a FlashBlade network access policy.

## Example Usage

```terraform
resource "flashblade_network_access_policy_rule" "example" {
  policy_name = "default"
  client      = "10.0.0.0/8"
  effect      = "allow"

  # Optional: restrict to specific protocol interfaces
  interfaces = ["nfs", "smb"]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `client` (String) IP address, CIDR range, or '*' matching the clients to which this rule applies.
- `effect` (String) The effect of the rule: 'allow' or 'deny'.
- `policy_name` (String) The name of the network access policy this rule belongs to. Changing this forces a new resource.

### Optional

- `interfaces` (List of String) List of protocol interfaces this rule applies to (e.g. ['nfs', 'smb', 's3']). If empty, applies to all.
- `timeouts` (Attributes) (see [below for nested schema](#nestedatt--timeouts))

### Read-Only

- `id` (String) The unique identifier of the network access policy rule (server-assigned UUID).
- `index` (Number) The server-assigned ordering index for this rule within the policy. Used for import.
- `name` (String) The server-assigned rule identifier. Used internally for PATCH/DELETE API calls.
- `policy_version` (String) The version of the parent policy at the time this rule was last read.

<a id="nestedatt--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import using composite ID: policy_name/rule_index
terraform import flashblade_network_access_policy_rule.example default/0
```
