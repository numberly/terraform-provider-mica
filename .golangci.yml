version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - gosimple
    - ineffassign
  settings:
    govet:
      enable-all: false
  exclusions:
    rules:
      - linters:
          - staticcheck
        text: "SA1019"
